# 출격! 라인 워 (컴퓨터가 싸운다 류) — 설계 v1

> 두 번째 유즈맵. 장르: **빌딩 배정 자동 출격 경제 전투**(SC "컴퓨터가 싸운다" / Income War).
> 네모네모 디펜스(트랙 디펜스)와 메커니즘 완전 분리. 동작 중인 nemo 엔진은 건드리지 않고 **독립 모듈**로 구현.

## 핵심 루프 (확정)
- **1라인 · 대각선 돌격(칼바람식)**: 내 진영=좌하단 코너(본진+생산), 적=우상단 코너. 유닛이 대각선으로 서로 달려와 가운데서 교전.
- **빌딩 배정 생산**: 내 진영에 **생산 건물**을 짓는다. 각 건물에 **생산할 유닛 종류 + 수**를 배정.
- **출격 주기**: 일정 시간마다 모든 건물이 배정 유닛을 **일괄 소환** → 적 본진 방향으로 자동 행군·교전.
- **영웅 변환(개체수 제어)**: 같은 유닛 **10마리**가 모이면 효율 좋은 **영웅 유닛 1기**로 변환 생성.
- **멀티 로테이션**(MVP 이후): 1:1=내 유닛 매 주기 출격 / 2:2·3:3=팀원과 **2주기·3주기 교대** 생산.
- **승리**: 적 **본진 HP 0**. (교착 방지: 시간 경과 시 본진 점감 옵션)
- **카메라**: 고정 X. 양측 **최전선의 중간(핵심 교전 지점)** 을 따라가며 줌. 큰 맵의 일부만 비춤.
- **경제**: 골드 시간당 자동 수급(+처치 보너스). 인컴/건물 업그레이드.
- **보상**: 승리 시 계정 코인 소량 적립(기존 메타 연동).

## MVP 범위 (1단계)
- **vs AI**, **1라인**, 기존 유닛 스탯 재사용, **2D 캔버스 + 팔로우 카메라**.
- 영웅 변환·빌딩 배정·경제·승패까지 포함.
- **이후 단계**: PvP(기존 Supabase 멀티), 3D 렌더, 새 유닛 모델 추가, 팀 로테이션.

## 통합 방식 (nemo 무영향)
- 맵: `USEMAPS.cpu` 를 이 모드로 사용(`cfg.mode='strike'`).
- 진입: `startGameNow()`에서 `MAP.cfg.mode==='strike'` → `strikeStart()` 분기(그 외 = 기존 nemo).
- 상태: 독립 객체 `GS`(strike 전용). 자체 `strikeStep(dt)` / `strikeRender()` / `strikeEnd()`.
- 에셋: `MAP_ASSETS.cpu` 에 이 맵이 쓰는 모델만 → 진입 시 `M3D.loadMapModels`, 이탈 시 `keepOnlyMap`.
- 이 두 번째 예시로 게임플레이 모듈 인터페이스(`init/step/render/teardown`)를 정립(#2).

## 구현 증분(Increment) 계획
1. **맵 등록 + 모듈 골격 + 진입/이탈 배선** (빈 월드·카메라·본진·골드 표시까지 실행)
2. **빌딩 짓기 + 유닛 배정 UI + 출격 주기 스폰**
3. **행군 + 자동 교전(기존 유닛 스탯) + 본진 타격/승패**
4. **영웅 변환(10→1) + 경제·인컴 업글 + 간단 AI 빌드**
5. **팔로우 카메라 튜닝 + 연출**
6. (이후) PvP 동기화 / 3D / 신규 모델

## 미정·추후 결정
- 건물 종류·코스트·배정 UI 상세
- 영웅 변환 시 영웅 스탯(원본 10기 대비 효율)
- AI 빌드 난이도 곡선
- 새 유닛 모델 목록(에셋 별도 작업)

_v1 — 변경 시 갱신._
