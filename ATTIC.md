# ATTIC — 🗄 지금 화면에 없는 것

**무엇이 왜 잠겨 있고, 되살리려면 무엇을 봐야 하는지.** 한 장으로 본다.

> ⛔ **지우지 않는다.** `GAME_DIRECTION.md` §5 가 「유보는 삭제가 아니다 — 코드와 아트를 지우지 말 것」이라
> 못박고 있다. 되살릴 때의 자산이다. 여기 있는 것은 **버린 것이 아니라 치워 둔 것**이다.

---

## 0. 왜 이 문서가 있나

한 파일 안에 살아 있는 코드와 죽은 코드가 섞여 있으면, 다른 작업자가 그 파일을 열었을 때
**어느 쪽이 지금 쓰는 것인지 알 수 없다.** 실제로 옛 화면과 옛 디자인이 여러 번 되살아났다.

그래서 둘로 갈랐다.

| | 어디에 | 무엇 |
|---|---|---|
| **코드** | `js/99-attic.js` | 도달할 수 없는 함수를 통째로 옮겨 둔다(맨 마지막에 로드) |
| **길** | 각 표(`NAV_TREE`·`HB_MORE`…) | 항목만 뺐다 — 함수와 화면은 그대로 살아 있다 |

⚠ **`<script>` 순서는 건드리지 않았다.** 이 프로젝트는 전역 하나를 공유하고 태그 순서가 곧 동작이라,
순서를 바꾸는 것은 금지돼 있다. 다락은 **맨 끝에 추가**했을 뿐이다 — 아무도 여기 것을 부르지 않으므로
순서가 무의미하고, 거꾸로 여기 것이 살아 있는 코드를 부르는 것은 런타임이라 문제가 없다.

---

## 1. 길만 닫은 것 — 코드는 원래 자리에 있다

화면에서 들어갈 방법이 없다. **함수도 화면 마크업도 그대로**다.
(⚠ **마을은 여기서 §3-B 로 옮겼다** — 길만 닫은 게 아니라 마크업·코드째 다락으로 갔다.)

| 무엇 | 왜 닫았나 | 되살리려면 |
|---|---|---|
| **성장**(`hbOpenGrow`) | 환생·진화는 §5-A 유보 | `HB_MORE` 에 `grow` |
| **토벌**(`openDungeonHub`) | §5-D 유보 — 도달 불가 상태로 문서에 명시됨 | `HB_MORE` 에 `dg` ⚠ 스모크 「용어 분리」가 되살아남을 잡는다 |
| **일일 퀘스트**(`openDaily`) | 14종 중 9종이 **옛 사냥터 계측**이라 캠프에선 거의 안 찬다 → 반복형 대신 **가이드**로 바꿨다 | 퀘스트 목록을 캠프 것으로 갈고 `dqNote` 계측을 캠프에 붙인 뒤 `HB_MORE` 에 `daily` |
| **건설**(`hbBuildStart`) | **이미 죽은 칸이었다** — `if(!_hb) return` 이라 캠프에선 눌러도 아무 일도 안 했다 | 캠프용 건설 입구를 새로 만든다(옛 것은 사냥터 전용) |
| **캐릭터·정비**(`openUpgScreen`/`openGear`) | 스탯·환생·스킬은 §5-A · 장비·펫·동료는 §5-B 유보 | `NAV_TREE` 에 되돌린다 ⚠ 스모크 「하단 네비 네 칸」이 잡는다 |

## 2. 다락으로 옮긴 것 — `js/99-attic.js`

**정적 분석 ∩ 런타임 계측** 둘 다에서 「한 번도 안 불림」으로 나온 것만 옮겼다(19개).
각 함수 위에 `// ── [원래 파일] 이름` 이 붙어 있으니 되돌릴 자리를 바로 안다.

| 원래 파일 | 무엇 | 성격 |
|---|---|---|
| `04-profile.js` | `profBuyItem` `profUnlockNeed` `profRebGrant` `profPetMats` `profClaimOffline` | 옛 상점 구매 · 해금 안내 · 환생 지급 · 펫 재료 · 방치 정산 |
| `05-home.js` | `hbGoRound` | 옛 라운드 목록에서 고르기 |
| `06-daily.js` | `dqRwTx` | 옛 보상 표기(아이콘판) — 지금은 `dqRwIco`/`dqRwPlain` 을 쓴다 |
| `08-hunt.js` | `HB_DG_ATK` `HB_DG_XP` `hbRoundK` `_mgK` `twApplyChar` `profPickSlot` | 옛 사냥터 곡선 · 마을 걷기 · 장비 슬롯 고르기 |
| `09-dungeon.js` | `dgStgHTML` | 옛 토벌 단계 표기 |
| `12-appshell.js` | `authIsGuest` `playerLeave` | 옛 게스트 판정 · 옛 퇴장 처리 |
| `14-input-fx.js` | `_btPickerHTML` `btAdd` | 옛 건설 고르개 |
| `18-strike.js` | `strikeWpnTotal` | 옛 무기 합계 |
| `08-hunt.js` | **마을 47개**(월드·카메라·건설·친구 허브·DM·마을 3D) | 🏘 §3-B 참고 — 통째로 갔다 |

## 3. 옮기지 **않은** 것과 그 이유

| 무엇 | 왜 남겼나 |
|---|---|
| **`19-camp.js` 의 환생 계열**(`campRebirth` 등 11개) | **지금 다른 작업자가 만들고 있다.** 아직 안 이어졌을 뿐 곧 쓰인다 |
| **친구·파티**(`friendSortCmp` 등) | 화면에 실재한다 — 스모크가 그 경로를 안 돌아서 「안 불림」으로 보였을 뿐 |
| **`17-build-cards.js`** | ⛔ 관리자 탭·오토배틀과 공유하는 금지 파일 |
| **부스트**(`hbOpenBoost`) | 사용자가 남기기로 정했다. ⚠ 다만 **지금 효과가 없다** — 배율이 옛 사냥터 전투에만 걸려 있고 캠프 수급(`campTapGain`/`campGatherMul`)에는 안 들어간다 |

## 3-B. 🏘 마을은 통째로 다락으로 갔다 — 2026-08-27

**마을은 안 쓴다.** 그런데 화면·시트·이동 엔진·친구 목록·DM 이 전부 살아 있는 파일에 섞여 있어
다른 작업자가 계속 그걸 되가져왔다. 통째로 갈랐다.

| 무엇 | 어디로 |
|---|---|
| `townScreen`(마을 화면) · `twChat`(채팅 시트 껍데기) · `twSocial`(친구 시트) · `dmChat`(1:1 DM) | `docs/attic/dead-markup.html` |
| 마을 함수 **47개**(월드·카메라·건설·친구 허브·DM·마을 3D) | `js/99-attic.js` |
| `APP_SCREENS` · `CUR_SCREENS` 의 `townScreen` | 목록에서 뺐다 |

### ⚠ 채팅 알맹이(`.msSocial`)는 **살렸다**

마을 채팅 시트 안에 있던 `.msSocial` 은 **유즈맵 하단 도크가 빌려 쓰는 유일한 소셜 DOM** 이다.
시트째 지웠으면 유즈맵 채팅이 통째로 죽는다. 그래서 **집을 `#msSocialDock` 으로 옮겼다** —
`mapDockSocial()` 이 하던 `dock.appendChild(so)` 와 같은 자리다.

스모크 「다락: 마을 화면이 되살아나지 않았다」가 **둘 다** 잰다 — 마을 7개가 없는 것과,
`.msSocial` 이 **정확히 하나**이고 도크 안에 있는 것.

### 남긴 훅 8개

실제로 아직 불리는 것만 제자리에 뒀다(런타임 계측 결과):
`twLeave` · `twStopLoop` · `twCloseChat` · `twCloseSocial` · `tw3dDetach` · `twGoMap` · `openTownPanel` · `closeTownPanel`

- `twGoMap()` 은 **하단 네비의 「유즈맵」 칸**이 부른다 — 마을과 무관하게 살아 있다.
- 나머지는 화면을 떠날 때의 정리 훅이다(`typeof` 가드가 붙어 있고, 다락도 로드되므로 어차피 안전하다).

### ⚠ 함께 사라진 것 — 「채팅/파티/친구」 탭 띠

`#msBottomTabs` 는 **도크에서 `display:none`** 이다(`#msSocialDock .msTabs2`). 탭 전환은 하단 네비가 맡는다.
마을이 없어지면서 이 띠는 **영영 안 보이는 요소**가 되어 같이 다락으로 갔다.
바로 전날 이 띠를 공용 컴포넌트로 통일했는데(DESIGN.md 2026-08-27), 그 작업은 결과적으로 헛일이었다.
**교훈: 통일하기 전에 「이게 화면에 뜨기는 하는가」부터 재라.**

---

## 3-D. ⚔🏘🏹 토벌 · 마을 팝업 · 옛 사냥터 팝업 — 2026-08-27

사용자가 화면을 하나씩 보고 판단했다. **셋 다 다락으로.** (코인 공학소 `#pointPanel` 은 살아 있어 남겼다.)

| 무엇 | 왜 |
|---|---|
| `dgScreen` · `dgSheet` · `dgHubScreen` (⚔ 토벌) | **전투 화면이 깨져 있었다** — 캐릭터·적이 좌상단에 겹쳐 쌓이고 아레나가 비었다. 들어갈 길도 없었다(더보기에서 뺌) |
| `townPanel` (🏘 마을 팝업 = 「내 캐릭터」) | 마을 화면이 §3-B 로 간 뒤 남은 조각 |
| `hbInfoModal` (스탯 출처) | 입구 `#hbHud` 가 캠프에서 **크기 0** 이라 도달 불가였다 |
| `hbGrowModal` (성장) | ⚠ 본문만 살렸다 — 아래 참고 |
| `hbMateModal` (동료) | `hbOpenMates` 를 부르는 곳이 **하나도 없었다**(동료 버튼은 2026-08-14 폐지) |
| `hbBunkerModal` (벙커) | 「벙커가 없습니다」 안내문뿐 |
| `hbRoundSheet` (던전·라운드) | 입구 `#hbMidTx` 가 옛 사냥터 줄에 있었다 |
| `hbMid` · `hbBar` · `hbHudTop` (옛 사냥터 화면 잔재) | 캠프에서 전부 **크기 0** |

### ⚠ 환생 본문(`#hbGrowBody`)은 **살렸다** — 소셜 때와 같은 함정

성장 팝업 안에 있었지만, **캐릭터 화면(`#upgScreen`)이 그 본문을 통째로 빌려 쓴다**(`CHR_SECS.reb`).
팝업째 지웠으면 캐릭터 화면의 환생 탭이 죽는다. 그래서 **집을 `#chrStash` 로 옮겼다** — 캐릭터 화면 안의
숨은 보관함이다. `renderGrowModal()` 은 그대로 같은 id 를 찾으므로 렌더러는 안 건드렸다.

> 🔁 **두 번째다.** 마을 채팅 시트에서도 `.msSocial` 이 같은 구조였다(§3-B).
> **팝업을 지우기 전에 「그 안의 본문을 남이 빌려 쓰지 않는가」를 반드시 확인할 것.**
> 찾는 법: 본문 id 를 `grep` 해서 **그 팝업 밖에서** 쓰는 곳이 있는지 본다.

### 스모크가 함께 고쳐진 것

마을·토벌에 기대던 스텝이 많았다. 특히 **장비창 검사 4개가 마을 팝업 경로**(`openTownPanel('gear')` · `#tpBody`)로
들어가고 있었다 — 살아 있는 정비 화면(`openGear()` · `#gearBody`)으로 옮겼다.
토벌 전용 스텝 4개는 걷어냈고, 스탯 출처·동료 본문 검사는 「다락에 있는가」 검사로 바꿨다.

⚠ **알려진 것 하나**: 정비 화면에서 아이템 상세(`.igInfo`)가 판(`.msPanel`) 밑변을 **5px 넘는다**.
마을 팝업 시절엔 카드 높이가 잠겨 있어 안 넘쳤다. 화면 밖으로 나가지는 않는다 — **아직 안 고쳤다.**

---

## 3-C. 마크업을 지우기 전에 통과해야 할 관문 셋

한 번 크게 틀렸다. 「스모크가 도는 동안 화면에 안 떴다」를 죽음으로 보고 세 덩어리
(`modeSheet` · `hbRoundSheet` · `hbBunkerModal`)를 잘라냈다가 **스모크가 바로 잡아냈다** —
`modeSheet` 는 유즈맵 카드를 누르면 뜨는 **모드 선택 팝업**이고, `hbRoundSheet` 는 `#hbMidTx` 가 직접 연다.
전부 되돌렸다. **「안 보였다」는 「없다」가 아니다.**

| 관문 | 확인 방법 |
|---|---|
| ① 여는 함수가 없거나 도달 불가 | `node scripts/attic-openers.mjs <id>` |
| ② 마크업 안 `onclick` 에 안 걸려 있다 | 위 표의 `html호출` 칸이 `-` |
| ③ `test/smoke.js` 가 그 id 도 그 함수 이름도 안 쓴다 | `node scripts/attic-audit.mjs <id...>` 가 `SMOKE-무관` |

마을은 이 셋을 **길이 닫혔다는 사실**로 대신했다 — 넷 다 들어가는 문이 `HB_MORE`/`NAV_TREE` 에서 빠져 있었다.
그리고 옮기기 전에 **런타임 계측**(`scratchpad/live.mjs`)으로 47개가 스모크 전 구역에서 한 번도 안 불리는 것을 확인했다.

---

---

## 4. ⚠ 이 목록을 어떻게 뽑았나 (다음 사람을 위해)

**세 번 틀리고 고쳤다.** 그냥 훑으면 반드시 산 코드를 죽었다고 한다.

| 실패 | 무엇을 놓쳤나 |
|---|---|
| ① 정적 훑기만 | `el.onclick = fn` 처럼 **JS 로 붙인 핸들러**를 못 잡는다 → 방금 만든 코드까지 「죽었다」로 나왔다 |
| ② 런타임 계측만 | 계측을 **로드가 끝난 뒤** 설치하므로, 파일 로드 시점에 실행되는 코드가 부르는 것을 통째로 놓친다(`buildGachaCurve`·`_gUnit` 이 그랬다 — 옮겼다가 페이지가 깨졌다) |
| ③ 호출 형태만 봄 | `addEventListener('x', fn)` · `.sort(fn)` · `setTimeout(fn, n)` 처럼 **값으로 넘기는** 것을 놓친다 |

지금 방식: **정적(로드 시점 코드 포함 · 이름이 언급되기만 해도 살아 있다고 봄) ∩ 런타임(스모크 전 그룹)**.
보수적이라 **놓치는 쪽**으로 기운다 — 죽은 걸 못 찾는 것은 안전하고, 산 걸 죽었다 하는 것은 위험하다.

**옮길 때의 안전장치 둘** (`scripts/attic-move.mjs`)
- 자를 때 문자열·주석·정규식을 **건너뛰며** 중괄호를 센다(그냥 세면 리터럴 `{` 에 걸려 다음 함수까지 잘라 간다)
- 잘라낸 덩어리에 **다른 최상위 선언**이 섞이면 되돌린다(들여쓰기 없는 `function`/`const`/`let`/`var`/`class`)

---

## 5. 관련 문서

- `GAME_DIRECTION.md` §5 — 유보 목록과 「지우지 말 것」 규칙
- `ARCHITECTURE.md` §1 — 파일 지도
- `CLAUDE.md` — 단일 소스 원칙(같은 UI 를 두 번 만들지 않는다)
