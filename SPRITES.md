# SPRITES — 유닛·건물 스프라이트 규격

**이 문서가 폴더 규칙·액션 목록의 단일 소스다.** 프레임을 뽑기 전에 여기부터 볼 것.
17,000장 규모라 폴더 이름을 나중에 바꾸는 것은 아주 비싸다.

**보는 법**

```
node scripts/sprite-manifest.mjs      색인 갱신 — 프레임을 넣거나 뺀 뒤 반드시
npx -y serve -l 4321                  서버 띄우기
→ http://localhost:4321/tools/sprites
```

`tools/sprites.html` 을 그냥 더블클릭해도 열리게 만들어 두었다(색인이 `fetch` 가 아니라
전역 대입이라 `file://` 에서도 통과한다). 다만 확인해 둔 것은 위 서버 경로다.

⚠ `package.json` 은 gitignore 되어 있다(`.gitignore` — Vercel 이 정적 사이트 대신 빌드를
시도하는 것을 막는다). 그래서 `npm run sprites` 같은 스크립트는 **내 컴퓨터에만 있고
클론한 곳에는 없다**. 문서에는 위처럼 명령을 직접 적는다.

---

## 1. 폴더 규칙

```
assets/sprites/<종족>/<id>/<액션>[_<방향>]/f00.webp
```

| 자리 | 값 |
|---|---|
| 종족 | `union` · `swarm` · `aetherial` · `feral` · `colossus` |
| id | 유닛·건물의 내부 id (`wolfrunner` · `union_barracks` …) |
| 액션 | 아래 §2 |
| 방향 | `0`~`7` — **방향이 있는 액션만** 붙인다 |
| 파일 | `f00` 부터 0채움 2자리 · 확장자 `.webp` |

⚠ 종족 키는 관리자·샌드박스 표기(`union`…)로 통일한다.
`STK_RACES` 는 기존 3종족에 옛 별칭(`terran`/`zerg`/`protoss`)을 아직 쓰고 있어
[scripts/sprite-manifest.mjs](scripts/sprite-manifest.mjs) 가 매핑해 준다.

예:
```
assets/sprites/feral/wolfrunner/move_2/f03.webp     유닛 · 오른쪽 이동 4번째 장
assets/sprites/union/union_barracks/produce/f00.webp 건물 · 생산 중(방향 없음)
assets/sprites/union/turret/attack_5/f01.webp        방어 건물 · 좌상 공격
```

## 2. 액션

### 유닛 — 넷 다 8방향

| 액션 | 뜻 | 프레임 | 비고 |
|---|---|---|---|
| `move` | 이동 | 8 | 루프 · 위상 정렬 필요(§4) |
| `attack` | 공격 | 6 안팎 | **루프 아님** — 첫·끝이 `idle` 과 이어져야 한다 |
| `cast` | 스킬 시전 | 6 안팎 | 시전 유닛만 · 없는 유닛이 많다 |
| `idle` | 대기 | 4 안팎 | 없으면 멈춘 유닛이 죽어 보인다 |

### 건물 — 방향 없음

| 액션 | 뜻 |
|---|---|
| `idle` | 평상시 |
| `build` | 건설 중 |
| `produce` | 유닛 생산 중 (빛·불이 들어온다) |
| `upgrade` | 업그레이드 작동 중 |
| `wreck` | 파괴 진행 중 |

### 방어 건물 — 위 다섯 + `attack` 만 8방향

어떤 건물이 방어 건물인지는 **[js/16-build.js](js/16-build.js) `TECH_DEF_BLDG`** 가 단일 소스다.
여기에 목록을 다시 적지 않는다.

```js
TECH_DEF_BLDG = { union:['bunker','turret'], swarm:['sunken','spore'],
                  aetherial:['cannon'], feral:['thornburrow'], colossus:['bastion'] }
```

## 3. 방향 번호 = 엔진 `face`

```
face = 번호 × 45°       face 0 = 카메라 쪽
```

엔진은 `face = Math.atan2(dx, dy)` 를 쓰고 `face 0` 이 카메라 쪽이다
([js/10-engine.js:1157](js/10-engine.js#L1157)). 화면상 배치는 이렇다:

```
  5 좌상    4 멀어짐   3 우상
  6 왼쪽      ·       2 오른쪽
  7 좌하    0 카메라쪽  1 우하
```

⚠ 엔진은 지금 방위를 **22.5° 단위(16방향)** 로 끊는다. 8방향 스프라이트를 쓰려면
`js/10-engine.js` 의 `Math.PI/8` 을 `Math.PI/4` 로 바꿔야 한다 — 안 바꾸면 유닛이
한 칸 어긋난 방향을 보며 움직인다.

## 4. 만드는 순서

```
① 정지 이미지 5장 (#0 #1 #2 #3 #4)   ART.md §9 · 나머지 셋은 반전으로 얻는다
② 방향별 걷기 영상 5편               카메라 고정 · 제자리 걷기 · 한 방향 고정
③ node scripts/unit-sweep.mjs        후보 구간 4개를 뽑아 한 장으로 비교 → 눈으로 고른다
④ node scripts/unit-frames.mjs       고른 구간을 --from/--to 로 추출 → 배경 제거
⑤ node scripts/unit-align.mjs        위상 정렬 + 좌우 반전으로 8방향 완성
⑥ 256px WebP 로 위 폴더에 배치
⑦ node scripts/sprite-manifest.mjs   색인 갱신
```


**⚠ 구간을 자동으로 못 고른다 — 눈으로 골라야 한다.** 지표를 두 번 만들어 봤지만 둘 다
실패했다. `--best`(다리/몸통 움직임 비율)는 점수 1.51 로 가장 높은 구간을 골랐는데 두
프레임만 화질이 튀어 걸음이 끊겨 보였고, 크기·밝기 흔들림 필터를 덧붙여도 그 구간이
문턱(크기 3.5% · 밝기 0.9) 안에 들어와 걸러지지 않았다. **「걸음이 이어지는가」는 픽셀
통계로 안 잡힌다.** `unit-sweep.mjs` 로 후보를 깔아 놓고 사람이 고른다.

**⚠ 위상 정렬을 건너뛰지 말 것.** 방향마다 영상을 따로 만들면 걸음의 시작 지점이
달라서, 유닛이 방향을 틀 때 다리가 순간이동한 것처럼 튄다. 보폭·속도 차이보다 이게
훨씬 눈에 띈다.

**좌우 반전으로 얻는 방향** — 만드는 것은 다섯뿐이다.

| 만든다 | 반전해서 얻는다 |
|---|---|
| `_1` 우하 | `_7` 좌하 |
| `_2` 오른쪽 | `_6` 왼쪽 |
| `_3` 우상 | `_5` 좌상 |
| `_0` `_4` | (자기 대칭) |

반전 쌍은 같은 프레임이라 **위상이 정의상 일치**한다.
⚠ 한 손에만 든 물건은 반대 손으로 간다(가시 사수의 창·대공 투석수의 투석끈).

## 5. 형식

| | 값 | 이유 |
|---|---|---|
| 크기 | **256×256** | 인게임 표시 크기의 여유분. 512는 17,000장이면 저장소가 감당 못 한다 |
| 형식 | **WebP** (알파) | 같은 장이 PNG 173KB → WebP 12KB |
| 배경 | 완전 투명 | 발밑 그림자도 지운다 — 엔진이 직접 그린다([js/90-m3d.module.js:196](js/90-m3d.module.js#L196)) |
| 여백 | **프레임마다 자르지 말 것** | 잘라내면 프레임마다 위치가 흔들린다. 캔버스 크기를 고정한다 |

## 6. 실패 기록

- **전체 구간 균등 샘플링** — AI 영상은 10초에 스트라이드가 6회씩 들어간다. 균등하게
  뽑으면 서로 다른 사이클이 섞여 다리가 안 이어진다. 한 사이클 구간만 잘라야 한다.
- **각도 지시를 늘리는 것** — 생성기는 긴 프롬프트를 요약하며 기하 지시를 먼저 버린다.
  줄일수록 잘 맞는다. 자세한 것은 [ART.md](ART.md) §9.
- **짙은 마젠타 배경** — 스틸 키잉에는 최적이지만 영상 압축(4:2:0)에서 초록 프린징을
  만든다. 그 초록은 장비의 옥빛과 채도·명도가 같아 자동 제거가 안 된다.
  영상 단계 배경은 중간 명도(예: 보라 120,60,160)가 낫다.
- **발 벌어진 폭으로 위상 재기** — 정면·후면 뷰에서 다리가 화면 안쪽으로 흔들려
  진폭비가 1.08(노이즈)이 된다. **바닥 띠 화소 수**를 쓴다(1.4~2.4배).
