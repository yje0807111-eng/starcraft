# HUNT2 — 사냥터 2차 설계 (SC2식 기지 운영으로 교체)

> ⚠️ **먼저 `GAME_DIRECTION.md` 를 읽을 것.** 이 문서는 그보다 **먼저** 쓰였고,
> 일부 결정이 **뒤집혔다** — 특히 「HB_UPG 35종 재편」·「장비를 사령관 장비로 재해석」·「펫 보너스 추가」는
> **무효**다(전투 성장은 `TECH_TREE` 연구로 전면 대체). 무효 목록은 `GAME_DIRECTION.md` §6 에 표로 있다.
> 여기서 여전히 유효한 것: 신규 시스템 N1~N7 · 캐릭터 해체(`S.char`→`S.army[]`) · 「그림은 병목이 아니다」.

> 이 문서는 **설계 계획서**다. 확정된 구현 사양이 아니라, 무엇을 죽이고 무엇을 살리고
> 무엇을 새로 만드는지에 대한 합의 문서다. 확정된 값은 구현하면서
> `ARCHITECTURE.md`(구조) · `BALANCE.md`(수치) · `RACES.md`(종족) 로 옮긴다.
> 작성: 2026-08-23 · 상태: **방향 확정** (§7 네 갈림길 결정 완료 — 아래 §0-1)

---

## 0. 무엇을 바꾸는가

**지금**: 캐릭터 1기가 자동 전투 → 웨이브 3개를 막으면 라운드 정산 → 미네랄로 업그레이드 31종.
**바꾼 뒤**: 종족을 고르고 **본부와 미네랄만** 있는 상태에서 시작 →
미네랄을 **눌러서** 채집 → **터치당 수급량** 업그레이드 → **일꾼**을 뽑아 초당 수급 →
**병영** 건설 → **전투 유닛** 생산 → 다음 던전으로 내려가면 **미네랄이 더 많지만 적군이 더 강하게 몰려온다**.

**웨이브는 폐지한다.** 「3파 막고 정산」이 아니라 **연속 압력**이다 — 적은 끊임없이 오고,
던전이 깊을수록 빈도·강도가 올라간다.

이 교체의 성격: 사냥터는 *방치형 강화 화면*에서 ***압축 RTS 운영 화면***이 된다.
플레이어가 하는 일이 「업그레이드 카드 누르기」에서 「자원 → 생산 → 방어의 순환을 돌리기」로 바뀐다.

---

## 0-1. 확정된 방향 (2026-08-23)

| 갈림길 | 결정 | 파급 |
|---|---|---|
| 캐릭터 | ❌ **없앤다 — 순수 기지 운영** | 가장 크다 → **§2-5 전용 절** |
| 던전 이동 | 🔘 **수동** — 「내려간다」를 내가 고른다 | 자동 승급 폐지 · 후퇴는 자동(본부 파괴) |
| 가스 | ✅ **남긴다** — 고급 유닛·건물 전용 | 유즈맵과 자원 구조가 같아진다 · `gk` 유지 |
| 첫 종족(B단계) | 🔵 **유니온** | 페럴 제외 지시 · 셋 중 준비도가 압도적으로 높다(아래) |

**첫 종족을 유니온으로 정한 근거** — 유니온/에테리얼/스웜 중 유니온만 4요소가 전부 갖춰져 있다:
본부 `union_command_center` · 일꾼 `worker_human` · 병영 `union_barracks` · 유닛 16기,
게다가 `CST_BLDG_CFG` 에 크기·정면각까지, `TECH_BLDG_UNIT.union` 에 배출원 11개가 이미 적혀 있다.
8방향 스프라이트도 `marine` 1기가 이미 있다(다른 둘은 0기).

---

## 1. 새 루프

```
종족 선택(5종 중 1)
   ↓
본부 + 미네랄 필드만 배치된 화면
   ↓
① 미네랄을 탭한다 ────────────→ 탭당 수급량 업그레이드
   ↓                                (손을 쓰는 축)
② 일꾼 생산 ──────────────────→ 초당 자동 수급
   ↓                                (방치 축)
③ 병영 건설
   ↓
④ 전투 유닛 생산 ─────────────→ 방어선
   ↓
⑤ 적군 압력을 버틴다 (연속 · 웨이브 아님)
   ↓
⑥ 던전 진입 = 미네랄 ↑↑ · 적군 ↑↑
   ↓
   (환생 = 던전 1-1 부터 다시, 깬 구간은 열려 있음 — 그대로 유지)
```

**손 축(탭)과 방치 축(일꾼)을 둘 다 두는 것이 이 설계의 핵심**이다.
탭은 초반 부팅과 「지금 보고 있을 때의 할 일」을, 일꾼은 「안 보고 있을 때의 성장」을 맡는다.
지금 사냥터에는 손 축이 아예 없다(전부 자동) — 그래서 화면을 열어 둘 이유가 약했다.

---

## 2. 살아남는 것 / 죽는 것 / 바뀌는 것

### 🟢 그대로 산다 (건드리지 않는다)

| 자산 | 위치 | 왜 사는가 |
|---|---|---|
| **난이도 곡선** `hbCurve` · `hbRoundHp` · `hbDgK` · `hbRoundS` | `js/08-hunt.js` | 던전별 기울기 + S자 리듬. **실측으로 맞춘 자산**(BALANCE.md §3). 웨이브와 무관하다 |
| **환생 전체** `profRebGainAt` · `profRebGrantAt` · `profRebirth` | `js/04-profile.js` | 축 역할 분리(배수=선형 / RP=복리)까지 끝난 부분. 던전 1-1 되감기 + `hunt.best` 유지도 그대로 |
| **성장 축 4개** 미네랄업글`log` / LP선형 / RP복리 / 환생배수선형 | BALANCE.md §0 | 폭주하지 않는 조합을 실측으로 찾아낸 것. 새 시스템도 이 위에 얹는다 |
| **던전 표** `HB_DUNGEONS`(10) · `HB_DG_UNLOCK` · 타일·틴트 | `js/08-hunt.js` | 던전이라는 그릇 자체는 그대로 |
| **큰 수 표기** `fmtCur` | | |
| **3D 렌더 경로** `M3D.unitSprite` · `M3D.syncBuild` · `hb3dReady` | `js/08-hunt.js` + `js/90-m3d.module.js` | ⭐ 아래 §4 참조 — **그림이 병목이 아닌 이유** |
| **타일 배경·카메라·줌** | | |

### 🔴 죽는다 (삭제)

| 자산 | 왜 죽는가 |
|---|---|
| `HB_WAVES=3` · `HB_WAVE_S=50` · `HB_GAP_S=3` · `HB_WAVE_MUL=[.45,.75,1.0]` · `HB_BOSS_EXTRA_S` | 웨이브 폐지 |
| `hbSpawnWave()` · `hbWavePlan()` · `hbWaveTime()` · `hbWaveReward()` · `hbWaveFail()` | 위와 같음 — **연속 압력 스포너로 교체** |
| `HB_UPG.mw`(미네랄/웨이브) · `HB_UPG.gw`(가스/웨이브) | 웨이브 정산이 없어지면 지급 시점이 사라진다 → 탭·일꾼 축으로 옮긴다 |
| `_hb.wave` · `_hb.phase='fight'/'gap'` 상태기계 | 국면이 없어진다(항상 전투) |

### 🟡 바뀐다 (재배치 — 여기가 실제 작업량)

| 자산 | 지금 | 바뀐 뒤 |
|---|---|---|
| **라운드** | 웨이브 3개를 막으면 +1 | **던전 안의 압력 단계** — 시간·처치로 자동 상승. `hbCurve(dg, round)` 입력은 그대로라 곡선·환생·실측이 전부 살아남는다 ⭐ |
| **미네랄 수급** | 킬(`mk`) + 웨이브(`mw`) | **탭 + 일꾼 + 킬** 3원. 던전이 깊을수록 미네랄 노드 수확량 ↑ · 가스는 킬(`gk`) + 가스 채취 건물 |
| `HB_UPG` 31종 | 캐릭터 23 / 동료 2 / 건물 4 / 펫 2 | 구역이 하나 는다 → **경제**(탭당 수급·일꾼 효율·일꾼 상한·노드 회복) |
| `HB_STRUCT` 벽·터렛·벙커 | 미네랄로 사서 배치 | **본부·병영이 합류.** 벙커는 그대로(유닛 주둔 = 새 시스템과 잘 맞는다) |
| **캐릭터** `hbCharStats` · `hbCharShot` | 유일한 아군 | ❌ 주인공은 사라진다. 다만 **함수는 산다** — 「한 아군의 사격/피격 경로」로 재해석해 유닛 여러 기가 함께 쓴다 → **§2-5** |
| `HB_FOE_KIND` · `HB_SPAWN` · `HB_RACE_FACE` | 웨이브 편성표가 소비 | 연속 스포너가 소비 (표 자체는 그대로 쓸 수 있다) |
| **동료** `HB_MATES` 12기 | 캐릭터 옆에서 함께 싸움 | 병영 유닛과 역할이 겹친다 → **영웅 유닛으로 격상**(수량 아닌 개성) |

---

## 2-5. 캐릭터 해체 — 가장 큰 파급 ⚠

**결정: 캐릭터를 없앤다.** 다만 *삭제*가 아니라 **주체 교체**다.

조사한 결합도: `hbChar*` 호출 23곳 · `S.char` 참조 31곳 ·
**6개 파일**(`04-profile` `05-home` `07-home-upgrade` `08-hunt` `09-dungeon` `test/smoke`).
지워 없애려 들면 여섯 파일이 동시에 무너진다.

### 원칙: 함수는 살리고 주어만 바꾼다
`hbCharStats`/`hbCharShot`/`hbCharHit`/`hbCharTake` 는 사실 **「한 아군의 사격·피격 경로」**다
(이미 벙커 유닛·동료·터렛이 이 경로에 얹혀 있다). 캐릭터 1기 전용이 아니라
**아군 유닛 N기가 공유하는 경로**로 일반화하면, 크리티컬·흡혈·넉백·멀티샷·바운스가 통째로 살아남는다.

- `S.char` (단수) → `S.army[]` (복수). 유닛마다 `{uid, kind, hp, shd, cd, x, y, face}`
- `hbCharStats()` → `hbUnitStats(kind)` — 유닛 종류별 기본값 × 업그레이드 배수
- 캐릭터 사망 = 패배 → **본부 파괴 = 패배** 하나로 통일(N6)

### HB_UPG 재편 — 31종 → 5구역
「캐릭터」 구역 23종은 **버리지 않고** 아군 유닛/경제로 옮긴다.

| 새 구역 | 들어가는 것 | 출처 |
|---|---|---|
| **army** 아군 유닛 | atk · aspd · crit · critm · lifest · knock · rng · multic · multin · bncc · bncn · scritc · scritm · hp · regen · shd · shdreg · mspd | 옛 char 18종 **그대로** (의미가 「내 캐릭터」→「아군 전체」로만 바뀜) |
| **econ** 경제 ⭐신규 | **탭당 수급** · **일꾼 효율** · **일꾼 상한** · **노드 회복** · **생산 속도** · mk · gk | 신규 5 + 옛 char 2(킬 수급) |
| **bld** 건물 | tuatk · turng · bnhp · bkatk + **본부 체력** · **본부 등급** | 옛 4 + 신규 2 |
| **hero** 영웅(동료) | alatk · alspd | 옛 ally 2 (이름만) |
| **pet** 펫 | peatk · pespd | 그대로 |
| 🔴 삭제 | mw · gw(웨이브 지급) · rrng(재생 범위 — 캐릭터 전용 개념) | 3종 |

→ 31종 − 3 + 7 = **35종**. 카드 UI·비용 곡선·`hbUpgNum` 은 손대지 않는다(표만 바뀐다).

### 장비 12슬롯 → 「사령관 장비」
`PROF_GEAR` 의 `vit/pow/agi/foc` 4스탯을 **캐릭터가 아니라 기지에** 먹인다:

| 스탯 | 지금 | 바뀐 뒤 |
|---|---|---|
| `pow` | 캐릭터 공격력 | **아군 유닛 공격력 배수** |
| `vit` | 캐릭터 체력 | **아군 유닛 + 본부 체력 배수** |
| `agi` | 캐릭터 공속·이속 | **생산 속도 + 유닛 이속** |
| `foc` | 치명·집중 | **경제 배수**(탭·일꾼 수급) |

12슬롯·페이퍼돌 아트·뽑기·강화·`profStat`/`csAxis` 계산식은 **전부 그대로 재사용**한다.
바뀌는 것은 「이 숫자가 무엇에 곱해지는가」 한 지점뿐이다.

### 레벨·경험치는 남는다
캐릭터가 없어져도 **레벨은 사령관 레벨로 남는다** — 환생 조건(Lv100)·LP·RP가 전부 거기 매여 있다.
경험치는 지금처럼 킬에서 나온다. **이름만 바뀌고 수식은 그대로다.**

---

## 3. 새로 만들 시스템 7개

| # | 시스템 | 명세 초안 | 난이도 |
|---|---|---|---|
| N1 | **종족 선택** | 최초 1회 + 환생 때 변경 가능. `STK_RACE_ORDER` 5종 재사용. 저장 = `hunt.race`. 종족별로 본부/일꾼/병영/유닛 키만 다르고 **수치는 동일**(1차) — 상성은 유즈맵의 몫 | 낮음 |
| N2 | **미네랄 노드 + 탭 수급** | 화면에 노드 N개. 탭 1회 = `hbTapYield()` 미네랄 + 노드 잔량 감소, 잔량은 시간으로 회복. 연타 상한(초당 탭 캡)으로 매크로 방지 | 중간 |
| N3 | **일꾼** | 생산 비용 = 기하 증가. 일꾼 1기 = 초당 `hbWorkerYield()`. 상한 = 본부 등급. 왕복 애니메이션은 3D 유닛 경로 재사용 | 중간 |
| N4 | **건설 → 생산 사슬** | 본부(있음) → 병영(건설) → 유닛(대기열). 대기열 UI는 **`renderCmdGrid` + `.cgBar`/`.cgQBar` 를 그대로 빌린다**(단일 소스 원칙) | 중간 |
| N5 | **연속 압력 스포너** | `hbPressure(dg, round)` = 초당 적 전력 예산. 예산을 `HB_SPAWN` 편성표로 환전해 계속 흘려보낸다. **정산 시점이 없으므로 보상은 킬 즉시 지급** | **높음** |
| N6 | **패배·후퇴 조건** | 웨이브 실패가 없어지니 새 조건이 필요: **본부 파괴 = 라운드 하락**(`hbRetreatDungeon` 재사용). 캐릭터가 없으므로 **본부가 유일한 패배 조건**이다 | 중간 |
| N7 | **수동 던전 이동** ⭐ | 라운드 99 자동 승급을 폐지하고 「**다음 던전으로 내려간다**」 버튼으로 바꾼다. 미리 내려가는 것도 허용 — 감당 못 하면 N6이 알아서 올려 보낸다. `hbAdvanceDungeon` 은 버튼 핸들러로 재활용 | 낮음 |

**가장 위험한 것은 N5**다. 「연속 압력」은 난이도가 시간에 대해 적분되므로,
지금의 「웨이브 3개 = 고정 총량」과 체감이 완전히 다르다. **반드시 `hbStep` 실측으로 잡는다**(BALANCE.md §4).

---

## 4. 이미지 — 결론: **병목이 아니다**

조사 결과를 먼저 적는다. 사냥터는 스프라이트 시트가 아니라
**3D GLB 모델 → `M3D.unitSprite(mdl, dir)` 로 뽑은 캐시**로 그린다(`js/08-hunt.js:1914`).
따라서 새 시스템이 필요로 하는 그림은 **대부분 이미 있다**.

| 필요한 것 | 지금 있는가 | 비고 |
|---|---|---|
| 종족별 **본부** | ✅ `union_command_center` · `swarm_hatchery` … | `assets/icons/buildings/` 51개 + 3D 모델 |
| 종족별 **일꾼** | ✅ `worker_human` · `worker_light` · `worker_feral` … | `HB_RACE_FACE.grunt` 에 이미 매핑돼 있다 |
| 종족별 **병영** | ✅ `union_barracks` · `swarm_spawning_pool` … | `CST_BLDG_CFG` 에 크기·정면각까지 있다 |
| 종족별 **전투 유닛** | ✅ 74기 (매니페스트 기준 union 16 · swarm 14 · aetherial 14 · feral 16 · colossus 14) | |
| **미네랄 노드** | ⚠ 확인 필요 | 유즈맵 엔진에는 있다 — 사냥터로 가져오면 된다 |
| **UI 아이콘**(탭·일꾼·경제) | ⚠ 일부 | `assets/icons/` 에서 먼저 찾고 없을 때만 신규 |

### 별개 트랙: 8방향 스프라이트 (`assets/sprites/`)
품질 고급화 라인이고 **이 설계의 선행 조건이 아니다**. 현황 **4/296 액션**
(페럴 4기만: 채집수·돌진수·가시사수·wolfrunner). 건물은 아직 매니페스트에 없다(`kind` 전부 `unit`).
→ **권고: 이 트랙은 지금 확장하지 않는다.** 시스템이 확정된 뒤,
   실제로 화면에 자주 나오는 소수(본부·일꾼·병영·유닛 2~3기)만 종족별로 채운다.
   지금 채우면 설계가 바뀔 때마다 다시 뽑게 된다.

---

## 5. 후속 설계 대상 (장비 · 펫 · 동료 · 던전)

### 5-A. 장비 (`PROF_GEAR` 12슬롯) — **「사령관 장비」로 확정**
매핑표는 **§2-5** 에 있다. 요약: 12슬롯·아트·뽑기·강화·계산식을 전부 그대로 두고,
`pow/vit/agi/foc` 가 **무엇에 곱해지는지**만 바꾼다. 아트 신규 제작 없음.

### 5-B. 펫 (`PROF_PETS` 9종)
보너스가 `{type:'atk'|'vit'|'coin', val}` 3종뿐이라 **어느 쪽이든 그대로 산다.**
새 시스템에 맞춰 **보너스 종류만 추가**하면 된다:
`tap`(탭당 수급 +%) · `worker`(일꾼 효율 +%) · `prod`(생산 속도 +%) · `pop`(인구 상한 +) · `gas`(가스 채취 +%).
→ 표 한 줄씩 추가. 작업량 작음. **새 시스템을 가장 싸게 홍보하는 자리**다.

### 5-C. 동료 (`HB_MATES` 12기) — **영웅 유닛으로 격상**
캐릭터가 사라지면서 **동료가 「개성 있는 아군」 자리를 물려받는다.** 병영 유닛이 *수량*이라면 동료는 *개성*이다.
→ 제안: **영웅 유닛으로 격상**. 병영 유닛은 *수량*, 동료는 *개성*(1기씩 · 부활 · 고유 효과).
   `dps/rng/spd` 3필드는 그대로 두고 **고유 효과 필드 하나만 추가**한다.
   뽑기 30단계(`HB_MATE_GACHA_MAX`)·티어·티켓 경제는 손대지 않는다.

### 5-D. 던전 (`HB_DUNGEONS` 10)
표는 그대로. **던전마다 바뀌는 값이 늘어난다**:

| 던전축 | 지금 | 추가 |
|---|---|---|
| 적 체력·공격 | `hbRoundHp(dg)` | 그대로 |
| 보상 | `hbKillReward` | **미네랄 노드 수확량**(요구: "더 많은 미네랄") |
| — | — | **압력 강도** `hbPressure(dg)` (요구: "더 강한 적군") |
| — | — | **적 종족** (표에 `race` 이미 있음 → 편성에 반영) |
| — | — | **지형/노드 배치** (던전마다 다른 맵 = 반복감 완화) |

### 5-E. 밸런스 재측정 (`BALANCE.md`)
새 축이 둘 늘어나므로(탭·일꾼) **§3 실측 기준선 전체가 무효가 된다.**
재측정 항목:
1. 탭 1회 가치 vs 일꾼 1기 초당 가치 (손 축이 방치 축을 압도하면 안 된다 — 반대도 마찬가지)
2. 일꾼 손익분기(몇 초 만에 회수되나) → 「일꾼만 뽑는」 지배 전략 방지
3. 병영·유닛 투자 vs 업그레이드 투자의 교환비
4. 던전 1~4 도달 레벨 목표 재확인 (250~300 / 500~600 / 1300 / 2000)
5. **N5 압력의 시간 적분** — 「버틸 수 있다/없다」의 경계가 어디인지

---

## 6. 접근안 — A+B 채택

### A. 골격 교체 ⭐ 채택
전투 경로·곡선·환생·업그레이드 **계산식을 살린 채**, 웨이브를 연속 압력으로 바꾸고
캐릭터를 아군 유닛 N기로 일반화하고, 미네랄 탭·일꾼·병영·유닛을 얹는다.
- 👍 BALANCE.md 곡선 자산과 장비·펫 아트가 전부 살아남는다 · 되돌리기 쉽다
- 👎 캐릭터 해체가 6개 파일을 건드린다(§2-5) — A의 실제 무게는 여기에 있다
- 규모: `js/08-hunt.js` 큰 수술 + 신규 500~800줄

### B. 유니온 수직 완성 ⭐ 채택 (A 다음)
**유니온 한 종족만** 본부·일꾼·병영·유닛 3기까지 끝까지 만들어 새 루프를 완제품으로 검증하고,
나머지 4종족은 그 틀을 복제한다. (첫 종족 근거는 §0-1)
- 👍 「진짜 재미있나」를 가장 먼저 알 수 있다 · 유니온은 4요소가 이미 다 있다
- 👎 그동안 다른 4종족은 못 쓴다 · 종족 선택 화면(N1)을 나중에 붙여야 한다
- 규모: A와 비슷하되 폭이 좁고 깊다

### C. 유즈맵 엔진 위에 재건축
사냥터를 버리고 `js/10-engine.js`(유즈맵 RTS 엔진) 위에 **진짜 소형 RTS**로 다시 세운다.
- 👍 건설·생산·전투·경로가 이미 다 있다 · 「SC2식」이 가장 정직하게 나온다
- 👎 **환생·업그레이드·장비·펫·동료·던전 곡선을 전부 새 엔진에 다시 배선**해야 한다 ·
     BALANCE.md 실측이 통째로 무효 · 방침("기존 사냥터 구역은 최대한 냅두고")과 충돌
- 규모: 가장 큼. 권하지 않는다

**확정: A 로 시작해서, N4(생산 사슬)까지 돌아가면 B(유니온) 로 깊게 판다.**
A와 B는 배타적이지 않다 — A가 뼈대, B가 살이다. C는 A가 실패했을 때의 대안으로 남긴다.

---

## 7. 착수 순서 (A단계)

갈림길 넷은 §0-1 에서 결정됐다. 남은 것은 순서다.
**⚠ 각 단계마다 `npm test` 를 통과시키고 넘어간다** — 캐릭터 해체는 6개 파일을 건드리므로
한 번에 몰아 고치면 어디서 깨졌는지 못 찾는다.

| 순 | 할 일 | 왜 이 순서인가 | 검증 |
|---|---|---|---|
| 1 | **`S.char` → `S.army[]` 일반화** (§2-5) | 다른 모든 것이 여기 매여 있다. 아직 **유닛 1기만** 넣어 동작을 똑같이 유지 | `npm test` 전부 통과 = 무손실 리팩터 증명 |
| 2 | **HB_UPG 5구역 재편** | 1이 끝나야 「아군 전체 배수」가 말이 된다 | 카드 표시 = 전투값 스모크(이미 있음) |
| 3 | **웨이브 → 연속 압력** (N5) | 가장 위험 · 1·2가 안정된 뒤에 | `hbStep` 실측 (BALANCE §4) |
| 4 | **미네랄 노드 + 탭** (N2) | 압력이 있어야 「수급이 부족하다」가 성립 | 탭 1회 가치 실측 |
| 5 | **일꾼** (N3) | 탭이 있어야 비교 대상이 생긴다 | 손익분기 실측 |
| 6 | **병영 → 유닛 생산** (N4) | 자원이 돌아야 살 것이 생긴다 | 생산 대기열 = `renderCmdGrid` 재사용 확인 |
| 7 | **본부 파괴 = 후퇴** (N6) + **수동 던전 이동** (N7) | 루프를 닫는다 | 던전 이동 스모크 |
| 8 | **장비·펫 재배선** (§5-A/5-B) | 전투가 확정된 뒤 곱해야 한다 | 스탯 반영 스모크 |
| 9 | **종족 선택** (N1) → **B단계 유니온 수직** | 마지막. 그 전엔 유니온 고정 | |

### 착수 전 확인이 남은 것
- **미네랄 노드 에셋** — 유즈맵 엔진에 있는지 확인해 가져온다(§4 표의 유일한 ⚠)
- **가스 채취 건물** — 유니온 `union_refinery` 를 `HB_STRUCT` 에 추가
- **`HB_DG_ALL_OPEN`** — 지금 `true`(개발용). 수동 이동이 들어가면 `false` 로 되돌린다 (BALANCE §5 C1)

---

## 8. 참고

- 구조: `ARCHITECTURE.md` §1 파일 지도 · 사냥터는 `js/08-hunt.js`(3,325줄, 최대 파일)
- 수치: `BALANCE.md` §4 「엔진 자동 플레이로 재는 법」 — ⚠ 해석적 추정 금지
- 종족: `RACES.md` — 5종족 유닛·건물·상성
- 그림: `ART.md` §9 유닛 참고 아트 · `assets/sprites/manifest.js`(자동 생성)
- ⛔ 새 UI를 만들기 전에 `CLAUDE.md` 공식 컴포넌트 레지스트리를 먼저 볼 것
