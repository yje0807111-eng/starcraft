# Claude Code Project Guidelines (Superpowers Style)

## 🗣 말하는 방식 (MANDATORY)
사용자에게 답할 때는 **쉽고 간단하게** 말한다. 여러 번 요청받은 사항이다.

- **결론부터 한두 줄.** 그 다음에 필요한 만큼만 덧붙인다.
- **표와 수식은 꼭 필요할 때만.** 숫자를 나열하기 전에 "그래서 무슨 뜻인지"를 한 문장으로 먼저 말한다.
- **과정 중계 금지.** 무엇을 어떻게 뒤졌는지가 아니라 **무엇을 알아냈는지**를 말한다.
- 전문 용어를 쓸 거면 **그 자리에서 한 줄로 풀어 준다.**
- ⚠ 다만 **나쁜 소식·불확실한 것은 줄이지 말고 그대로** 말한다. 짧게 말하라는 것이지
  숨기라는 것이 아니다. "안 쟀다 / 틀렸다 / 못 고쳤다"는 분명하게.
- 문서·주석은 이 규칙의 대상이 아니다(거기는 지금처럼 촘촘하게 쓴다).

## Project Context (this repo)
- Deliverable: a static file set — `sc-ums-web.html` (markup only, ~900 lines) + **`css/` 5 files** + **`js/` 19 files** — a mobile StarCraft-style usemap (vanilla JS, Three.js 3D, Supabase realtime). No build step and **no test framework**.
  - **Find the right file first: `ARCHITECTURE.md` §1 파일 지도.** Don't grep the HTML for logic — it holds only markup now.
  - `js/*.js` are **classic scripts** sharing one global scope, executed in tag order. ⛔ Never reorder the `<script>` tags, never convert them to `type="module"`, and remember declarations hoist **within a file only**.
- 🔀 **캠프 ↔ 오토 배틀이 헷갈리는 진짜 이유 — 파일이 아니라 `campWithStk` 다**(2026-09-10 정리).
  캠프가 **전역 `STK` 를 제 것(CAMPB)으로 바꿔치기**하고 오토 배틀 함수를 부른다(빌려 쓰는 20종).
  그래서 빌려 간 함수 안에서는 **자기가 누구를 위해 도는지 알 수가 없다** — ⛔ 함수를 나눠서는 못 가른다.
  ⭐ **새 코드는 `battleCtx()`(js/10-engine.js) 하나만 묻는다** — `'camp'|'autobattle'|'sandbox'|'usemap'|'none'`.
  가장 정확한 신호는 `STK === CAMPB`(지금 빌려 쓰는 중)이라 화면·맵과 무관하다.
  ⚠ 옛 판별기 다섯(`campIsOn`·`techWallet`·`G.strike`·`G.sandbox`·`MAP.id` · 합 156곳)은 **그대로 둔다**
  — 서로 다른 질문이라 헷갈리던 것이고, 일괄 치환하지 않는다(touch-it-fix-it).
  ⚠ 빌려 쓰는 20종과 **밸런스에 직접 닿는 일곱**은 `ARCHITECTURE.md` §「🔀 캠프 ↔ 오토 배틀」 표에 있다 —
  그 함수를 고칠 때 **반드시 양쪽을 본다**. ⛔ 상태를 인자로 넘기는 근본 해결은 **개편 뒤에**(333곳).
- ⚔ **캠프 전투는 `js/21-camp-battle.js` 가 소유한다**(2026-08-31). `campStepUnits(dt)` 한 함수가 표적 선정·자리·이동·사격을 다 한다. ⛔ `js/18-strike.js`(유즈맵 오토배틀)를 고치지 말 것 — 거기서는 **부품만** 가져온다(`strikeHit`·`strikeMoveToward`·`strikeSeparate`·`strikeSkillTick` …). ⛔ 옛 이동 장치 넷(`campPostSnap`·`campPostStep`·`campEngageStep`·`campLeash`)은 `19-camp.js` 에 남아 있지만 **배선이 끊겼다** — 되살리면 미는 주체가 둘이 되어 유닛이 덜덜 떤다(실측 96회/유닛 → 6.5회). 구조·실측은 `ARCHITECTURE.md` §「⚔ 캠프 전투」.
  - 🎬 **전투 움직임을 만졌으면 `node scripts/camp-trace.mjs` 로 눈으로 볼 것** — 궤적 그림 + 떨림·사거리 수치. 이 프로젝트는 움직임을 숫자로만 좇다가 네 번 헛짚고 전부 되돌렸다. 그다음 `scripts/camp-bench.mjs` 로 밸런스를 다시 잰다(이동이 바뀌면 화력이 바뀐다).
- 🏕 **HOME 메인은 「캠프」다**(`js/19-camp.js` · 2026-08-23). 옛 **사냥터(웨이브 방어)를 대체했다** — 새로 만든 게 아니라 관리자 건설 시스템을 빌려 쓴다. 구조는 `ARCHITECTURE.md` §「🏕 캠프」.
  - 🗄 **옛 사냥터·마을은 다락으로 갔다**(2026-09-10). `js/08-hunt.js` 는 없어졌고, 남은 부품만 **`js/08-ui-parts.js`**(던전 이름표 `hbDun` · `edgePush` · 3D 예열 · 로그아웃 확인 · `resIco` · 캠프 상점 · `segNavHTML`)에 있다. ⛔ 사냥터·마을 화면을 되살리지 말 것 — `npm test` 의 다락 래칫이 잡는다.
  - ⚠ 문서·코드의 **「사냥터」는 대부분 옛 이름**이다(102곳). 다만 화면의 **「사냥터 업그레이드」(`.hmUpg`)와 `UM_*` 경제 상수는 살아 있는 이름**이라 일괄 치환 금지.
- 🚨 **2026-09-08 전면 개편이 확정됐다 — `GAME_DIRECTION.md` §0-A 를 먼저 읽을 것.** 게임의 뼈대가 바뀐다:
  **던전 10 → 3**(적 종족과 짝: 유니온 기지 → 스웜 기지 → 에테리얼 기지) · **종족 선택 없앰**(유니온 → 스웜 → 에테리얼 **순차** · 「종족 변이」로 넘어간다) ·
  📈 **레벨**(2026-09-11 · 옛 1차 환생을 대체 — 캠프에서 적을 잡으면 오르고, **레벨 포인트**로 성장 트리·룬 칸을 연다 · 환생하면 되감긴다) ·
  🔁 **환생은 하나뿐**(옛 2차 환생 · 2026-09-11 구현) — 관문 = **마지막 던전 클리어**(`campRebDepth`≥`campRebNeed`) ·
  포인트 = **도달 깊이**(통산 관문 수) · 배수 = **1 + 환생 횟수** · 되감김에 **레벨·성장 트리·깬 던전(`C.dgDone`)** 이 포함된다.
  ⛔ 「번 돈」(`campWealth`)을 관문·포인트의 자로 되돌리지 말 것 — 돈→포인트→배수→돈 되먹임이 그거였다.
  ⛔ 배수를 난이도 로그(`CAMP_REB_K`)로 되돌리지 말 것(없앴다).
  🔁 **환생 포인트로 사는 곳은 「환생 강화」**(2026-09-11 · `CAMP_REB_UPG` · 화면 `campRebEnter('boost')`) —
  갈래 셋(스킵·자동화·상한 해제) **일곱 줄**이고 **「세지는 것」은 안 판다**(그건 레벨·성장 트리의 몫). 🚫 **일부러 안 넣은 것 셋**: **보급소 상한**(엔진이 인구를 `TECH_SUP_MAX` 200 에서 자르므로 그 벽을 넘는 길은 트리의 「인구 상한」 하나뿐 — 줄을 세우면 같은 효과를 두 곳에서 파는 것이 된다) · **오프라인 수익률**(트리의 「방치 수급」이 그 배수다 — 대신 **상한** `awayCap` 을 판다) · **시작 자원·일꾼**(트리에 있다). ⏳ 남은 하나는 **시작 구성 바꾸기**인데 **닿는 데가 없다**(캠프 시작 건물을 정하는 자리가 `campWipeBoard` 한 줄이다) — ⛔ 그 줄을 만들기 전에 표에 올리지 말 것.
  ⛔ 성장 트리와 **같은 효과를 팔지 말 것**(인구 상한·시작 미네랄·시작 일꾼은 트리에 있다 · 스모크가 키 겹침을 잰다).
  🚨 **`dgStart.max` 는 `CAMP_DG_MAX − 1` 이 상한**이고 `campRunReset` 은 **`C.dgDone` 을 반드시 비운다** —
  둘 중 하나만 어겨도 환생 직후에 관문이 참이라 **누를 때마다 배수가 +1 씩 붙는 무한 환생**이 된다 ·
  ♾ **무한층**(2026-09-11 구현 · 단계 3) — 던전 3 을 깨면 그 위로 층이 **끝없이** 이어진다.
  ⭐ 표를 **빌려 쓴다**(`campInfDef` 가 `CAMP_DG` 를 종족 순환으로 · ⛔ 무한층 전용 건물 표를 만들지 말 것) ·
  난이도는 `campFoeDiff` 가 **사다리를 그대로 잇는다**(한 층 = 관문 여섯 ×7.17 · ⛔ `CAMP_INF_R` 류 두 번째 자를 두지 말 것) ·
  보상은 `campMineDef` 가 표 밖을 ×5.83 으로 외삽한다(**난이도보다 느린 것이 벽**이다 · ⛔ 같게 맞추지 말 것) ·
  🧗 한 층을 깨면 **곧바로 다음 층**(⛔ 여기서 `campFail()` 을 부르지 말 것 — 등반이 끊긴다 · 던전 1~3 의 「완주하면 캠프로」와 다른 규칙이다) ·
  🏆 기록 `C.infBest` 는 **안 되감긴다** · 🪙 보상은 **코인 하나**(`PLAYER_META.coins` = 유즈맵 강화 지갑 · ⛔ 새 재화 금지) ·
  🔓 문은 **마지막 던전 클리어**(환생 관문과 같은 자) · ⛔ `campDgN()` 에 위쪽 clamp 를 되돌리지 말 것(등반이 통째로 없는 일이 된다) ·
  ⚠ 값 둘은 **안 쟀다**(`CAMP_INF_COIN`=12 · 보상 외삽 비).
  환생 포인트·관문이 **「번 돈」이 아니라 「도달 깊이」** 기준.
  ⛔ **「1차 환생」·「2차 환생」이라는 말을 되살리지 말 것** — 층이 하나다. ⛔ 성장 트리(`C.rbTree`)를 환생 뒤에 남기지 말 것.
  ⛔ **레벨이 배수를 직접 주지 말 것** — 레벨이 주는 것은 포인트뿐이고 배수는 트리가 준다.
  📣 **레벨이 오르면 말해 준다**(2026-09-12 · `campLvUpSay` ← `campAddXp` 한 입구): 「Lv.N 달성 · 성장 포인트 +n · 💠 룬 칸 n개 열림」. ⛔ 킬 지점마다 붙이지 말 것(경험치 지급을 한 곳에 모은 것과 같은 이유) · ⚠ 한 번에 여러 레벨이 올라도 **한 줄만** 말한다 · 💠 룬 칸은 `campRuneSlotsAt` 로 **오르기 전/후를 견줘** 센다(화면을 열기 전에는 열린 줄 모른다).
  🔔 **캠프에도 알림이 보인다 — 2026-09-12 에 고쳤다.** `toast()` 는 `#chatLog` 에 글을 넣는데 캠프가 채팅바와 함께 그 줄까지 `display:none` 으로 숨기고 있었다(2026-08-25~) — **환생·던전 완주·자원 부족·오프라인 수급이 한 마디도 안 보였다.** ⛔ `#phone.campMode #chatLog{display:none}` 으로 되돌리지 말 것: 빼는 것은 채팅 **바**(입력칸)뿐이다 · 📐 캠프에서는 **위**에 뜬다(아래는 커맨드 시트가 늘 가린다 · z 61 = 화면 60 위 · 재화 바 62 아래 · 좌상단 던전 칩을 안 밟게 38px 내린다) · 스모크가 잰다.
  ✅ 레벨 값 셋(`CAMP_XP_KILL`·`CAMP_XP_A`·`CAMP_XP_R`)을 **쟀다**(2026-09-11 · BALANCE §5-12): 한 회차(D1→D3 **연속**) = **Lv.17 · 성장 포인트 16점**이고 성장 트리 전체가 **82점**이라 회차마다 그 **20%**를 산다 — 고칠 것이 없다. ⛔ 던전 하나씩 잰 레벨을 **더하지 말 것**(요구량이 등비다) — `node scripts/camp-clear.mjs all 20 "5,36,257"` 연속 모드로 잰다. 🔧 개발 스위치 **둘 다 껐다**(`CAMP_RT_PTS_FREE` · `CAMP_DEV_START_MIN` · 2026-09-11 사용자 확정) — ⛔ 다시 켜고 밸런스를 재지 말 것. 💰 빈손 출발도 **쟀다**(BALANCE §5-13 · `node scripts/camp-bench.mjs 45 1` 의 「🏁 첫 도달」): 병영 3.6분 · **병력 1기 7.0분** · 미네랄 10만 19.1분 · **병력 5기 27.4분** · 20기 37.5분. ⚠ **7~27분이 「병력이 안 느는 구간」**이다. ❌ **원인이 유닛 반복 구매 배수라고 봤는데 틀렸다**(BALANCE §5-14 · 2026-09-11 실측): 초반 유닛 값을 45% 깎는 「무릎」을 넣어 봤더니 **병력 5기 28.3 → 29.3분(그대로)** 인데 **끝 병력만 25 → 36기(+44%)** 로 불어서 되돌렸다. ⛔ **`CAMP_UNIT_R` 을 초반 때문에 만지지 말 것** · ⛔ **「무릎」을 다시 넣지 말 것**(`campUnitCost` 위에 같은 경고가 있다). ✅ **원인을 밝혔다 — 「인구」다**(BALANCE §5-15 · 인구 표를 직접 찍었다): 본부 인구 **10** 에 일꾼이 4~6기 들어앉으면 **병력이 4~6기에서 천장**이고, 그 문을 여는 것이 **보급소 3만**(`CAMP_SUPPLY0`) 하나다 — 그래서 판마다 **「보급소 시각 = 병력 5기 시각」**이 붙어 다닌다(28.2/28.3 · 33.0/33.1). 즉 「빈 20분」은 **보급소를 언제 짓느냐**다. ⚠ **§5-13 의 시각은 한 판으로 읽지 말 것 — 판마다 ±3분 흔들린다**(같은 설정에서 보급소 27.3/28.2/33.0분). ⚠ 벤치가 늦는 이유 둘(보급소 ROI 에 **병력 몫이 없다** · 정책 A 는 **저축을 안 한다**)은 확인했지만, 「인구 막히면 모은다」로 고쳐 봤더니 **흔들림 안**이라(28.2 → 31.3분) 안 남겼다. 🏠 **첫 보급소를 3만 → 5,000 으로 내렸다**(2026-09-11 사용자 요청 · `CAMP_SUPPLY_FIRST` · BALANCE §5-16) — **첫 채만**이고 둘째부터는 옛 값 그대로다(3.6만). ⛔ `CAMP_SUPPLY0`(3만) 자체를 내리지 말 것(사다리 전체가 싸져 후반 인구가 헐거워진다). 📏 실측 세 판: 보급소 **27~33분 → 19.3~19.6분**(9분 당김)이고 **판 사이 흔들림이 5.7분 → 0.3분**으로 사라졌다 — 「우연히 3만을 모으는 때」가 **결정적인 시각**이 됐다. 타임라인으로 인구 천장이 20분에 10 → 18 로 열리고 그 뒤 병력이 8 → 18 로 꾸준히 차는 것을 확인했다. ❌ 다만 **「병력 5기」 시각은 안 당겨졌다**(30~31분) — 인구는 열 칸이 남아 있었으므로 남은 병목은 **지갑**이다(벤치가 현금을 `RESERVE` 600 까지 긁어 가서 유닛 살 현금이 늘 없다). ⇒ 그건 **벤치의 성질**이고 ⛔ 게임 값으로 고치려 하지 말 것. 🔟 **1만도 재 보고 되돌렸다**(2026-09-11 · BALANCE §5-16 끝) — 1만이면 **흔들림이 되돌아오고**(0.3 → 4.6분) 병력 5기만 4분 빨라지는데, 그 자리는 위에서 밝힌 **벤치 지갑 성질**이라 신호가 약하다. ⛔ 「너무 싸다」는 느낌만으로 올리지 말 것 — 올리면 위 표의 값으로 돌아간다. 🧰 벤치에 남긴 자 둘: 「🏁 첫 도달」에 끝 병력·30/40/60기 · `TIMELINE=1`(15초 표본 · **인구 칸을 꼭 함께 볼 것**).
  ⛔ **배수 항은 전부 「더한다」 — 곱하지 말 것**(실측 5회 만에 ×1,900만 · BALANCE §0).
  ⛔ **「환생 관문 문턱 완화」를 성장 항목으로 두지 말 것** — 남은 되먹임 고리다.
  ⛔ **룬을 유즈맵으로 옮기지 말 것**(2026-09-08 접음) — 룬은 젬으로 사는데 오토배틀은 8인 대전이라
  「현질하면 대전에서 이긴다」가 된다. 룬은 캠프에 남는다.
  🪧 **아군은 저절로 진격하지 않는다 — 자리를 지킨다**(2026-09-10 사용자 확정 · `js/21-camp-battle.js`).
  자는 **하나**다: 적 유닛이든 **적 건물이든 자리에서 `campEngageOut`(**400** · 2026-09-11 사용자 확정 — 1200 은 기다리던 시절의 값) 안의 것만** 친다.
  ⛔ 옛 규칙(「표적 건물이 있으면 거기로 간다」)은 그 목표만 자리 제한을 안 타서, 던전에 들어가는
  순간 전 병력이 맵 끝까지 **자동 돌격**했다 — 플레이어의 판단이 사라진다. 되돌리지 말 것.
  🖐 **진격은 드래그 명령**(`campMoveSel`)이 자리를 옮겨서 한다 — 새 자리에서 같은 규칙이 다시 돈다.
  자리 안의 것을 다 깨면 멈추고, 플레이어가 또 민다(실측: 던전 1 을 드래그 12~17회로 170~240초에 깬다).
  ⚠ **아군(me)에만** 건다 — 적(ai)은 내 기지를 치러 오는 쪽이라 규칙이 다르다(걸면 패배 규칙이 죽는다).
  ⚔ **적이 먼저다**(2026-09-10 사용자 확정 「적을 먼저 쳐야 아군이 덜 맞는다」) — ⛔ 「건물에 닿은
  유닛은 건물부터」로 되돌리지 말 것. 그건 적이 **틈 없이** 흘러나오던 시절의 우회다.
  ⏳ **틈은 스폰이 만든다** — 한 번에 많이(8~18마리) · 주기는 길게(16~11초 · `CAMP_FOE_RELAY_S`/`_N`).
  **몰려온다 → 다 잡는다 → 그 틈에 벽을 친다.** 무리를 정리하고 남는 시간이 곧 공성 시간이라,
  「우리 화력이 무리를 제때 잡는가」가 그 관문의 물음이 된다(실측 틈 39~63% · 화력이 셀수록 커진다).
  ⛔ 옛 값(2~5마리 / 1.8~3.4초)으로 되돌리지 말 것 — 틈이 없어 건물을 영영 못 친다.
  ⚠ 상한(`CAMP_FOE_LIVE_MAX`)은 **무리의 두 배쯤**이고, 자리를 볼 때 **무리 전체가 들어가나**를 본다
  (「한 마리라도 들어가나」로 재면 상한을 무리−1 만큼 넘긴다 · 실측 24 > 18).
  ⛔ **진격 정지 판정(`CAMP_STALL_T`)을 되살리지 말 것**(2026-09-10 제거) — 가만히 서 있는 것은
  이제 플레이어의 선택이라, 두면 기다리는 사이 75초마다 원정이 강제 종료된다.
  💀🏥 **죽음과 회복 — 규칙 넷이 한 벌이다**(2026-09-10 사용자 확정 · `ARCHITECTURE.md` §「죽음과 회복」).
  ① **죽으면 영구히 죽는다**(부활 없음 · 건물에서 다시 뽑는다 · `campReapDead`) ·
  ② 죽으면 **인구와 재구매 배수가 되돌아온다**(`campUnlist`) ·
  ③ 🏥 **회복 존** — **내 구역 입구**(`CAMP_HEAL_GY` 0.36 = 맵의 난간·빗금 · 실측) 아래면
  **5초마다 체력 가득**(유닛·살아 있는 내 건물 · `CAMP_HEAL_S` · `campHealZone`) ·
  🧱 **밖에도 건물은 지을 수 있다**(벙커·포탑 · ⛔ 막지 말 것) — 대신 **노란 격자 + 「이 구역은
  회복되지 않습니다」**(`campBuildNoHeal` → `.bfoot.warn`/`.bghost.warn`/`.bhint.warn`) ·
  🔨 **건물은 맞는 중에 안 고쳐진다**(직전 주기에 줄었으면 건너뛴다) — ⛔ 무조건 채우지 말 것:
  실측에서 그 때문에 ④가 죽어 있었다(전멸 뒤 10분에 본부 750↔728 · 회복이 적 화력의 35배).
  고친 뒤 전멸→본부 파괴 **165~185초**(사다리 전체 동일 · BALANCE §5-10-2) ·
  ⚠ **유닛에는 걸지 말 것** — 유닛은 지켜 주고 건물은 소모된다 ·
  ④ **패배 = 본부 파괴 하나뿐**(전멸은 패배가 아니다).
  ⭐ 이래야 난이도의 문턱이 **한 자리**로 모인다: 「적 화력이 회복 속도를 넘는가」.
  ⛔ **`_wiped`(전멸=원정 끝 · 2026-09-09)를 되살리지 말 것** — ②가 그 순환(병력 0 → 못 되살림)을
  이미 끊었고, 되살리면 「전멸했지만 돈이 있어 다시 세운다」는 반격이 사라진다.
  ⛔ 부서진 건물을 회복 존이 살리지 말 것(적이 본부까지 못 와 ④가 죽는다) ·
  ⛔ 자리(`_post`)가 아니라 **지금 서 있는 곳**으로 잴 것(자리로 재면 적진에서도 회복된다) ·
  ⛔ 경계를 `techY0`(0.18)나 ART.md 표의 0.46 으로 되돌리지 말 것 — 전자는 통로 한복판이고
  후자는 석판 한참 안쪽이다(둘 다 실측으로 물렸다) · ⛔ 경고용 바를 새로 만들지 말 것 ·
  ⛔ `campUnitOwned` 에 전장 명부를 더하지 말 것(2026-09-10 실측: 두 번 세어져 배수가 1.30 → **1.69/기**).
  ⛔ 되살아나면 안 되는 이름들: `campRoundRevive` · `campCatchDown` · `campReviveStep` · `CAMPB._down`
  (남은 것은 `campHealAll` = 관문·던전 전환 때의 **판 전체 회복** 하나이고 회복 존과 다른 자다).
  🧯 **적 상한**(`CAMP_FOE_LIVE_MAX`) — 상한이 없으면 못 이기는 판이 **반드시 지는 판**이 된다
  (죽이는 속도가 나오는 속도보다 느려지는 순간부터 무한히 쌓인다). ⛔ 없애서 난이도를 올리지 말 것.
  📏 **난이도 사다리는 실측으로 맞는다 — ⛔ `CAMP_GATE_RATE` 를 만지지 말 것**(2026-09-11 ·
  `node scripts/camp-clear.mjs` · 화력병 20기로 던전 1 ×5 161초 · 2 ×36 172초 · 3 ×257 141초 ·
  필요 배수의 비가 `campFoeDiff` 와 같고 클리어 시간도 셋이 비슷하다 · BALANCE §5-10-4) ·
  ⛔ **「전장이 닫혔다」를 패배로 읽지 말 것** — 완주도 `campBattleClose` 를 지난다(판정은
  `C.dgDone[D]` 하나 · 이걸로 한 번 헛짚어 「×20 으로도 못 깬다」는 결론을 냈다) ·
  ⚠ 스모크의 「던전 1 이 실제로 깨진다」는 앞 step 들이 쌓은 연구 위에서 도는 값이라
  **깨끗한 문턱이 아니다** — 문턱은 그 스크립트로 잰다.
  📏 **난이도 사다리는 관문 6개**(`CAMP_GATE_RATE`)다 — ⛔ 라운드 50 눈금으로 되돌리지 말 것
  (그러면 던전 안은 ×1.36 으로 평평하고 던전 넘을 때만 ×540 절벽이다 · BALANCE §3-2-17).
  ⛔ `campFoeDiff` 의 **천장을 옮기지 말 것** — 보상·환생 포인트가 같은 값을 읽는다.
  🔗 **릴레이 — 적은 「활성 건물」 한 곳에서만 나온다**(2026-09-09 사용자 확정 · `js/23-camp-dungeon.js`).
  살아 있는 진행 건물 중 **차례(step)가 가장 앞선 한 채**가 제 자리에서 적을 뽑고, 깨면 다음 채가
  이어받아 **더 센 유닛을 더 자주** 보낸다(`CAMP_FOE_RELAY_S`/`_N`). ⛔ 「살아 있는 건물 수로 나눈다」로
  되돌리지 말 것 — 밀수록 편해져 **마지막 건물이 제일 시시해진다**. ⚠ 시간이 아니라 **내가 깬 것**에
  반응하므로 「안 보면 손해」가 아니다. 🚪 기지는 **3구간**이고 구간마다 **문지기 탑**이 하나다 —
  그 탑이 살아 있으면 그 구간의 진행 건물을 **못 때린다**(`campFoeZoneOpen`). 탑·보급고는 언제든 표적이다.
  ⚡ **보급고**(옛 「치장」)를 깨면 30초 자원 ×2(`campDepotMul` — ⛔ `campCommonMul` 에 넣지 말 것: 피버와
  같은 이유로 대표 숫자가 튄다). 🔌 D3 파일런은 **그 구간의** 탑만 먹인다(⛔ 기지 전체로 보지 말 것).
  ⛔ **「치장(deco)」을 되살리지 말 것** — 표적이 안 되는 건물은 화면만 채우고 판단을 안 만든다.
  🏰 **던전이 「라운드 50 방어」에서 「적 기지 치기」로 바뀐다** — 진행 건물 6채(+부수 6채)를 하나씩
  부수고, 깰 때마다 적이 세진다. ⛔ **라운드는 없어진다**(칩·던전 선택 슬라이더·라운드 부활·룬 칸 해금
  기준까지 한꺼번에 갈 것 — 반쯤 바꾸면 두 체계가 공존해 어긋난다). 지면 직전 체크포인트로 되돌아가고
  **재화는 지키되 죽은 유닛은 다시 산다**(공짜 복구는 무한 재시도라 판단이 사라진다).
  🔁 **환생 한 바퀴 = 한 종족**(종족은 환생 때 고른다) · 무한모드 진입은 **「던전 3 클리어」**.
  🔁 **환생 포인트를 쓰는 곳은 「환생 강화」 하나다**(`CAMP_REB_UPG` · `campRebUpgBuy` · 화면 `#rebUpgScreen`).
  갈래 셋(스킵 · 자동화 · 상한 해제)이고 자루는 `C.rbUpg` — **환생을 넘어 남는다**.
  ⛔ **배수를 파는 항목을 만들지 말 것** — 환생하면 저절로 +1 이라 살 것이 아니다.
  ⛔ **별자리 트리로 한 벌 더 만들지 말 것.** 2026-09-11 에 실제로 **두 벌**이 됐다
  (`CAMP_RBT_LINES`/`CAMP_RBT_BR`/`campRbtVal` · `campCap` · `C.rebTree` · 「지금 트리」 스위치
  `campWithRebTree`/`campWithGrowTree`) — 같은 지갑 · 같은 갈래 · 같은 줄 이름이라 서로를 모른 채
  포인트를 나눠 썼다. 사용자가 **메인 쪽으로 합쳤다**(그 이름들은 전부 지웠고 스모크가 잰다).
  ⛔ 트리는 **성장 트리 하나뿐**이다 — `campRtLines()`/`campRtBRs()` 에 갈림을 다시 넣지 말 것
  (효과 함수가 통째로 0 이 되는 누수가 거기서 났다).
  🤖 **자동 일꾼·자동 연구**(`autoWk`·`autoRes` · `campAutoTick` · 캠프 시계가 부른다)는
  그때 합치면서 **환생 강화 표로 옮겨 온 둘**이다. ⭐ 「켜고 끌 이유가 없는 것만」 골랐다 —
  일꾼은 상한까지가 늘 이득이고, 연구는 **가스 전용**이라 미네랄과 안 다툰다.
  ⚠ 자동화는 **주기(2초)마다 하나씩**만 한다(⛔ 한 틱에 몰아 사지 말 것) · 구매는 사람이 누르는
  **같은 함수**(`techDoProduce`/`techDoResearch`)를 부르되 그동안 **입을 막는다**(`campAutoQuiet`) ·
  💰 돈 문지기는 자동 강화와 **같은 것**(`CAMP_AUTOUPG_KEEP` 배가 남을 때만) — ⛔ 빼지 말 것:
  유닛을 사려고 모으는 미네랄을 자동화가 먼저 써 버린다. 자동 건설은 자리 찾기가 붙어 아직 없다.
  ⚠ **연구 건물이 없으면 자동 연구는 아무 일도 안 한다**(정상 · 자동화는 내가 지어 둔 건물 안에서만 돈다)
  — ⛔ 그걸 버그로 읽지 말 것(본부만 있는 판으로 재서 「1시간에 0레벨」이라는 잘못된 결과를 냈다).
  🔓 상한의 단일 소스는 **`campWorkerMax()`·`campUnitRate()`** 다(⛔ 상수를 직접 읽어 비교하지 말 것).
  ⚠ **보급소 상한은 팔지 않는다** — 엔진이 인구를 `TECH_SUP_MAX`(200)에서 자르므로 24채면 이미
  상한이고 25채부터는 지어도 인구가 1도 안 는다(실측 BALANCE §5-17 ③). ⛔ `TECH_SUP_MAX` 를 올려
  고치지 말 것(관리자·오토배틀 공유) — 캠프에서 그 벽을 넘는 길은 **`campApplySupCap`** 하나다.
  👷 **일꾼 값은 다항식이다** — `50 × (보유+1)²`(`CAMP_HIRE0`·`CAMP_HIRE_P` · 2026-09-11 실측 · BALANCE §5-18).
  ⛔ **지수(`50 × 2.5^n`)로 되돌리지 말 것** — 그 곡선에서는 **일꾼 축이 10기쯤에서 죽었다**
  (회수 495분 · 같은 자리 채취 강화는 41분). 그래서 상한 해제가 **아무 일도 안 하는 항목**이었다.
  ⭐ 일꾼과 채취 강화는 **같은 축**(둘 다 왕복당 +N원 · 선형)인데 값의 밑만 2.5 대 1.22 였다 —
  선형 수입에 지수 값을 붙이면 축의 수명이 밑에서 정해진다. ⛔ 밑만 낮춰서 고치지 말 것(결국 터진다).
  ⚠ 고친 뒤 둘이 **자리를 바꾼다**: 채취 Lv.0 은 채취가 압도 · **Lv.30 이 갈림**(24분 ↔ 25분) ·
  Lv.60 은 일꾼이 압도. 재는 자는 **회수 시간**이고 도구는 `node scripts/camp-hire-bench.mjs` 다.
  ⛏ **광맥 자리는 일꾼 상한을 따라간다** — `campMineCap()` = `max(5, ceil(campWorkerMax()/CAMP_MINE_COLS))`
  (2026-09-11 사용자 요청 「광맥 자리도 열어줘」). ⛔ **상수로 되돌리지 말 것** — 상수(5)였기 때문에
  상한 해제로 80기를 열어도 자리가 40이라 41기부터 한 기가 **절반 값어치**였다(Δ 883 → 439 → 고친 뒤 1,064).
  ⚠ `cap` 은 광맥 객체에 **찍히는** 값이라 `campMineCapSync()` 가 캠프 시계에서 다시 찍는다(⛔ 찍는 곳을 늘리지 말 것).
  ⛔ 칸 수(`CAMP_MINE_COLS` 8)를 늘려서 열지 말 것 — 그건 화면이다(2026-09-02 사용자 확정).
  ⚠ 기본 상태에서는 `max(5, ceil(40/8))` = 5 라 **지금과 똑같다**(8 × 5 = 40).
  📊 **옛 환생 트리 여덟 줄의 값도 재 뒀다**(BALANCE §5-17) — 줄은 사라졌어도 **잰 사실은 쓸모가 있다**:
  「관문 n채를 부순 채로 시작」은 관문 2채(=33%)를 건너뛰어도 클리어가 **14%만** 줄었다
  (릴레이가 관문 3 부터 돌아 적이 더 세다). ⛔ 되살릴 일이 생기면 그것부터 볼 것.
  🏕 **캠프 = 집(던전 0) · 던전 = 원정**(2026-09-09 확정). 탭·일꾼 경제는 캠프에서 계속 돈다.
  지면 **캠프로 돌아와 재정비**하고 다시 들어가면 **그 던전 처음부터**(부순 건물은 되살아난다 · 재화는 지키고
  죽은 유닛은 다시 산다). ⛔ 옛 「직전 체크포인트」 안은 폐기.
  🎨 **던전 개성 확정**(2026-09-09): 적 종족 = 던전 · 커리큘럼(던전 하나가 하나를 가르친다) · **던전 클리어가 테크를 연다** ·
  배치·기믹 하나. ⛔ 「적 기지가 시간이 지나면 성장」 금지(안 보면 손해). ⭐ 적 기지는 **배치 데이터에서 만들 것** —
  나중에 남의 캠프를 치는 비동기 습격이 그 데이터를 그대로 쓴다. 재미 후보 넷(최고기록 타이머·반격 웨이브·전리품·안개)은 미확정.
- 🗺 **지형(벽·언덕·램프)은 `js/24-terrain.js` 가 소유한다**(2026-09-10 · 구조는 `ARCHITECTURE.md` §「지형층」). 층이 셋이다: **바닥**(통 텍스처 `.bmapFloor`) · **지형**(투명 배경 `.bmapTerr` · z 1) · **건물·유닛**(3D). ⛔ 바닥이 통째로 들어간 「완성 벽 타일」로 되돌리지 말 것 — 바닥 종류마다 벽 세트를 다시 뽑아야 하고 언덕이 들어오면 조합이 곱해진다.
  - 📐 **격자는 안개에서 받아 온다**(`G.tech.fog` 의 cols·rows·wy0) — ⛔ 따로 계산하지 말 것: 시야 차단이 `f.height` 를 읽으므로 두 격자가 어긋나면 「보이는 절벽」과 「가리는 절벽」이 한 칸씩 밀린다. ⛔ 서명(`CAMPT.sig`)에 `campFogTop()` 같은 흔들리는 값을 넣지 말 것 — **매 프레임 다시 굽는다**(1200×2000 캔버스 · 실측: 스모크가 통째로 멎었다).
  - 🎨 조각은 **네 조각 오토타일**이다(한 칸을 1/4씩 · 이웃 셋만 본다 · 그림 12칸으로 256가지). ⛔ 47조각 비트마스크·회전 재활용으로 되돌리지 말 것. 지형은 안 변하니 **한 번 굽고**(`campTerrBake`) 매 프레임엔 한 장만 그린다.
  - ⚠ **고원 윗면은 반투명**이다 — 지금 바닥은 배경 그림 한 장이고 거기 고원이 이미 그려져 있다(ART.md §17). 불투명하게 채우면 커다란 회청색 판이 된다(실측). 바닥이 타일로 바뀌기 전에는 되돌리지 말 것.
  - 🚪 **오르막(램프)은 유일한 통로다**(⛔ 빼지 말 것) — 📐 폭 5칸 · **길이 1칸**(2026-09-11 사용자 확정: 「4칸에 걸쳐서 올라가는게 아니고 1칸만에 바로」) · 둘 사이 12칸 이상 · 입구 줄은 **평평**(전부 실측 · 스모크가 잰다) · 🎨 그림은 **「절벽에 난 틈」**이다 — 오르막 칸의 **아래 모서리**에서 시작해 낮은 땅으로 내려간다. ⛔⛔ 절벽 선 **위**(고원 안)에서 그리지 말 것: 고원과 색이 다른 것은 무엇이든 **「고원에 올려놓은 상자」**로 읽힌다(2026-09-11 에 이걸로 세 번 헛돌았다 — 밝은 미끄럼틀 → 사다리 난간 → 흰 판) · 셋이 함께 있어야 한다(**단 셋**(위가 밝고 아래로 어두워진다) · 단마다 밑의 **어두운 홈**(밝은 선 = 사다리 가로대) · **쐐기 옆벽**(위는 폭 0 · 아래로 벌어진다 — ⛔ 폭이 일정한 띠는 사다리 난간이 된다)) · ⚠ 입구에서 고원의 **밝은 테를 걷어 낼 것**(안 걷으면 구멍에 뚜껑을 덮은 꼴) · 🎬 고르는 도구는 **`scripts/camp-ramp-pick.mjs`**(⚠ 비교판도 게임과 **같은 겹치기**라야 한다 — 한 캔버스에 그리면 `clearRect` 가 바닥까지 지워 **검은 구멍**으로 보인다: 1차 비교판이 통째로 거짓이었다) · 🚧 **길이 막히면 안 된다**(`campTerrConnected`) · 🧱 벽은 **0.05~0.16 띠에만**(내 격자·적 기지 줄과 겹치면 건물 위에 선다) · ⛰ 고원 밑변은 **계단**(열마다 따로 굴리면 빗살이 된다) — ⚠ **오르막 입구를 평탄화한 뒤에는 양옆을 한 칸씩 이어 내릴 것**(`TERR_EDGE_MAX` 만큼 단차가 생겨 빗살이 된다 · 2026-09-11 회귀 · 씨앗에 따라 터져 한동안 운으로 통과했다 · 스모크 ⑤ 가 잡는다).
  - 👁 **시야 차단은 배선됐다**(2026-09-10 2단계) — 막는 일은 엔진(`_fogReveal`)이 이미 하고 있었고, 새로 만든 것은 **안개의 `f.height` 를 채우는 함수 둘**뿐이다(`campTerrHeightFill` 훅 ← `techFogInit` · `campTerrPushHeight`). ⛔ 차단 규칙을 새로 짜지 말 것 · ⛔ 훅을 「sync 만 부르고 끝」으로 줄이지 말 것(안개는 방금 새로 파여 높이가 0인데 `techFogInit` 이 **바로** 시야를 계산한다 → 첫 시야가 절벽을 넘어 고원 건물이 `b.vis` 로 켜진다) · 🚪 **램프는 낮은 쪽으로 친다**(⛔ 고지로 치면 올라갈 길이 어둠에 잠긴다) · 🧹 **집으로 오면 고저를 걷는다**(⚠ 우리가 심은 것일 때만 — 관리자 `TECH_HILL` 을 지우면 안 된다).
  - 🚶 **길막기는 「전장 흐름장」이다**(2026-09-11 3단계 · `campTerrMask`→`campTerrFlow`→`campTerrWay` → `campMove` 가 **목표만 바꾼다**). ⛔⛔ **지형을 `_techNavRects` 에 얹는 방식으로 되돌리지 말 것** — `_techFindPath` 는 기지 격자 전용이라 `p[1]>0.12` 로 노드를 걸러 **격자 위 지형의 꼭짓점을 전부 버린다**(장애물은 보되 돌아갈 모서리가 안 생겨 **아군이 갇힌다** · A/B 6판 중 2판). 지형은 흐름장, 건물은 A\* — ⛔ 하나로 합치지 말 것 · ⛔ 밀어내기를 더하지 말 것(2026-09-05 갇힘의 원인).
    - ⚠ **레인 밖(gx 0.06~0.94 · gy −0.431~0.62 밖)을 반드시 막을 것** — 전장이 못 가는 자리다(실측: 유닛이 30초간 못 가는 칸으로 가라는 지시를 받고 안 움직였다). 연결성 검사도 **레인 안에서**(`campTerrLaneRows` · ⛔ 「gy 가 든 행」으로 잡으면 한 행 어긋나 300/300 실패한다).
    - ⚠ 「막힌 칸을 밟은 프레임」은 **0~1 이 정상**(밀어내기를 안 하므로) — 우회를 끄면 3~8 이다. 스모크가 그 차이로 배선을 잰다.
    - ⚠⚠ **「오르막이 유일한 통로」는 격자 위에서만 참이고 전장에서는 거짓이다**(2026-09-11 · 300판 실측). 고원 밑변을 넘는 일의 **39%가 절벽을 그대로 통과한다**(2026-09-11 직선 판정을 **격자 행진**으로 고쳐 51.5% → 39.1% · ⛔ 선 위에 **점을 찍어 보는** 방식으로 되돌리지 말 것: 절벽 테두리가 한 칸 두께라 대각선이 점 사이로 샌다 · ⛔ 「점을 더 촘촘히」로 고치려 하지도 말 것 — 모서리를 스치므로 어떤 간격에도 빈틈이 남는다 · 스모크가 촘촘한 점 찍기를 기준자로 견준다) — 오르막에서 6칸 이상 떨어진 자리에서 넘은 것이 177회라 표집 오차가 아니다. 원인은 **밀어내기를 안 하는 설계**(목표만 바꾸고 몸은 안 막는다)이고 **내 오르막 변경 전에도 45.3%** 였다. ⭐ 그래도 흐름장은 제 일을 한다(끄면 87.3% · 막힌 칸 프레임 24 → 161). **고원 도달은 300/300 이라 진행에는 영향이 없다.** ⛔⛔ **「절벽 위에서 속도를 깎는다」는 해 봤고 안 된다**(2026-09-11 · 시도 후 되돌림 · ⛔ 다시 시도하지 말 것): ① 속도는 **방향을 안 바꾼다**(늦게 넘을 뿐 덜 넘지 않는다) ② 막힌 칸을 밟는 것이 판당 중앙 17 유닛프레임뿐이라 **깎을 시간이 없다** ③ 띠를 넓히면 **오르막 입구까지 느려진다**(띠 2칸에서 40.3% 로 악화 · 등반 28초). 실측은 ARCHITECTURE §지형층. 🔍 **갈라 쟀다**(2026-09-11 · 300판 567회): 넘긴 것은 **이동 명령 86.4%** · 겹침 밀어내기 12.5% · 기타 1.1%. 그 86.4% 중 **51.6% 는 우회점도 선도 멀쩡한데 넘었다**(명령 방향에서 평균 **38.6°** 이탈) · 안전망이 막힌 우회점을 준 것은 **0%**. ⭐⭐ **남은 원인은 판정이 아니라 「조타 이탈」**이다(`stepUnitMove` 의 회피·조향) — 지형층은 제 일을 하고 있다. ⛔ 선 판정을 더 손봐도 못 줄인다 · ⛔ 밀어내기는 금지(갇힘) · ⛔ 속도 벌칙도 안 된다(위). ✅ **「우회점을 가깝게」는 먹혔다**(`TERR_WAY_NEAR`=**1칸** · 800판): 절벽 통과 33.0% → **23.1%** · 등반 시간 그대로 · 움직임도 좋아졌다(떨림 9.4 → **6.6회/유닛** · 사거리 안 비율 14.1% → **18.9%**). ⛔ 6칸 이상으로 되돌리지 말 것(6칸은 옛 동작과 차이가 없다 — **아주 조일 때만** 효과가 난다) · 스모크가 6칸을 글자 그대로 잰다. 🧾 남은 23% 는 `stepUnitMove` 조향의 몫이라 ⛔ 18-strike 를 고치지 않는 한 여기서 멈춘다.
  - 🧱 **바닥은 타일이다**(2026-09-11 사용자 확정 「전면 타일로」 · 표는 `CAMP_TILE`(19-camp.js) 하나 · `assets/tiles/camp_*.webp`). ⛔ 그림 한 장으로 되돌리지 말 것(확대하면 뭉개지고 3D 와 축척이 안 맞았다) · ⭐ 배경 변수는 `--campBg` 그대로 쓰고 **까는 방식만** `#phone.tileFloor` 로 가른다(⛔ 두 번째 변수 금지) · 📐 크기는 **px 180**(⛔ 격자 한 칸 8px 에 맞추면 무늬가 통째로 뭉갠다 · 실측) · 🌑 **톤 낮추기(.60)를 걸지 말 것**(밝은 그림용 값이라 타일엔 검은 여백이 된다 — 밝기는 CSS 한 줄로 조절) · ⚠ 바닥이 평평해지므로 **지형층 고원 색이 진해진다**(`TERR_HI_TOP.tile` · ⛔ 옛 그림 던전에서 진한 값으로 되돌리지 말 것) · 🩹 새 타일은 **`scripts/tile-import.mjs --flat`** 으로 들인다(세로 밝기 기울기를 나눈다 — 안 하면 가로 줄무늬).
  - 🎬 **손댔으면 `node scripts/camp-terr-shot.mjs`(지형) · `node scripts/camp-terr-climb.mjs`(정말 올라가지나) · `node scripts/camp-ramp-pick.mjs`(오르막 안 고르기) · `node scripts/camp-floor-pick.mjs`(바닥 크기·밝기) 로 눈으로 볼 것.**
- 🧱 **밸런스를 잴 땐 「도달 던전·라운드」를 쓰지 말 것**(2026-09-08 실측) — 한 팔 안의 편차가 팔 사이 차이만큼
  크다. **번 돈**이 훨씬 안정적인 자다. 전략이 갈리는지는 **`node scripts/camp-strat.mjs`**(전략 프리셋 + 갈림도)로 잰다.
  ⚠ 옛 「구매 정책 A/B/C」는 **전략이 아니다**(셋 다 같은 ROI 순위) — 그걸로 잰 비교는 전부 같은 전략을 견준 것이다.
- **Read `GAME_DIRECTION.md` before any 사냥터(=지금의 캠프) gameplay/성장 decision** — 이 게임을 *어떤 게임으로* 만들 것인가의 단일 소스(재미·성장 축·확장·유보). 전투 성장은 추상 카드가 아니라 **`TECH_TREE` 연구**다. ⚠ `HUNT2.md`(구현 부록)는 그보다 먼저 쓰여 **일부 결정이 무효**다 — 무효 목록은 `GAME_DIRECTION.md` §6.
- 🗄 **안 쓰는 코드는 `js/99-attic.js`(다락)에 있다 — 목록은 `ATTIC.md`.** 🔒 **CSS 도 다락이 있다**(`css/99-attic.css` · ⛔ 링크하지 않는다) 그리고 **`npm test` 가 죽은 코드 총량을 잠근다**(`scripts/dead-audit.mjs` · 2026-09-05): 아무도 안 부르는 함수·어디에도 없는 CSS 클래스가 새로 생기면 실패한다 → `scripts/attic-move.mjs`/`attic-css.mjs` 로 옮기거나 이유를 적어 `test/dead-known.json` 에. ⚠ 새 화면을 만들 때 **grep 에 걸린 클래스가 다락 것이면 쓰지 말 것** — 그게 「옛 디자인이 되살아나는」 경로였다. 옛 화면·옛 디자인을 되살리기 전에 거기부터 볼 것. ⛔ 지우지는 않는다(유보는 삭제가 아니다). 되살아나면 스모크 「다락」이 잡는다.
- **Read `ARCHITECTURE.md` first** — section map (jump by banner search strings), global state, frame pipeline, M3D API, and a pitfall list. Update it when structure changes.
- **Read `DESIGN.md` before any visual change** — 확정된 스타일 규칙(각진 SF · 볼륨 3단 · 라운드 0/3/6/9 · 역할별 액센트). 값은 고민하지 말고 표에서 꺼낼 것. **일괄 치환 금지** — 화면을 만지는 김에 그 화면만 체크리스트를 통과시킨다(touch-it-fix-it).
  - 🎬 **화면 전환(페이드·디졸브·줌)을 만진다면 `DESIGN.md` §5.5 체크리스트를 먼저 읽을 것.**
    로딩→종족 선택 전환 하나에 몇 시간을 썼고, 같은 자리에서 네 번 넘어졌다(2026-08-27).
    요지: **연출은 프레임을 저장해서 눈으로 본다**(`SHOT_SAVE=1 node scripts/shot.mjs flick`) —
    평균 밝기·시점 표본은 배경 크기 변화·작은 UI·한 프레임짜리 섬광을 **전부 놓친다**.
    그리고 투명도로 안 고쳐지는 전환 버그는 대개 **z 축(가림 순서)** 이다.
- **Read `ART.md` before generating any image asset** — 장면 이미지(유즈맵 키 아트·배경)의 모델·비율·프롬프트 템플릿·후처리 규격. **프롬프트를 새로 쓰지 말 것**: 고정 블록 4개를 그대로 복사하고 장면 한 칸만 바꾼다. 세션이 바뀌어도 같은 스타일이 나와야 하므로 이 문서가 단일 소스다. 계열이 셋이다(유즈맵 키 아트 §2~§7 · 타이틀 배경 §8 · **유닛 참고 아트 §9**) — 어느 계열인지 먼저 정할 것. 새로 뽑았으면 그 계열 절에 전문을 추가하고 **`node scripts/art-lint.mjs`** 로 규격(고정 블록·금지 표현·맵 표↔`UMAP_BG` 일치)을 확인할 것.
- **Read `RACES.md` before touching any race/unit/building number** — 5종족 오각형 상성의 단일 소스(설계·실수치·상수·측정 결과). ⚠ 상성은 **모델로 추정하지 말고** `node test/race-matchup.mjs` 로 실제 엔진을 돌려 잰다 — 여기서 자체 웨이브 모델을 네 번 짰다가 전부 폐기했다. 종족을 오토배틀에 넣을 땐 `ARCHITECTURE.md` §1 의 "조용히 빠지는 표" 목록(`SB_ATK_MODE`·`UNIT_COMBAT_CLASS`·`FXLAB_AIR`·`TECH_BLDG_UNIT`)을 체크리스트로 쓸 것.
- **Read `GEM.md` before touching 젬·상점·부스트·환생 증폭** — 현질 재화의 자리(정체·획득·용도 둘·안전장치). ⛔ 젬으로 **영구 능력**을 팔거나 배율을 **곱해서 중첩**하면 지수 축이 둘이 되어 폭주한다. 상점의 장비·펫·동료 꾸러미는 **화면에서만 빼고 코드는 남긴다**(유보 규칙).
- 📊 **수치·방향 문서의 층위(2026-08-25 정리)** — 헷갈리면 이 순서다.
  0. 🚨 `GAME_DIRECTION.md` **§0-A**(2026-09-08 전면 개편) — 뼈대. **모든 것보다 위다.**
  1. `GAME_DIRECTION.md` — ***왜***(재미·성장 축·확장·유보). 최상위.
  2. `HUNT_R1.md` — ***설계 출발점***(1회차·환생 수치). ⚠ 규칙이 아니라 출발점 — 실측이 이긴다.
  3. `BALANCE.md` — ***실측 확정치***(지금 값·기준선·재는 방법). 값을 바꿨으면 여기를 갱신.
  4. `HUNT2.md` — ***구현 부록***(SC2식 기지 운영 = 지금의 캠프). 일부 결정 무효(목록은 `GAME_DIRECTION.md` §6).
  5. `GEM.md` — 젬·상점·환생 **증폭**만. 환생 규칙 자체는 `HUNT_R1.md` §4.
- **Read `BALANCE.md` before touching any growth/difficulty number** — 성장 축 넷의 역할 · 환생 규칙 · 지금 상수 · **실측 기준선** · 남은 일. ⚠ 값을 바꿨으면 **해석적 추정으로 끝내지 말고** 거기 §4 의 엔진 자동 플레이로 다시 재고 표를 갱신할 것 — 이 프로젝트에서 모델 추정은 여러 번 크게 빗나갔다(회수 시간·손익분기·레벨 간격). *왜 그렇게 설계했는가*는 `ARCHITECTURE.md` 가 단일 소스다.
- Behavioral verification = **`npm test`** (headless suite: `test/smoke.js` + `test/run-smoke.mjs`, groups lobby/game/sandbox **+ `duo(2인)`** = two real clients relayed to each other via `test/duo.mjs`) plus browser preview for visuals. Run it after every change; add a `step(...)` when you add a feature. Never claim "done" without it passing.
  - ⚠ **Touching anything multiplayer? The `duo` group is the only thing that catches a send/receive shape mismatch** — a one-page test with a stubbed channel never will. See `ARCHITECTURE.md` §8 「멀티 검증」.
- After editing JS, **syntax-check** the file you touched: `node --check js/<file>.js` (module file: `node --input-type=module --check`). No more extracting from the HTML.
- Edit with exact-string replacements; match the surrounding style and line endings.
- Commit only when asked; end commit messages with the `Co-Authored-By` trailer.

## 🧩 UI 단일 소스 원칙 (Single-Source UI — MANDATORY)
같은 UI를 두 번 만들면 반드시 어긋난다. 아래 규칙은 예외 없이 적용:

1. **재구현·복사 금지.** "메인에 있는 것을 가져와라/통합해라"는 요청 = 기존 DOM을 `cloneNode`로 재사용하거나 기존 함수를 호출. 마크업/SVG/CSS를 손으로 베껴 두 번째 구현을 만들지 말 것.
2. **"동일하게"의 검증 = 원본과 직접 diff.** 스타일 몇 개 비교로 통과 처리 금지 — 원본 요소와 innerHTML(값 제외)을 통째로 비교해서 동일함을 증명할 것. (선례: 스타일은 같았지만 단위 표기 `C/E/CP`가 빠져 다르게 보였음)
3. **새 UI가 필요하면 먼저 아래 레지스트리를 확인**하고, 기존 컴포넌트로 안 되면 새로 만들되 레지스트리에 등록. 비슷한 것을 새로 만들었다면 그건 버그다.
4. **낡은 사본 발견 시 보고.** 작업 중 중복 구현을 발견하면 사용자에게 알리고 통합을 제안.

### 🧹 잔상 금지 — 공용 자원을 빌리면 '지우고' 시작한다 (MANDATORY)
3D 렌더러·캔버스·모델 풀은 화면들이 **공유**한다. 새 화면이 이걸 빌려 쓸 때 이전 화면이 만든 것이 남아 있으면
그대로 배경에 비쳐 보인다(선례: HOME에 '미사일 포탑' 고스트 3개가 은은하게 남음).

- **숨기지 말고 삭제한다.** `visible=false`는 어딘가에서 다시 켜지면 도로 나타난다. 지우면 나타날 수가 없다.
- **빌릴 때와 돌려줄 때 양쪽에서** 지운다 — 한쪽만 하면 반대 방향 전환에서 샌다.
- 각 풀은 전부 '없으면 만든다' 구조라 원래 화면으로 돌아가면 알아서 재생성된다. 지우는 게 안전하다.
- 3D는 `M3D.clearGameModels()`(유닛) + **`M3D.clearIdlePools()`**(비콘·미건설 고스트·배치 고스트·건설 전시) 둘 다.
  `M3D.sync()`가 자동으로 숨겨 주는 풀은 일부뿐이라는 것을 전제로 짤 것.

### 공식 컴포넌트 레지스트리 (단일 소스)
| 컴포넌트 | 단일 소스 | 비고 |
|---|---|---|
| 자원 표시(크레딧/에너지/인구) | `#hudR` (메인 HUD) | 다른 화면은 cloneNode+숫자 치환 (예: `techMapRender`의 `.bres`) · 단위 `<i class="ru">C/E/CP</i>` 포함 |
| 프로필/커맨드 그리드 | `renderCmdGrid(host, model)` + `_cgSlotHTML`/`_cgInfoHTML` | 모든 프로필 시트(건설 탭·업그레이드 탭 공용) · 모델 객체로만 내용 제어 |
| 카드 높이/모드 | cm1(간소화 4그리드 1줄, 150px) ↔ cm2(최소화) | 2줄(cm0) 없음 · 5칸↑=페이지네이션(슬롯 위치 보존) |
| 생산 진행 | 파랑 바 `.cgBar`(앞 유닛 %) + 얇은 대기열 선 `.cgQBar`(수량·흰→빨강) | 연구도 동일: 남은 초 = 라벨 옆 `progTime` |
| 지정(선택) 표시 | 3D 하단 링 | 유닛·건물·라바·중립 자원(미네랄/가스) 전부 이것으로 통일 |
| 지정 해제 버튼 | 금지(⊘) SVG — 메인 `#deselTop` = 건설 `#btDesel` = 일시정지 카드 | 새 해제/중단 UI도 이 아이콘 재사용 · 🖐 **캠프 「화면 이동 모드」를 끄는 유일한 길**이다(2026-09-10 사용자 확정) — ⛔ 「탭하면 해제」로 되돌리지 말 것: 화면을 옮기다 손이 미끄러질 때마다 꺼졌다 · 모드 중에도 탭은 그대로 된다(유닛·건물·광맥을 고른다) — 모드는 「**빈 바닥 드래그가 팬이 된다**」는 뜻일 뿐이다 · 버튼이 켜지는 조건은 `techUIRender`(17-build-cards) 한 줄이고 상태는 `campPanIsOn()` 으로 읽는다(⛔ `_campPanMode` 를 공유 파일에서 직접 읽지 말 것) |
| **유닛 지정 중 내 건물 탭** | `techPtrUp`(17-build-cards.js) — **건물이 지정되고 유닛 지정은 풀린다** · ⚠ `techPtrDown` 끝의 이동 명령은 **건물 위에서는 안 나간다**(`if(e.type==='bldg') return`) | 2026-09-10 사용자 확정 · 🐞 2026-09-11 **전투 유닛은 안 되고 있었다**(사용자 신고) — down 끝의 이동 명령이 up 의 지정을 삼켰다. 일꾼만 됐던 것은 롱프레스(수리) 갈래가 먼저 return 해서 **우연**이었다(실측: 캠프에서 전투 유닛 지정 + 본부 탭 = 이동) · 스모크가 일꾼·전투 유닛 둘 다 잰다 · ⛔ 옛 규칙(「그 자리로 이동」)으로 되돌리지 말 것 — 건물을 고르려면 ⊘ 로 유닛을 먼저 풀어야 해서 두 번 눌러야 했다 · ⚠ 그 대신 **건물 위를 탭해 이동시키는 길이 사라졌다**(건물 옆 바닥을 탭한다) · ⚠ 롱프레스(탑승·수리)는 다른 갈래라 그대로 산다 · ⚠ 예외 하나 — 일시정지 건물 + 유휴 일꾼(`_keepWk`)은 재개에 쓸 일꾼 지정을 붙들어 둔다 · 🎒🔁 **자원을 든 일꾼 + 본진은 두 번 누른다**(2026-09-11 사용자 확정 · `_btResumed`): 첫 탭 = 「자원 작업 재개」(`_techResumeCarry` · 그 자리에서 본진으로 반납하고 계속 캔다) · **같은 본진을 한 번 더** 누르면 재개를 건너뛰고 그 건물이 지정된다 — ⛔ 첫 탭에서 바로 건물을 고르게 되돌리지 말 것(자원을 든 일꾼을 되돌려보낼 길이 사라진다) · ⚠ `_btResumed` 는 **탭 하나마다 비운다**(`techPtrDown`)라 딴 데를 한 번이라도 누르면 다시 「재개」부터다 |
| **확인 팝업(정말 하시겠습니까)** | **`.ecCard` + `.ecTitle`/`.ecMsg`/`.ecBtns` + `.ecCancel`/`.ecGo`** | 게임 나가기(`#exitConfirm`) = 로그아웃(`#logoutPanel`) — 한 컴포넌트(2026-08-27) · 되돌릴 수 없는 주 동작은 **붉은 글자**(`.ecGo`) · ⛔ 확인창을 새로 만들지 말 것 · ⚠ 설정에서 부르는 확인은 **`#phone` 직속 + z-index 97**(설정이 게임 밖에서 95라 그 아래면 통째로 가려진다) |
| 확정/취소 플로팅 버튼 | `.bArmBtns` (▶ ok / ✕ cancel) | 배치 확정·재개·철거 공용 |
| **부팅 로딩 터치 문** | **`bootTapWait()`**(js/08-ui-parts.js) + 문구 `.opTap` | 👆 막대가 100% 가 되면 **저 혼자 안 넘어가고 손가락을 기다린다**(2026-09-12 사용자 요청) · `enterAfterWarm` 안에서 `opBarDone()` 과 `titleToBlack()` **사이** 한 걸음 · ⛔ 새 전환을 만들지 말 것(있던 페이드를 그대로 탄다) · **로그인 뒤 부팅 로딩에만** 붙는다(유즈맵 게임 진입 `#gsRoot` 는 제외 · 사용자 확정) · 여는 길 셋: 진짜 `pointerdown`/Enter · `bootTapGo()`(스모크·`shot.mjs flick` 이 쓰는 뒷문) · `BOOT_TAP_GATE=false`(문을 끈다 — ⛔ 끈 채 내보내지 말 것: 스모크가 상수를 잰다) · ⚠ **`enterAfterWarm` 을 기다리는 곳은 전부 문을 열어 줘야 한다**(지금 네 곳) — 안 열면 문이 문서에 리스너를 건 채 남아 한참 뒤 다른 탭이 「게임 시작」으로 먹힌다(실측: 캠프 스텝 둘이 같이 터졌다) · 🔀 **막대가 다 물러난 뒤에 문구가 들어온다**(`--opTapOut` .30s → `--opTapIn` .42s · 둘 다 `#opening`) — 동시에 트면 바닥 같은 자리라 100% 막대 위에 글자가 겹쳐 지나간다(프레임으로 잡았다 · 평균 밝기로는 안 보인다 · 스모크가 차례를 정적으로 잠근다) |
| 게임 진입 로딩 | `#gsRoot` + `gameStartCountdown()` · `.teamed`(팀전) / `.solo`(개인) | 카드 덱 한 화면이 협동·팀전·개인을 다 맡는다 · 팀 색=카드 윗변 / 준비=밑변 · 초록은 '준비 완료' 전용 · 초상 `avatarHTML()` · 버튼 `.actBtn` |
| **액션 버튼(확정·취소·시작)** | **`.actBtn`** + `.pri`(주 동작) / 기본(하위) / `:disabled`(잠김) | **옅은 면 + 1px**(2026-08-26 · B4안) · 위계는 **면과 테두리의 밝기**(부 5%/.1 · 주 14%/.32 + 흰 글자) · ⛔ 볼록한 판·밑변 광원·`--font-ti`(Jua) 를 되돌리지 말 것 · 새 버튼에 면·테두리를 따로 쓰지 말고 클래스만 붙일 것(화면이 덮는 건 크기뿐) · `.cpBtns` 안에서 취소는 `.sub` · **팝업 버튼(`#ovBtn`/`#ovBtn2`/`.ecGo`/`.ecCancel`)도 같은 얼굴**(36px) — 로그인 `.authBtn`/`.authGuest` 만 제 디자인을 갖는다 |
| 유닛 초상 | `_techUnitPortrait(uid)` | 카드·헤더·대기열 공용 |
| 프리뷰 패널 | `#cstPrev` + `techHidePreview()` | (구 cstHidePreview는 삭제됨) |
| 알림/사운드 | `toast()` / `playSfx()`·`playSfxT()` | |
| **채팅 입력줄** | **`.msChatBar` + `.msChatSend`** (`css/40-social.css`) | 유즈맵 하단 도크 = 대기실 — 한 컴포넌트(2026-08-27 통일 · 옛 사본 `.lbChatBar` 는 없앴다) · 범위 배지(`.msScopeDD`)는 **선택 슬롯**이다(대기실은 고를 범위가 하나뿐이라 뺐다) · 전송은 **중립 밝은 회색** — ⛔ 빨강으로 되돌리지 말 것(대기실의 빨강은 「시작」 주 동작의 색) · ⚠ 인게임 `#chatBar` 는 **다른 것**이다(전장 위에 떠서 접히는 말풍선) |
| **인게임 채팅바** | `#chatBar` + `chatToggle()`/`chatOpenBar()`/`chatFoldBar()` | 접힘(말풍선 44px) ↔ 열림(`[∨｜입력｜전송]` 한 상자) · **유즈맵 안 전 구역**에 있다(캠프만 제외) · ⛔ 열려도 왼쪽 ∨ 를 없애지 말 것(접을 방법이 사라진다) · 상시 청록·붉은 밑변 광원 금지 · ⚠ 구역마다 시트가 다른 요소다(`.bp` ↔ 건설 `#btSheet`) — `_syncSheetLift()` 가 갈라 잰다 |
| 세로 스크롤바 | `.uiScroll` (CSS 공용) · 막대를 감추려면 **`.uiScroll.bare`** 를 함께 | 스크롤 영역에 클래스만 추가 · ⚠ Chrome 은 굵기가 **thin / none 둘뿐**이라 「더 얇게」가 없다 — 「거의 안 보이게」의 답은 `bare`(감춤)이고, 그러면 「더 있다」는 신호가 사라지므로 **아래 페이드**(mask-image)로 대신 알린다(룬 가방이 그 예다) · `::-webkit-scrollbar`를 새로 정의하지 말 것 (Chrome 최신은 웹킷 의사요소를 무시하고 표준 `scrollbar-width`/`scrollbar-color`만 적용 → 화면마다 굵기가 달라지는 원인이었음) |
| **재화 아이콘**(미네랄·가스·젬·인구) | **`resIco(key, cls)`** → `assets/icons/res_*.webp` | ⛔ **이모지를 임의로 넣지 말 것.** 한글 이름으로도 찾는다(`resIco('미네랄')`=`resIco('mineral')`) · 새 UI에서 재화를 표시할 땐 무조건 이 함수 · 상단 재화 바(`#curBar`)·인게임 HUD와 같은 그림이 나온다 |
| 상점 | `#shopScreen` + `SHOP_SECS` (추천 · 젬 상점 **두 칸**) | 캠프 기준 재편 2026-08-31 · 옛 5칸(한정구매·뽑기·재화·패키지·충전)은 사냥터 기준이라 **화면에서만 뺐다**(코드는 남음) · 캠프 지갑에 넣는 유일한 입구는 `campAddRes()` — ⛔ `PROF().pcoin` 은 옛 지갑이라 캠프와 안 통한다 · 팩 배수는 **합**(GEM.md §5-2) · 젬 = 유일한 현질 재화 |
| 정비(장비·펫·동료) | `#gearScreen` + `renderGear()` | 전용 화면 · 내용은 **전부 기존 렌더러 호출**: 장비=`renderProfGear()`(마을 장비창과 같은 함수) · 펫=`_shopPetPanel()`(상점 '보유 펫'과 같은 함수) · 동료=아직 시스템 없음(HOME 건설로 안내) |
| 보유 펫 목록 | `_shopPetPanel(note)` | 상점 ④ 구역 = 정비 '펫' 탭 — 한 함수 |
| 토벌 입구 | HOME 스킬 바(`renderHbBar()`)의 **토벌** 버튼 → `openDungeonHub()` 팝업 | 네비에서 뺐다(2번 칸은 정비) · 다른 화면에서 부르면 먼저 `openHome()` |
| **유즈맵 하단 채팅 접기** | `.msDockBar` + `mapDockToggle()`/`mapDockPeek()` | 한 요소가 두 모습 — 접힘=줄 전체(마지막 채팅+▲) / 펴짐=윗변 15px 손잡이(▼) · 접힌 줄은 `#msChat` 마지막 줄의 **복제**(채팅 렌더러는 하나) · 상태는 `nm_mapdock` · 🎬 **여닫을 때 높이가 자라고 줄어든다**(2026-09-12 사용자 요청 「위로 올라가고 내려가는 애니 · 속도감」 · 펴기 `--t-dock` .26s · 접기 `--t-dockOut` .2s · 곡선은 팝업이 쓰는 `cubic-bezier(.22,.9,.28,1)` 그대로 — ⛔ 새 곡선을 만들지 말 것) · ⛔ **되돌리면 애니가 통째로 죽는 것 둘**: ① 높이를 `flex-basis` + `min-height` 두 값으로 나누면 펼 때 min-height 가 먼저 계단처럼 뛴다(그래서 `height:max(...)` 한 줄이다) ② 접힘을 `flex:0 0 auto`·`.msSocial{display:none}` 으로 되돌리면 **전환할 상대가 없다**(접힌 높이는 `--dockPeek` 토큰 · 소셜은 opacity+visibility 로 사라진다) · 📏 실측 32.8 ↔ 211.4px · 스모크 「유즈맵 소셜 도크: 여닫는 애니」가 중간 프레임 수와 곡선의 앞쪽 빠르기를 잰다 · 🎞 눈으로 보려면 `node scripts/mapdock-shot.mjs`(10배 느리게 찍은 필름스트립 · ⚠ `pg.screenshot` 은 한 장에 100ms 넘어서 그걸로 샘플하면 늘 「이미 끝났다」만 나온다 — 속도는 rAF 로 잰다) |
| 상자(목록 판) | `.msPanel` | 유즈맵 목록 · 정비 · 캐릭터 공용 |
| **방향·증감 버튼(◀▶)** | **`.arwBtn` + `data-arw="l\|r"`** (`paintIcons`가 함께 채운다) | 기본 = **글리프만**(판·선 없음 · 그림자로 띄움 · 터치 영역 30px 유지) · 판이 필요한 자리에만 `.framed`(모서리 컷 테두리) · 새 화살표 SVG를 마크업에 직접 박지 말 것 |
| 로비 팝업 껍데기 | **`_lobbyOv(id, onClose)`** + `.ptInviteOv.top` / `.ptInviteCard` / `.ptInviteHead` | 친구 초대·친구 추가·파티 찾기 공용 · `#phone`에 붙여 유즈맵 도크와 마을 시트 어느 쪽에서 열어도 보인다 |
| 친구 목록 | `renderFriendList()` → `.ptHead`/`.ptTitle`(파티 머리줄과 같은 것) + `.foList` | 온라인/오프라인 **섹션 라벨 없음** — 정렬(`friendSortCmp`)이 온라인을 위로 올리고 오프라인은 `.foRow.off`(어두운 상자)로 갈린다 |
| 친구 추가 | `openFriendAdd()` → `#foAddOv` | 목록 위가 아니라 팝업 · 검색 id(`#foSearch`/`#foSearchResult`)는 그대로라 `friendSearch()`/`friendAdd()` 재사용 |
| 파티 게시판(찾기·만들기) | `openPartyFind()` → `#ptFindOv` + `pbRooms()`/`pbJoin()`/`pbCreate()` | 머리줄 **`파티 찾기` 버튼으로만** 연다(자동 노출 금지) · 맵과 무관한 자유 파티 |
| **캠프 종족** | **첫 바퀴는 유니온 고정** — `campEnter`/`campOpen` 이 `C.race='terran'` 을 박는다(2026-09-09 · REDESIGN_PLAN 1-E) | 종족 선택 화면(`#campRaceOv` · `campRaceSheet`/`campPickRace`)은 **다락**으로 갔다(ATTIC.md §5-D) — ⛔ 되살리지 말 것(스모크 「캠프: 종족 선택 · 진입」이 잰다) · 종족 변이는 **환생 뒤 단계 4** 의 몫이고 그때는 `segNavHTML(CAMP_RACE_ORDER)` 띠로 고른다 · ⚠ 첫 진입 연출(검은 판 → 캠프 → 튜토리얼 `tutoKick`)은 `campRaceToCamp()` 가 그대로 맡는다 — ⛔ `campEnter` 를 바로 부르지 말 것(튜토리얼이 안 뜬다) |
| **🏰 적 기지 화면**(던전) | **모델은 `js/23-camp-dungeon.js`** — `campFoeBld3D()`(3D 엔트리) · `campFoeMarks()`(표식) · `campFoeTgtName()`(띠) · `campFoeTapAt()`(탭) · `campFoeSheetModel()`(프로필) — **DOM 은 19-camp**(`campFoeOverlayHTML` → `#cstLabels .fbLayer`) | 🌫 **던전은 안개에 잠겨 있다**(2026-09-10 사용자: 「적 구역은 안개 · 내 본진은 깔끔하게 열림 · 유닛을 데리고 가야 시야만큼 열린다」) — **관리자 건설 탭의 안개를 그대로 빌린다**(`G.tech.fog` · 10-engine `techFogInit/Compute/Draw` · ⛔ 캠프용 안개를 새로 만들지 말 것) · 빌리면서 바꾼 것은 둘뿐이다: ① 덮는 **세로 범위**(`f.wy0~wy1` · 기본 0~1 → 던전은 격자 위 적 기지까지 · 칸 수 rows 도 범위에 비례해 늘린다 · ⛔ 기본값을 건드리지 말 것: 관리자·오토배틀이 같이 어긋난다) ② 시야를 내는 것에 **전장 병력**을 더한다(훅 `campFogExtra` — 엔진에 캠프 코드를 넣지 않으려고 뺐다) · 🏠 **내 격자(techY0 아래)는 매 계산마다 활성으로 칠한다** — 집은 탐험 대상이 아니다 · 👀 **적 건물·광맥도 안개에 가려진다**(2026-09-10 사용자: 「적 건물과 유닛도 암흑 안개에 가려지고, 다가가서 시야를 열면 보이도록」) — 그리는 판단은 `campFoeShown(b)` **하나**이고 그 자는 **`b.vis`**(내 시야에 실제로 들어온 적이 있나 · `campFogExtra` 가 켠다)다 · ⚠ **`b.seen` 과 다른 자다**: seen 은 **엔진의 표적 기억**이라 관문 규칙(`campFoeRevealGate`)이 아군을 안 멈추게 미리 켠다 — 그걸 그림에 쓰면 **가 보지도 않은 건물이 어둠 속에 뜬다**(실측) · ⛔ 3D·표식·탭·쪽지가 서로 다른 자를 쓰지 말 것(안 보이는 것이 눌린다) · 🧱 건물·광맥·가스는 **한 번 보면 남고**(지형 기억) 유닛은 **지금 보일 때만** 그린다 · 🚫 **안개 층(#cstFog)은 3D 캔버스(#techMap3d)보다 아래**(둘 다 z 7 · DOM 순서)라 3D 로 그린 것은 오버레이로 못 가린다 — 안개 속 적 건물·유닛·일꾼·광맥은 **엔트리에서 뺀다**(관리자 탭과 같은 규약 · `campFogHidesAt`=전장 좌표 · `techFogHidden`=격자 좌표 · ⚠ 둘을 섞지 말 것) · 👁 진입 뷰는 **보이는 것**에 맞춘다(`campFoeLookAt` — 안개 중엔 12채가 아니라 `campFoeShown` 만 + 내 격자 위끝까지 · **한 채도 못 봤으면 집을 보여 준다**(`campZoom`) — ⛔ 적 기지에 맞추지 말 것: 통째로 검은 화면이다 · 실측) · ⚠ 켜고 끄는 곳은 `campFogSync()` 하나(던전이면 켜고 캠프면 끈다 · 화면 클래스 `#phone.dgFog` 가 #cstFog 를 여닫는다) · ③안 「밑변 광원」(2026-09-09 사용자 확정 · 목업 `docs/mock/camp-foebase-4.html`): **진행 건물 6채만 바닥이 붉다** · 다음 표적 = **붉은 모서리 + 「다음 표적」** · ⛔ **못 본 건물의 속 빈 실루엣은 안개가 대신한다**(2026-09-10 · 안개가 켜지면 아예 안 그린다 — 둘을 같이 두면 어두운 안개 위에 실루엣이 떠서 「가려졌다」가 아니라 「덜 그렸다」로 보인다) · 잔해 = ✕ · 체력 선은 **맞은 건물에만** · 잠긴 구간의 진행 건물은 🔒 · ⛔ 번호 배지·깃발·후광 금지 · ⛔ **부수 건물을 물리지 말 것**(방어탑이 흐려지면 위험이 안 보인다) · 🏗 3D 는 **기지 건물과 같은 규약**으로 `M3D.syncBuild` 목록에 얹는다(`campWithBattleDraw` 가 전투 유닛 뒤에 덧붙인다 · id `cb_`+모델키 · fitW · z) — ⛔ 두 번째 3D 동기화 함수를 만들지 말 것(`techMap3DSync` 는 **아무도 안 부르는** 관리자 맵용이다 — 거기 확장했다가 되돌렸다) · 🗺 **적 기지는 격자 위 한 화면에 있다**(2026-09-09 사용자: 「지금은 너무 가깝다 — 화면 완전 위」) — `CAMP_LANE_TOP` 을 0.18 → **−0.26** 으로 내려 격자로 옮기는 기울기만 두 배로 했다(전장 좌표는 그대로 · ⛔ 전장 y 를 음수로 늘리지 말 것 — 18-strike 길찾기 셀이 음수를 못 받는다) · 표(`CAMP_DG` gy)는 그 격자 값이라 TOP 을 바꾸면 표도 옮긴다(gy′ = −0.26 + (gy − 0.18) × 2) · 👁 **던전에 들어가면 뷰가 위 끝(적 기지 전체)** 이고 아래로 끌면 내 기지가 나온다(`campFoeLookAt` · `campFoeBase` 끝에서 — ⚠ `campEnterDungeon` 때는 `CAMPB` 가 아직 없다 · **목표 뷰 t 만** 옮긴다: v 를 직접 쓰면 바닥·광맥·그림자가 옛 자리에 남는다 — 실측) · 위 한계는 `campViewTop()`(표의 맨 위 건물 − 0.18 · 상단바 몫 포함)이고 캠프(0)에서는 격자 위끝 그대로 — ⚠ 캠프는 `_techClampView` 를 **덮어쓴다**(19-camp 6858) — 거기의 yLo 가 진짜 경로다(17-build-cards 를 고쳐 봐야 캠프엔 안 걸린다 · 실측) · 🏕 원정이 끝나 캠프로 오면 `campZoom()` 으로 시점도 집으로(안 하면 보간이 남아 프레임 제한이 안 걸린다) · 🏗 3D 는 격자 위라 행이 음수 → z 가 −1000 아래로 내려가 카메라 범위(−1200)를 벗어난다 — `zOf` 를 −1190 에서 받는다(실측: 3D 가 통째로 안 보였다) · ⚠ **전장 유닛도 같다**(`campBattleList` 의 z 를 −1185 에서 받는다 — 2026-09-11 사용자: 「안개 구역에 들어가면 아군·적군 다 안 보여」 · gy < 0.095 부터 사라지고 체력 바만 남았다) · 🖼 **바닥 그림은 격자 위 0.58 까지 깐다**(2026-09-10 · `css/30-home.css` `.bmapFloor::before{top:var(--floorTop)}` · 값은 `campSkin` 의 **`CAMP_BG_V2`** 표가 정한다 — §17 그림이면 −58%, 옛 §11 그림은 −18%: ⛔ 옛 그림을 −58% 로 깔면 홈 캠프 석판이 1.34배 커져 본부가 타일 한 장 위에 선다(실측) · **새 그림을 갈아 끼우면 그 던전 번호를 true 로** — ⛔ 바닥 요소에 바로 배경을 주면 격자 위가 검게 남고, 요소를 늘리면 뷰 변환이 어긋난다 · 실측) · 그림 규격은 ART.md §17(9:16 으로 뽑아 `scripts/map-crop.mjs` 로 1:2 크롭 · 위 8~27% 가 적 고원 = gy −0.45~−0.15) · 📐 **`campG2W`/`campW2G` 는 레인 위(t<0)를 전장 y 0 까지 허용한다**(`CAMP_LANE_TMIN`) — ⛔ 0 에서 자르면 −0.26 위의 줄이 전부 한 줄로 무너져 본진·테크·탑이 겹쳐 선다(2026-09-10 실측 · 스모크가 표↔전장을 대조한다) · 🎲 **배치는 생성기가 뽑는다**(2026-09-09 사용자: 「항상 정해져 있지 않고 … 랜덤」 · `campFoeLayout(d, seed)`) — 규칙은 고정이고 자리만 씨앗이다: **맨 위 광맥 호 → 본진 → 테크 → 앞줄 생산**(내 기지의 거울 · 줄은 `CAMP_FOE_ROW` · gy −0.43~+0.02 · 2026-09-10 사용자 두 차례: 「고원에는 높은 건물, 아래 통로에 펼쳐서 · 화면 가운데보다 조금 아래까지만」 → 본진·테크·탑③ 은 **고원(실측 −0.45~−0.14)** 안에, 탑②·생산·탑① 은 통로에 0.08 간격 · **광맥도 고원 안**(−0.43 · 호가 0.018 위로 부푼다) · 본진은 광맥과 0.05(내 기지 본부↔광맥 0.053 의 거울) · 3D 크기는 **내 건물과 같다**(`CAMP_FOE_SCL` 1 — 0.85 배는 사용자가 물렸다)) · 탑은 구간 앞 가운데 · 보급고류는 **탑 줄의 좌우 0.19/0.81**(가로 흔들림 없음) · 기지 전체 좌우 반전 · 씨앗은 원정마다 새로(`C.foeSeed` · `campEnterDungeon`) — ⛔ Math.random 금지(저장·복원하면 자리가 바뀐다) · ⛔ 표(`CAMP_DG`)에 gx·gy 를 되살리지 말 것 · 📏 같은 열에 서는 쌍은 **줄 차이 − 흔들림 합 ≥ 0.07**(발판) — 본진·탑·보급고는 세로 흔들림이 없고 옆 열은 0.005 다(씨앗 12,000개로 잡은 규칙 · 스모크가 900판을 잰다) · 👁 진입 줌은 **세로·가로 둘 다** 맞춘다(`campFoeLookAt` — 세로만 맞추면 바깥 보급고가 화면 밖) · ⛏ **적 광맥·가스·일꾼은 연출**(사용자 확정 「연출만」 · `CAMPB._fmine/_fgas` · `campFoeWorkers3D` — 게임 값에 안 들어간다 · ⛔ `G.tech.minerals` 에 넣지 말 것: 내 일꾼이 캐러 간다) · 🔍 던전 안 축소 하한은 1.0(`campMinZoom` · 캠프는 1.45 그대로) · 🎨 **적 건물·일꾼은 붉다**(2026-09-10 사용자) — 색은 **오토배틀의 적 진영 색**(`PLAYER_VIEW_COLORS[1]` · 18-strike 가 `u.pcol` 로 쓰는 그 값)이고, 칠하는 일은 3D 공용 **인스턴스 틴트 `applyTeamTint`**(직스가 쓰던 것)가 한다 — 엔트리에 **`rimCol`** 을 얹으면 `syncBuild` 가 유닛 경로와 같은 함수를 부른다(⛔ 건물용 틴트를 새로 만들지 말 것 · ⛔ 새 빨강을 정하지 말 것) · 🩹 시트는 서명이 아니라 **그려진 모델**(`_cgModel.foeEid`)을 본다 — 요약판이 서명을 안 지우고 덮은 적이 있다(실측 3회 중 1회) · 🗺 맵 띠 오른쪽 `.cbTgt` 「다음 **이름**」 · 👆 **적 건물을 누르면 「들여다보기」**(`CAMPB._foeSel` · 세션 값)이고 시트에 프로필(공용 `renderCmdGrid` · 「공격 대상」 카드 하나)이 뜬다 — **표적 지정은 그 카드**(`campFoeTgtSet` → `C.foeTgt`)가 한다 · ⛔ 탭에서 바로 foeTgt 를 박지 말 것: 잠긴 건물을 누르면 아무 일도 안 일어나 「안 눌린다」로 느껴진다(실측) · 안개에 가린 것은 안 눌린다 · ⊘ 해제(`techDeselU`)가 들여다보기도 닫는다 · 표식은 **한 층(`.fbLayer`)으로 갈아 끼운다** — 덧붙이기만 하면 프레임마다 쌓인다(실측 12채 → 144개) |
| 대기실 종족 선택 | `#lbRaceSec` + `segNavHTML(STK_RACE_ORDER)` → `setLobbyRace()` | 입구는 이 띠 **하나뿐** · 슬롯 칩(`.lbRace`)은 읽기 전용 · 종족 없는 유즈맵은 `.lbRaceLk` 안내로 대체 |
| 난이도 고르기(선택 화면·방 만들기) | `.sdStepRow`/`.sdStepTx`/`.sdDots` + 상세 판 + `.sdInf`(무한 모드) | 개인 플레이 난이도 선택(`#soloDiffPanel`)이 원본 · 방 만들기가 그대로 빌린다 · ⛔ 화면별 난이도 UI 를 새로 만들지 말 것 |
| **무장 칸(계열 연구)** | `CAMP_ARM_TREE`(20-camp-research.js) — 계열마다 **네 축**: 공격력 `atk` · 공격속도 `as` · 체력 `def` · 방어력 `dr` | ⚠ `def` 는 표 이름이 「…방어력」이지만 캠프에서 하는 일은 **체력**이다(HUNT_R1 §3-4) — 화면 라벨만 「체력」으로 덮는다 · ⚔ `as`·`dr` 은 **캠프 전용 연구**라 `CAMP_ARM_ADD` 가 `TECH_TREE` 에 주입한다(⛔ 15-tech-data.js 를 직접 고치지 말 것 — 관리자 건설과 공유) · 노출은 `_techResList`(16-build.js)가 `camp` 플래그로 거른다 · 🛡 방어력은 **받는 피해 −1.5%/레벨(최대 −60%)** 이고 구현은 **체력 배수 환산**이다(엔진 armor 는 감산이라 공격력 1짜리가 무력화된다 — HUNT_R1 §3-1) · 배선은 `UNIT_UPG`(11-cmdcard.js) → `campResLv` → `campScaleAllies` 셋이 이어져야 한다(스모크가 넷 다 잰다) · ⏫ **한 번에 여러 레벨**(×1 → ×5 → MAX)은 **자원 칸과 무장 칸이 함께 쓴다**(2026-09-03 · ⛔ 자원용 배수를 따로 두지 말 것 — 상태는 `_armMul` 하나 · 자원은 **미네랄**로, 무장은 **가스**로 MAX 를 잰다 · 자원의 뒷단은 `campUpgDry`/`campUpgAfford`/`campUpgBuyN`(19-camp.js)이고 비용은 **campUpgCost 를 레벨마다 다시 물어본다** — ⛔ 식을 새로 쓰지 말 것) — 트레이 오른쪽 위의 **칸 하나를 눌러 돌린다**(`m.topRight` · 껍데기는 **글리프 버튼 `.cgGly`**(옅은 면 · ⛔ **`onclick` 으로 달지 말 것** — 트레이도 칸과 **같은 위임**을 타야 손가락에서 눌린다: `campResRoot` 의 `pointerdown`/`pointerup` 쌍 + `data-armmul`/`data-armback`) — 판·테두리 없이 아주 옅은 면만(2026-09-03 · 목업 camp-arm-tray-thin-8 ④안 · ⛔ 붉은색을 넣지 말 것: 그건 칸의 신호다) · 순서는 **[배수][뒤로]** 이고 둘 다 `m.topRight` 한 칸에 담는다(`m.back` 을 쓰면 왼쪽으로 간다 — 공유 코드라 순서를 못 바꾼다) · ⛔ 버튼 셋으로 되돌리지 말 것 — 트레이는 판 밖이라 칸이 늘수록 전장을 가린다)이고 `campArmMulN` → `techDoResearch(bk, rk, n)` → `rj.n` 으로 간다 — ~~클릭만 줄이고 시간은 안 줄인다~~ → ⚡ **캠프의 연구는 즉시 끝난다**(2026-09-11 사용자: 「업그레이드할 때 즉시 되도록」 · `CAMP_RES_INSTANT` · 16-build `_techResearchTime` 이 캠프일 때만 0 — 관리자 건설·오토배틀은 그대로 시간이 든다) · 🔁 **연구가 끝나면 이미 선 병력에도 다시 얹는다**(`campOnResearch` → `campRescaleAllies` · `techApplyResearch` 의 훅 · 설계 능력치부터 다시 쌓되 **체력·실드 비율은 지킨다** — 맞던 유닛이 연구로 풀피가 되면 안 된다) · 📊 **프로필은 지금 값을 보인다**(`_techUnitStatList` 가 전장 유닛 `_fu` 의 dmg·rng·cdMax 를 읽는다 · 머리줄 체력도 `campFieldEnts` 가 실어 보낸다 · 연구가 끝나면 시트 서명을 지워 다음 프레임에 새로 그린다) · 값은 레벨마다 다르므로 **한 칸씩 더해** 합친다 · MAX 는 지금 가진 가스로 살 수 있는 만큼(`campArmAfford` · 상한 `CAMP_ARM_MAX_STEP`) · 아이콘은 `CAMP_ARM_ICO`(공격속도 `up_atkspd` · 방어력 `up_carapace` 공용 — ⛔ `up_speed` 는 **부츠**다) · ⚠ **건물 프로필의 연구 카드는 별칭 표 `UPG_ICO`(15-tech-data)를 지난다** — 캠프 전용 키(inf_as·inf_dr …)를 거기에도 적어야 그림이 나온다(2026-09-11 공학소에 빨간 폴백이 떴다) · 🏷 공학소 카드의 def 축 이름도 「체력」으로 덮는다(`_techResearchCard` · 안 덮으면 「보병 방어력」이 둘) |
| 오토배틀 대전 설정 | `STK_OPTS`(상하한 표) + `STK_PRESETS` + `renderCpMode()` | 상하한·기본값은 표 한 곳에서만 · 엔진 반영은 `MAP_CFG_OVR` → `mapCfg` 한 입구(시작 때 심고 로비 복귀 때 반납) |
| 목록 고르는 판(방 찾기·파티 찾기) | `.rmCard` + `.rmHead`/`.rmNum`/`.rmList`+`.roomItem`/`.rmBtns` | 방 찾기(`#rooms`)가 원본 · 파티 찾기는 이 컴포넌트를 그대로 빌린다(딤만 `.pfOv`) · **새 목록 판을 만들지 말 것** · 행 밑변 광원 = `--dc`(난이도 색, 없으면 중립) · 하단은 `.actBtn`(주 동작 길게 + `.sq` 38px 둘) |
| **캠프 단계·라운드 표시** | `#curTitle` 칩(`curPaintChip()` · `js/12-appshell.js`) | **한 줄 · 가운뎃점**(`잊혀진 회랑 · 30/50` + ⌄ · 2026-09-03 사용자 확정 · 목업 `docs/mock/camp-chip-cmd-8.html` 5안) — ⛔ **판(면·테두리)·왼쪽 광원 띠·청록(--hud)을 되돌리지 말 것**: 좌상단만 색이 갈려 하단 구역과 안 어울리던 것이 이유다 · 어휘는 하단 커맨드 카드에서 왔다(흰 글자 · Rajdhani 숫자 · 진행선은 비용색 `#ff7676` · **박스 밖** 음수 bottom — 안쪽 패딩으로 두면 글자가 재화 줄보다 위로 뜬다) · 캠프(0단계)도 같은 자리를 쓴다(`캠프 · 0/50` · 상한은 라운드 50 · 아이콘 없음) · 라벨은 화면에서 뺐고 `aria-label` 에만 남는다 · 폭 예산 110px(스모크) · 🖥 **칩을 누르면 던전 선택이 전체 화면으로 열린다**(`#campDrop` · 2026-09-03 사용자 확정 · 목업 `docs/mock/camp-dgpick-full-8.html` 1안 — 그 전에 칩 아래 드롭다운 3벌(`camp-dgdrop-redo/harmony/layout-8`)을 거쳤다: 청록 판 → 검정 판(너무 진함) → 그늘 위 글자(좁음) 순으로 버려졌다): `#phone` 직속 · z 61(재화 바 62 **아래**라 칩이 위에 남아 머리줄이자 닫는 손잡이다 — 다시 누르면 닫힘) · `bottom:var(--navH)` 로 네비 자리를 비운다 · 위는 던전 **카드** 목록(큰 번호 · 이름/부제 · 배수 · 「지금 여기」= 붉은 테두리+붉은 번호 · 지나온 곳은 번호 옅게) · 아래 고정 `.cdFoot`: ROUND **큰 숫자 + 공용 `.arwBtn` ◀▶(누르고 있으면 반복 · pointerdown) + 🎚 슬라이더(`.cdSld` · 붉은 채움 + 흰 손잡이 · 누르는 높이 28px · 위치 (r-1)/(max-1) · pointer capture)** · [이동]은 **공용 `.actBtn.pri` 44px** — 값 입구는 `campRndTap` 하나(◀▶·슬라이더 둘 다) · ⛔ 청록·발광·굴림 피커(`.cdRn`)·제 버튼·칩 아래 작은 판을 되돌리지 말 것(스모크가 전부 잰다) · ⛔ 맵 띠(`#campBar`)에 다시 두지 말 것 — 거긴 **적 수와 ⚡ 피버만** — 🚪 화면 입구는 두지 않는다(트리 2026-09-01 · 환생 2026-09-03 제거 · 둘 다 하단 네비에 제 칸이 있다) · 상태는 `campDgN()`/`campRoundN()` 이 단일 소스(칩은 읽기만) |
| **구역 상단 띠**(환생·룬·유즈맵·상점) | **`curSplit(on)`** (12-appshell.js) → `.curBar.split` | 📐 네 화면이 **같은 얼굴**이다(2026-09-05 사용자 확정 · 목업 `docs/mock/top-fill-8.html` ③안) — 위에서 **그늘**이 내려오고 아래에서 **빛**이 올라오며 맨 아래는 **밑변 광원 한 줄**이라 상단이 본문과 완전히 갈린다 · ⛔ 캠프에는 걸지 않는다(좌상단이 던전 칩이고 배경이 밝은 돌이라 규칙이 다르다 — `navShow('camp')` 가 끈다) · ⚠ 켜고 끄는 판단은 **`curSplitSync()` 한 곳**이 한다 — 「구역 화면(`#campRune`/`#campReb`/`#campTree`)이 열려 있나」를 **DOM 에서 조회**하고, 아니면 화면 규칙(`SPLIT_CUR_SCREENS`)에 맡긴다 · ⛔ 열기·닫기마다 켜고 끄지 말 것 — 여는 함수가 안에서 닫기를 부르므로 순서에 흔들린다(2026-09-05에 세 번 겪었다: 환생만 안 켜짐 → 캠프에 남음 → 다시 안 켜짐) · ⚠ `campRuneIsOn()` 류를 판단에 쓰지 말 것 — 닫은 뒤에도 참을 주는 때가 있다 · ⚠ 캠프로 돌아가는 길(`navShow('camp')`)은 `showAppScreen` 을 안 타므로 `curSplitSync(false)` 로 **화면 쪽 판단까지** 내려 준다 · ⚠ `curSplit(true)` 는 **바도 함께 켠다**(`curShow`) — 환생·룬은 `CUR_SCREENS` 에 없어 아무도 안 켜 준다 · ⚠ 캠프의 「내려오는 그늘」(`#phone.campMode .curBar.bare::after` 56px)이 **더 구체적**이라 그냥 `.split::after` 로는 진다 — 그 조합까지 함께 적는다(⛔ !important 금지) |
| **캠프 구역 공통 상단** | **공용 재화 바 `#curBar`** — 환생·업그레이드·룬 세 화면 위에 그대로 올린다(`#phone.artLift .curBar{z-index:121}`) | 새 바를 만들지 말 것(단일 소스) · 🏷 **화면 이름은 재화 바 왼쪽(`#curTitle`)** 이다 — 유즈맵 선택·상점과 같은 자리 · 이름은 `campZoneTitle()` 한 곳이 정하고(환생 / 환생 트리 / 룬 / 룬 상점) `curPaintChip()` 이 던전 칩 대신 그것을 쓴다 · ⛔ 화면 안에 제목을 또 두지 말 것(`.rnTitle`·`.ctTitle`·`.crTitle` 은 감춰 뒀다 — 요소는 공용 제목 규격 계약이 잰다) · ⚙ 설정 버튼은 구역에서 **톱니**다(`#phone.campMode.artLift` — 캠프 맵의 ☰ 와 얼굴을 가른다) · 🧍 **인구 칸**은 캠프 밖에서도 보인다 — 화면 목록은 `POP_SCREENS`(12-appshell.js) 한 곳 · 세 화면의 상단 띠는 **위 44px**(`--topPad` + `--curH`)을 비워 재화 바와 안 겹치게 한다 |
| **환생 구역** | **`campRebEnter(sec)` 가 유일한 입구** — `'info'`=`#campReb` · `'tree'`=`#campTree` · `'umap'`=`#mapUpgScreen`(🗺 유즈맵 강화 · 2026-09-10) · 내용은 `campRebRender()`/`campTreeRender()`/`renderMapUpg()` | `#phone` 직속(z-index 120) · **하단 네비가 「환생 · 환생 트리」 두 칸으로 갈린다**(2026-08-31 · 두 번째 칸 이름은 2026-09-04 에 「업그레이드」에서 바뀌었다 — 그 말은 캠프의 자원·무장 연구를 가리키는 다른 이름이다) · 열릴 때 var(--t-screen) 페이드인(⛔ animation-fill-mode 를 주지 말 것 — 탭이 백그라운드면 애니가 멈춰 화면이 통째로 안 보인다) · ⛔ `campRebOpen`/`campTreeOpen` 을 직접 부르지 말 것 — 서로를 안 닫아 둘 다 열린다(캠프 맵 띠 🔁·🌳 칩도 `campRebEnter` 로 온다) · **네비를 가리지 않는다**: 두 화면은 `bottom:var(--navH)` 로 자리를 비우고, 키 아트는 `#phone.artLift .navBar{z-index:121}` 로 네비를 그 위에 올린다(⛔ 키 아트를 `bottom` 으로 자르는 것으로는 안 된다 — 호흡 애니가 8px 더 그린다) · 실행 확인은 `.ecCard` 공용 확인창 · 배경은 **환생 구역 전용 한 장**이다(`#campRebBg` · `assets/backgrounds/reb/reb.webp` · ART.md §14) — 두 화면이 그 한 층을 함께 켠다(⛔ 화면마다 제 그림을 그리지 말 것 · ⛔ 공용 키 아트 `#titleBg` 를 빌리던 옛 방식으로 되돌리지 말 것 — 로그인·부팅과 서로 간섭한다 · 스모크가 잡는다) · 아래 넷은 빼지 말 것: ⭐ **히어로 = 환생 뒤 획득 배수**(`.crBig` · 「먼 목표」의 자리 — 없으면 첫 환생을 손해로 판단한다 · HUNT_R1 §4-2-0) · **포인트 계산 근거**(`.crFx` · 재화×던전×라운드 세 값) · **이번 회차 지표 다섯 줄**(`.crLi` · 터치 / 터치로 번 미네랄 / 자동으로 번 미네랄 / 가스 / 플레이 시간) · 💳 **환생 팩 버튼**(`.crPk` · **버튼 구역**이되 위계는 「환생」보다 한 단 아래 — 높이 38 vs 50 · 색은 보라(현질) · 산 뒤엔 `.on` = 「적용 중」 **상태 표시**라 버튼이 아니다 · ⛔ 젬 1회권으로 되돌리지 말 것 — 결제 팩 영구 ×2 다 · GEM.md §4-1) |
| **룬 구역** | **`campRuneEnter(sec)` 가 유일한 입구** — `'slot'`=장착 · `'shop'`=룬 상점 · 화면 `#campRune` · 내용은 `campRuneRender()` | 하단 네비 **세 번째 칸**(연구·환생·**룬**·유즈맵·상점 · 2026-09-02) · 환생 화면과 **같은 규격**(`#phone` 직속 · z-index 120 · `bottom:var(--navH)` 로 네비 자리를 비운다) · ⛔ `campRuneOpen` 을 직접 부르지 말 것(네비 하위가 안 나온다) · 🖼 배경은 **환생 구역과 같은 그림**이다(`#campRebBg` 한 장을 세 화면이 나눠 쓴다 · 2026-09-03 사용자 확정 — ⛔ 룬만의 그림을 따로 두지 말 것: 구역을 오갈 때 배경이 바뀌면 번쩍인다 · 끄기는 **한 박자 뒤**라야 「닫고→연다」 사이에 안 꺼진다) · 🗺 상단(`.rnTop`)은 **판 위에 떠 있다** — 판이 화면 맨 위까지 차오르고 진행 수치도 제목 옆(`.rnRound`)에 합쳤다(⛔ 판 위에 머리줄을 다시 띄우지 말 것) · 🌌 **구역은 오로라 + 이름**이다(2026-09-04 사용자 확정 · 목업 `docs/mock/camp-rune-zone-8.html` ②안) — 무리마다 갈래 색 번짐(`.rnAu`)과 이름(`.rnZn`) · ⚠ 둘 다 `#rnG` 안이라 판과 **같이 밀리고 확대된다**(⛔ 화면에 고정하지 말 것) · ⭐ **환생 트리의 성운과 같은 문법**이다 — **아주 넓은 타원(250×215) + 아주 옅은 세기(.22/.07)**(2026-09-04 사용자 확정: 「트리 배경처럼 은은한 빛」) · ⛔ 반경을 줄이며 세기를 올리지 말 것 — 경계가 보여 「빛」이 아니라 **동그란 얼룩**이 된다(첫 시도 r132·.50 이 그랬다) · ⛔ blur 필터 금지(트리에서 팬·줌 38→23 프레임) · ⚠ 이름은 칸 바깥에 **바싹** 붙인다(`RUNE_ZONE_DY`) — 더 띄우면 `_runeBox()` 범위 밖으로 나가 전체 보기에서 잘린다 · 🔺 성좌는 **작은 정삼각**(위·왼쪽·오른쪽 = 경제·전투·성장 · 반지름 RUNE_TRI 하나가 자리를 낸다 — ⛔ 각도 순서를 바꾸면 좌우가 뒤집힌다) · 🎨 **빈 칸 테두리는 갈래 색**이다(2026-09-04 · 목업 `docs/mock/camp-rune-edge-8.html` ③안) — 낀 칸과 **같은 어휘**(위 흰빛 → 아래 갈래 색 · `#rnEg<갈래>`)이고 일반 칸은 그 성좌의 갈래, 가운데 유니크 칸은 **보라** · ⚠ 세기는 낀 칸보다 **약하다**(흰빛 .92 → .34) — ⛔ 올리지 말 것: 빈 칸이 더 시끄러우면 끼웠을 때 달라지는 것이 없다(스모크가 잰다) · 🕳 **빈 칸은 파인 홈**이다(2026-09-04 · 목업 `docs/mock/camp-rune-slot-8.html` ②안) — 위가 어둡고 아래가 밝은 면(`#rnWell`) + 안쪽 얇은 흰 선 · ⛔ 「+」·숨 원(`.rnEmB`)을 되살리지 말 것 · ⚠ **빈 칸·잠긴 칸의 반투명은 얕게**(빈 .86 · 잠김 .68 · 2026-09-04 사용자 확정: 「반투명을 원해, 근데 그게 과했다」) — 옛 값(.72/.40)은 뒤의 구역 오로라가 그대로 들어와 칸이 갈래 색으로 물들었고, 불투명은 판때기가 됐다 · ⛔ 하한 아래로 내리지 말 것(스모크가 범위를 잰다) · 🃏 **낀 칸은 카드 그림 한 장이다**(2026-09-12 사용자 확정 「직접 만든 디자인 말고 실제로 이미지로 만든 카드가 들어갔으면」) — **칸 = 가방 = 상점**이 전부 `runeIcoSrc` 를 쓴다(⛔ 두 번째 그림 경로 금지) · 🚨 **2026-09-04 의 「칸은 도형이다」를 뒤집은 것**이다 — 그때 물린 이유(「판까지 합친 그림은 배경과 상호작용이 없어 **스티커처럼 얹힌다**」)는 카드에 **등급색 번짐**(drop-shadow)을 걸어 풀었다 · ⛔ 번짐을 빼지 말 것(그러면 그때 그 문제로 돌아간다) · ⛔ **손으로 그린 껍데기를 카드 위에 다시 얹지 말 것**(바깥 링 · 면 그라데이션 · 뒷광 · 안쪽 흰 실선 · 꼭짓점 점 — 카드에 이미 있어 테가 둘이 된다 · 전부 다락으로 갔고 되살리면 다락 래칫이 잡는다 · ATTIC.md §5-E) · 📐 크기의 단일 소스는 **`RUNE_CARD_K`**(칸 지름 × 0.93 = 옛 육각 면과 같다 — 2.0 으로 올리면 이웃과 맞닿고, 1.0 쯤으로 줄이면 「칸 속의 작은 카드」가 된다 · 둘 다 찍어 견줬다) · 칸이 바깥으로 뻗는 거리는 **`RUNE_CELL_OUT`** 하나가 말한다(전체 보기·팬 경계·이웃 간섭이 그걸 읽는다) · ⚠ **이펙트·애니메이션은 그대로다** — 가림(`.veil`)·날아오기·부풀림(`.rnPop`)·고리(`.rnRipple`)·교체 대기(`.rnCand`/`.rnAnts`)·물림(`.rnDim`)·선택 링이 모두 카드 위에서 돈다(그림의 클래스가 `rnImg` 그대로라 배선이 안 바뀌었다) · ⚠ 글리프 에셋(`assets/icons/rune/glyph/`)은 **카드를 굽는 재료**라 남겨 뒀다(`scripts/rune-compose.mjs`) — 앱은 더 이상 안 읽는다 · ⛔ 점선 후광을 되살리지 말 것: 옆 칸을 밟는다) · 🔍 최대 축소 = 전체 보기(RUNE_ZLIM.out=1) · 🚧 팬 경계는 공용 엔진 svvClampPan(**무리의 가장자리와 화면 가장자리를 직접 잇는다** — SVV_SLACK 만큼만 밖으로 나간다 · ⛔ 「가운데에서 잰 여백」식으로 되돌리지 말 것: 아무리 조여도 한 화면 폭씩 밀렸다) · 📊 **지금 걸려 있는 효과는 오른쪽 위에 합쳐서 나열한다**(`.rnSum` · `campRuneEffList` · 2026-09-04) — 같은 효과를 여러 칸에 끼웠으면 **한 줄**이고, 값은 **`campRuneEff` 한 곳**에서 가져온다(⛔ 여기서 다시 더하지 말 것 — 그래야 구매 비용 감소의 뚜껑도 따라온다) · 최대 12줄이고 나머지는 「외 n가지」로 접는다 · ⚠ `.rnTop` **안에 넣지 말 것** — 그 높이가 hideT 로 잡혀 성좌가 아래로 밀린다 · ⚠ `pointer-events:none`(판을 밀 때 손가락을 가로채면 안 된다) · 🗺 상단 진행 수치는 없앴다(칸마다 「Lv.26」이 이미 말한다) · 칸은 **이중 테두리**(⛔ 십자 반짝임·후광 원은 트리의 어휘다 — 되살리지 말 것) · 성좌 중심에서 뻗는 선은 **칸 밖에서 멈춘다** · 🎚 **유니크는 등급이다**(2026-09-05 · 하급 0.5% · 중급 1% · 상급 1.5% · **유니크 5%**) — ⛔ `kind:'uniq'` 로 되돌리지 말 것(룬 종류가 아니다) · 칸 무리를 정하는 것도 **등급**이고(`runeBucket`) 갈래는 성좌가 정한다(`runeSlotGrp`) · 💠 **유니크 칸은 갈래를 안 가린다 — 어느 갈래의 유니크 룬이든 들어간다**(2026-09-12 사용자 확정 「그 구역 상관없이 다 낄 수 있게」): 유니크는 **종류 4 > 칸 3** 인 자리라 갈래까지 묶으면 전투 유니크는 Lv.30 · 성장은 Lv.50 까지 낄 데가 없다 · ⭐ 판정은 **`runeSlotTakesGrp(kind, i, grp)` 하나**다(⛔ 장착·교체·가방이 저마다 다시 비교하지 말 것 — 실제로 어긋나 있었다: 장착은 통과시키는데 **교체 후보가 막아서** 칸이 다 차면 바꿀 수가 없었다) · 🎨 그래서 빈 유니크 칸의 테두리는 **제 등급 색**이다(성좌 색이면 「여기는 경제 칸」이라고 거짓말한다) · 🎒 가방도 **안 좁힌다** · 🔁 교체 때 화면도 **전체 보기 그대로**다(칸 셋이 세 성좌에 흩어져 있다) · ⛔ 넷 다 되돌리지 말 것(스모크가 잰다) · 룬 **21종**(경제 7 · 전투 7 · 성장 7) · 🛡 **방벽의 룬**은 최대 체력의 n% 를 **실드로 더한다**(2026-09-08 사용자 요청) — ⛔ 「피해 감소 %」나 체력 배수로 바꾸지 말 것: 그러면 수호의 룬과 글자만 다른 룬이 된다(실드는 `strikeHit` 에서 **상성을 무시**한다 — 그게 다른 축인 이유다) · ⚠ 원래 실드가 있는 유닛에는 **더한다** · 📉 **감소형 셋**(스킬 쿨·미네랄 비용·가스 비용)은 뚜껑 **50%**(`RUNE_CUT_CAP`) — 합산이라 100% 를 넘으면 부호가 뒤집힌다 · 칸은 🏆 **통산 최고 레벨**이 연다(2026-09-11 · `campRuneBestLv()` → `campBestLevel()` · 표 `RUNE_SLOT_LV` · 젬으로는 못 연다) · 🎚 **여는 레벨: 일반 Lv.5~49 · 유니크 Lv.15/30/50**(2026-09-12 사용자 확정 「5레벨부터 하나씩 · 50레벨이면 전부」) — ⭐ 스물일곱 개를 손으로 찍지 않는다: **시기 셋의 「첫 칸~마지막 칸」**(`RUNE_LV_PHASE` · ⚠ 성좌가 아니라 **초·중·후반**이다)과 유니크 셋(`RUNE_LV_UNIQ`)만 적는다(⛔ 생성된 배열을 리터럴로 되돌리지 말 것) · 🔁 **갈래를 번갈아 연다 — 경제 → 전투 → 성장 → 경제 …**(2026-09-12 사용자 지적 「성좌 1,2,3 으로 가면 내가 원하는 구역이 너무 늦게 열린다」): 칸은 **제 성좌의 갈래만** 받으므로(`runeCellGrp`) 성좌를 통째로 먼저 열면 **세 번째 갈래의 룬이 Lv.31 까지 낄 데가 없다**(젬으로 산 룬이 가방에 묶인다) — 지금은 경제 5·9·13·18·23·29·36·44 / 전투 6·10·14·20·25·31·39·46 / 성장 8·11·16·22·27·34·41·49 이고 **Lv.8 이면 세 갈래가 다 열린다** · ⛔ 성좌를 통째로 먼저 열지 말 것(스모크가 순환을 글자 그대로 잰다) · ⚠ **옛 규칙 「성좌를 다 열면 그 한가운데가 열린다」는 버렸다** — 번갈아 열면 성좌 셋이 같이 차서 유니크가 Lv.44~49 에 몰린다. 유니크는 **15·30·50 고정**이다 · ⛔ 되돌리지 말 것 · ⚠ 유니크 칸은 **갈래를 안 가리므로**(아래) 15/30/50 이 단순히 「유니크 칸이 하나씩 늘어난다」는 뜻이다 · ⚠ **Lv.5 이전에는 한 칸도 안 열린다**(옛 표는 첫 칸이 Lv.1 이었다) — Lv.5 는 첫 던전 관문 셋이면 닿는다(실측 73초)이고 잠긴 칸에 여는 레벨이 적혀 있다 · ⚠ **깊이는 안 쟀고 계산만 했다**: 한 회차(D1→D3) = Lv.17 이라 첫 완주에 10칸이고, 무한층이 한 층마다 경험치를 ×7.17 로 올리므로 Lv.30 ≈ **무한 3층** · Lv.50 ≈ **무한 7~8층**이다 · ⚠ 레벨은 **한 회차 안에서** 올려야 한다(환생하면 되감긴다 · 남는 것은 `C.lvBest` 뿐) · 🔧 **`CAMP_RUNE_DEV_SEED` 가 켜져 있으면 칸이 전부 열린 것으로 친다** — 그 상태로 해금 속도를 확인하려 하지 말 것 · ⚠ 열린 칸 수를 묻는 단일 소스는 **`campRuneSlotsAt(kind, lv)`**(레벨을 받는다)이고 `campRuneSlots(kind)` 가 그것을 지금 최고 레벨로 부른다 — ⚠ 자는 **되감기지 않는 `C.lvBest`** 다: 레벨 자체는 환생하면 1 로 돌아가는데 💠 룬은 젬으로 산 물건이라 한 번 열린 칸이 닫히면 결제가 사라진다(⛔ `campLevel()` 로 열지 말 것 · ⛔ `campRunReset` 이 `lvBest` 를 지우지 말 것 · `campRebirth`·`campTutoReset` 의 keep 목록에도 있어야 한다) · ⛔ 옛 자(라운드 · 통산 관문)로 되돌리지 말 것 — 관문은 던전 셋 = 18 이 끝이라 한 바퀴에 27칸이 다 열린다 · ⚠ **문턱은 안 쟀다**(실측 「던전 3 완주 = Lv.18」 하나에 기대어 첫 완주 18칸 · 나머지 9칸은 ~Lv.27) · 효과는 **`campRuneEff(key)` 합산** 하나뿐(⛔ 곱 금지 · GEM.md §5-2) · 산 룬은 **환생해도 남는다**(`C.rune`) · 화면은 **🌌 성좌 판**(2026-09-03 · `_runeMapSvg` SVG 한 장 · 유니크가 중심, 일반 8칸이 고리 · 칸을 고르면 그 성좌로 확대해 들어간다) · 판은 **밀고 확대한다** — 조작은 **공용 엔진 `svv*`**(19-camp.js · ARCHITECTURE §「공용 SVG 뷰」)가 맡는다(⛔ 팬·줌을 화면마다 새로 짜지 말 것) · ⛔ 가방 머리줄에 「누르면 빈 칸에 끼웁니다」를 되돌리지 말 것(2026-09-04 — 한 번 배우면 자리만 차지한다 · 칸을 고른 상태의 안내는 남는다)  · 👆 **넣고 빼기는 한 번씩**(2026-09-04): 가방을 누르면 들어가고 **낀 칸을 누르면 바로 빠진다**(`campRuneSlotTap`) — ⛔ 낀 칸을 「고르기」로 되돌리지 말 것(빼려고 두 번 눌러야 한다) · 🗒 **칸을 길게 누르면 효과 쪽지**(`.rnTip` · `campRuneSlotHold`) — 확인창이 아니라 **읽는 쪽지**라 버튼이 없고 아무 데나 누르면 사라진다 · 길게 누르기는 **공용 엔진의 `onHold`**(19-camp.js `svvBind` · 420ms)가 준다(⛔ 화면마다 새로 짜지 말 것 · ⚠ 발동한 뒤의 탭은 삼킨다 — 안 삼키면 손을 뗄 때 룬이 같이 빠진다)  · ⚠ **가방 탭은 「그 룬이 들어갈 수 있는」 빈 칸을 찾는다**(`campRuneAuto`) — ⛔ 그냥 첫 빈 칸을 잡지 말 것: 갈래가 다른 성좌에서 걸려 장착이 실패하고 교체 모드로 빠진다(2026-09-04 실제 버그) · 🔇 칸 밖 아래의 **% 는 없다**(⛔ 되살리지 말 것 — 스물일곱 칸에 숫자가 붙으면 판이 시끄럽고, 값은 쪽지와 가방 줄이 말한다) · ) · ✈ **넣을 때는 늘 날아서 들어간다**(`campRuneEquipFly` 한 입구 · 2026-09-04) — ⭐ 연출은 넷이 한 벌이다(2026-09-04 「단순하고 끊기는 느낌」): ① 날아가는 동안 **받을 칸의 문양을 감춘다**(`.veil` — 도착해야 나타난다: ⛔ 감추지 않으면 그림이 도착해도 아무 일이 없어 보인다 · ⚠ 감춤은 **그릴 때부터** 걸어야 한다(`_runeVeil` 상태) — 다 그린 뒤에 클래스만 붙이면 문양이 1→0 으로 **페이드아웃**되어 누르는 순간 룬이 「생겼다 사라진다」) · ② 궤적은 **호**다(Web Animations · ⛔ 직선으로 되돌리지 말 것 · ⚠ 스모크는 **가로 진행률**로 잰다 — 시간으로 재면 easing 때문에 직선도 통과한다) · ③ 도착하면 칸이 **부풀고**(`.rnPop`) 등급 색 **고리가 퍼진다**(`.rnRipple`) · ④ 가방으로 돌아가면 그 **줄 버튼이 부푼다** · 교체는 **나가는 것이 먼저**(`RUNE_FLY_GAP` 110ms — 둘이 같이 날면 어느 것이 들어오는지 안 읽힌다) — 빈 칸이든 교체든 같다(⛔ 교체만 날아가게 두지 말 것: 두 동작이 다른 화면처럼 보인다) · 📜 **가방이 내려가 있던 자리를 지킨다**(`campRuneRender` 가 `.rnBagG` 의 scrollTop 을 안고 다시 그린다) — ⛔ 다시 그릴 때마다 맨 위로 올리지 말 것: 아래쪽 룬을 하나 넣을 때마다 다시 찾아 내려가야 한다 · 🔁 **칸이 꽉 찼을 때는 교체**다(2026-09-04 · `campRuneSwapBegin`/`campRuneSwapDo`): 가방을 누르면 그 갈래 성좌가 **보이는 자리의 한가운데**로 오고(⛔ 판 한가운데로 잡지 말 것 — 가방이 아래를 덮는다 · ⚠ 화면 좌표를 `svvToView` 로 바꿔 앵커로 쓴다: 화면 비율을 viewBox 값에 그대로 곱하면 35px 어긋난다), 바꿀 수 있는 칸만 **숨쉬고 둘레에 점선이 돈다**(`.rnCand` + `.rnAnts` · 2026-09-04 사용자 확정 · 목업 `docs/mock/camp-rune-wait2-4.html` ①안 — ⭐ 움직임을 칸 **안**(크기 1↔1.075)과 **밖**(둘레 점선)으로 나눠 서로 안 부딪히게 한다 · ⛔ 좌우로 떠는 흔들림으로 되돌리지 말 것: 게임에서 그 몸짓은 「고르라」가 아니라 「잘못됐다」다 · ⛔ 퍼지는 고리를 대기에 쓰지 말 것 — 도착(`.rnRipple`)이 이미 그 어휘이고, 실제 판에서 1.7배는 31.5 까지 번져 옆 칸(28.0)을 밟는다(실측) · ⚠ 점선은 칸 밖 `RUNE_ANTS_GAP`(3.2)까지만 — 이웃 칸 가장자리가 28.0 이다) 나머지는 물린다(`.rnDim`) — 일반 룬이면 유니크 칸이 잠긴다 · ⚠ 유니크 룬은 칸 셋이 **세 성좌에 흩어져** 있어 전체 보기로 둔다 · ✈ 있던 룬은 **가방으로**, 새 룬은 **칸으로** 날아간다(`.rnFly` · 320ms) — 상태는 **즉시** 바뀌고 그림은 그 위에 얹히는 장식이다(⛔ 애니가 끝날 때 상태를 바꾸지 말 것: 중간에 화면을 나가면 반영이 사라진다) · ⛔ 확인창을 띄우지 말 것· 🎯 **넣으면 다음 빈 칸으로 옮겨 간다**(`campRunePickNext` · 2026-09-04) — 가방을 연달아 누르면 그 갈래의 빈 칸이 차례로 채워진다 · ⛔ 고른 자리를 그대로 두지 말 것: 다음 탭이 방금 넣은 것을 **덮어쓴다** · ⚠ **갈래는 안 넘어간다** — 성좌가 바뀌면 화면이 멀리 뛰고 가방도 통째로 갈린다(다 차면 선택을 푼다) · 🔎 **빈 칸을 고르면 가방이 그 갈래만** 보여 준다(`grpSel`) — ⛔ 물리기(.off)만 하지 말 것: 못 끼우는 줄이 화면을 차지한다· 아래는 **상시 가방**(`.rnBag` · 프로필 장비창의 가방과 같은 나눔) — 누르면 **칸을 골라 뒀으면 그 칸에, 아니면 빈 칸 중 첫 칸에** 끼운다(`campRuneBagTap`/`campRuneAuto`) · 🎒 **가방은 줄 목록이다**(2026-09-04 사용자 확정 · 목업 `docs/mock/camp-rune-hexbtn7-4.html` ③안): 줄 하나 = 룬 한 종류 — [그림] [**효과 이름**(`def.de`) / 등급 값 `1% · 2.5% · 5%`] … [**육각 버튼 셋**(등급마다 하나 · 34px)] · 갈래 머리줄 넷(경제·전투·성장·유니크 · 성좌와 같은 색) · 줄 사이는 **구분선 한 줄**(설정 목록과 같은 어휘 — 면을 더하지 않는다) · 부제의 등급 값은 **그 등급의 색**이라 버튼 테두리와 이어 읽힌다(2026-09-04) · 버튼 안은 **보유 개수 `×N`**(13px)이고 못 가진 등급은 «–» 로 물린다 · 아래 아주 작은 %(5.5px)는 등급의 눈금이다 — ⚠ ×N·% 두 줄은 **한 덩어리로 세로 가운데**에 두고 1.2px 만 내린다(⛔ %를 `bottom` 에 붙이지 말 것: 육각 아래가 뾰족해 글자가 밖으로 나간다 — 스모크가 잰다) · 그라데이션은 **한 벌만**(`.rnBagDefs`) 두고 버튼 40개가 나눠 쓴다 · ⛔ 4열 카드 그리드(`.rnB`)로 되돌리지 말 것 — 같은 룬의 세 등급이 흩어져 「이 효과를 얼마나 갖고 있나」가 안 읽혔다 · ⛔ 줄 이름을 룬 이름(「윤회·손끝」)으로 되돌리지 말 것 — 무엇이 오르는지 못 읽는다 · ⛔ 칸을 눌러야 뜨는 임시 시트(`.rnPick`)로 되돌리지 말 것 — ⛔ 목록형(`_runeRowHTML`)으로 되돌리지 말 것(다락에 있다) · 칸은 **일반 24(8칸×성좌 3) · 유니크 3** 이고 **한 성좌를 다 열면 그 중심이 열린다**(표는 GEM.md §8-3) · **유니크만 종류(4)>칸(3)** 이고 일반은 칸이 더 많다 — 그래서 일반의 선택은 「어느 축에 몇 칸을 주나」다(⛔ 같은 룬의 중복 장착을 막지 말 것) · ⚠ **아직 닿는 데가 없는 룬은 그렇게 말한다**(`def.soon` → 상점 칸에 「준비 중」) — 젬을 받으면서 말을 안 하면 거짓 판매다 · 지금 **하나**다: **전과의 룬**(적 처치 보상) — 처치 보상 시스템이 아직 없다(⚠ 옛 「성장의 룬」(경험치)은 전과의 룬과 겹쳐 없앴고, **레벨은 2026-09-11 에 생겼다** — 그 줄이 오래 낡아 있었다) · ⛔ 추천에는 넣지 않는다 · 배선하면 `soon` 을 지운다 · 🧾 **일반 상점은 가로줄**이다(2026-09-05 사용자 확정) — **가방과 같은 짜임**: [그림] [이름 / 등급 값] … [등급 버튼 셋(`.rnBuyS`)] · ⛔ 세로 카드로 되돌리지 말 것: 룬 하나가 화면을 한 덩어리씩 먹어 「무엇을 파는가」가 한눈에 안 읽혔다 · ⚠ **추천·할인은 카드(`.rnBuy`)로 남는다** — 상점에는 모양이 둘이므로 무엇을 셀 때 **둘 다** 봐야 한다(스모크가 한쪽만 세다 터졌다) · 🛒 **카드의 얼굴**(2026-09-08 사용자 확정 · 목업 `docs/mock/sale-card-8.html` ④안 「모서리 컷 + 금속 테」): 왼쪽 위·오른쪽 아래를 **45°로 잘라낸 판**(`--cut` 7px · clip-path)이고 테두리는 border 가 아니라 **`::before` 의 그라데이션 띠**(등급색 → 금속 · 1px 만 남기고 안을 도려낸다) — ⛔ border 를 되살리지 말 것: 잘린 모서리를 못 따라가 네모가 남는다(스모크가 border 0 · polygon 을 잰다) · ⚠ clip-path 라 **바깥 그림자는 못 쓴다**(잘린다) · 값은 **제 톤다운 판**(`.rnBuy u` · 어두운 면 + 카드 폭을 채우고 맨 아래 · 같은 컷 4px) · −30% 배지는 **붉은 면에 흰 글자 + 같은 컷 3px**(⛔ 둥근 모서리로 되돌리지 말 것 — 카드가 각진데 배지만 둥글면 붙인 스티커로 보인다 · 옛 주황 반투명은 그은 제값과 섞여 안 읽혔다) · 그림 38px · 카드 최소 높이 116px · ⚠ 진한 테두리(등급색 60%)로 되돌리지 말 것 — 여섯이 나란히 서면 격자가 먼저 보이고 룬이 뒤로 물러난다 · 🏷 할인 중이면 줄에서도 말한다(배지 `.rnOffS` 는 버튼 **위로 걸친다** — 폭 60px 안에 두면 등급 이름과 부딪힌다) · 🗂 **상점의 구역은 판이 아니라 제목 + 아래 구분선**이다(2026-09-05 · 목업 `docs/mock/rune-shop-8.html` A④안) — ⛔ 판(면·테두리)을 되돌리지 말 것: 칸 테두리와 겹쳐 둘 다 흐릿해 보였다 · 🔷 **갈래 탭은 「아이콘 탭」**(`.pdSeg.stack`)이다(2026-09-05 사용자 확정 · 목업 `docs/mock/shop-icontab-4.html` ①안) — **육각 아이콘 위 · 이름 아래**, 칸마다 아주 옅은 판이 깔리고 **고른 칸만** 한 단 밝아지며 아래 **밑변 광원 한 줄**이 켜진다(화면의 다른 띠·커맨드 카드와 같은 어휘) · ⭐ 껍데기는 **공용 함수 `segNavHTML`** 그대로다 — 그림은 `label` 에 담고 세로로 세우는 일은 **공용 변형 `.stack`**(css/40-social.css) 하나가 한다 · 아이콘은 `_runeTabIco()`(육각 테두리 = 갈래 색 + 속 글리프 `RUNE_TAB_GLYPH` 넷 · 새 에셋을 만들지 않는다) · ⛔ 상점 전용 탭 함수·마크업을 새로 만들지 말 것 · 🛒 **상점은 세 구역**이다(2026-09-04 · GEM.md §8-4): **추천 3**(값은 그대로 · 바로가기) · **주간 할인 6**(−30% · **종류마다 1개** · 월요일 09:00 갱신 · 목록은 **주 번호 해시**라 같은 주엔 같다 — ⛔ Math.random 금지) · **일반**(제값 · 갈래 탭은 공용 `segNavHTML`) — ⛔ 할인에 재고를 두지 않으면 그 주의 6종을 아무도 제값에 안 사 **일반 구역이 죽는다** · 📦 **한 종류는 여덟 개까지**(`RUNE_OWN_MAX`)이고 그 위로는 버튼이 잠긴다 · 🔧 **확인용 스위치가 둘이다** — `CAMP_RUNE_FREE`(갖고 있다고 **친다** · 개수가 늘 9라 상한에 걸려 **상점에서 아무것도 못 산다**)와 `CAMP_RUNE_DEV_SEED`(저장에 **진짜로 심는다** · 종류·등급마다 3개 + 젬 999만 + 칸 전부 · 한 번만 심고 다시 안 채운다 — 그래야 젬이 빠지는 것이 보인다) · ⛔ 둘 다 내보내기 전에 false 로 되돌릴 것(스모크가 다섯 곳에서 끄고 정상 규칙을 잰다) · ⛔ 「보유 젬」 줄을 되살리지 말 것(상단 재화 바에 있다) · 값 표는 **GEM.md §8-3 단일 소스** — **젬 값은 등급이 정한다**(`RUNE_GEM`) · ⚠ **효과 값은 룬마다 다르다** — 같은 +17% 라도 실제로 닿는 폭이 **75배** 갈리기 때문이다(가속 +116% ↔ 예리 +1.5%). 전달률로 나눠 **다 채우면 실제 +15~18% 로 모이게** 맞췄다(표는 GEM.md §8-8) — ⛔ 표시값을 통일하지 말 것 · ⛔ 예리·열기(+170%) 같은 큰 숫자에 놀라 낮추지 말 것(확률에 비율을 곱하는 자리다) · ⛔ 가속(+3.4%)을 기본표로 되돌리지 말 것(시간 축이라 혼자 +116% 가 된다) · ⛔ 룬마다 값을 흩뿌리지 말 것 · ⛔ 효과를 두 자릿수로 되돌리지 말 것(「룬은 게임을 심하게 바꾸면 안 된다」 — 한 축의 상한이 **+17%**(성좌 8칸×1.5% + 유니크 5%)라는 것이 그 보장이다 · GEM.md §8-8) · ⛔ **룬 값을 실측으로 정하려 하지 말 것** — 벤치의 구매 관문이 문턱이라 1.5% 차이가 결과를 100배 갈라 놓는다(네 번 시도하고 접었다 · BALANCE §3-2-9) · **효과가 닿는 곳은 룬마다 한 자리뿐**(표는 GEM.md §8-5) — ⛔ 한 효과를 두 곳에 걸면 표기가 거짓말이 된다 · **일반 7종 · 유니크 4종**(2026-09-03) — ⛔ 「재화의 룬」(손끝을 품어서)과 「질주의 룬」(가속과 겹쳐서)을 되살리지 말 것 · 신속의 룬은 **유니크**로 올라왔다 · 유니크 값은 **넷 다 5%**(가속만 2.5% 이던 것을 접었다 — ⚠ 다시 재야 한다) · 가속의 룬 = `campFrame` 의 dt 한 줄(`campDtMul()`) · 신속의 룬만 캠프 게이트(`campRuneMulIn`)가 필요하다(16-build.js 는 관리자 탭과 공유) · 전리품의 룬은 ⛔ **젬에 안 건다** · 열기의 룬은 피버 **발동 확률**(`campFevPct`)에만 — ⛔ 지속·배수에 붙이면 피버가 상시 배수가 된다 · ✨ **치명 터치**(`campCritPct`/`campCritMul` · 예리·일격의 룬)는 피버와 **곱하지 않는다** — 피버 중에는 안 굴린다(곱하면 ×36) · ⚠ 탭 경로 둘(`campMineOnce`·`campMineTap`)은 **`campTapRoll()` 하나**를 쓴다 · 🎯 **성좌를 가운데로 데려오는 길은 `campRuneLookCons(ci)` 하나다**(2026-09-12) — 교체 대기(`campRuneSwapLook`) · 칸 고르기(`campRuneFocus`) · **빈 칸에 넣기**(사용자 요청) 셋이 그 함수를 쓴다 · ⛔ 가운데로 옮기는 길을 또 만들지 말 것: 앵커가 갈리면 같은 동작인데 화면이 다르게 선다(옛 `campRuneFocus` 는 `RUNE_MAP_H*0.34` 라는 **손으로 적은 근사값**을 썼고, 지금은 셋 다 상단 띠·가방을 **실제 높이로 재서** 남는 자리의 한가운데를 쓴다) · ⏳ **넣는 그림은 판이 다 미끄러진 뒤에 난다**(`_runeAfterLook`) — 날아갈 자리(`_runeSlotAt`)는 **화면 좌표**라 미끄러지는 중에 재면 룬이 칸이 **있던 자리**에 내린다(궤적은 고정된 keyframe) · 📏 제대로 기다리면 오차 **0px**(실측) · ⚠ **장착은 즉시**이고 미루는 것은 그림뿐이다(⛔ 애니가 끝날 때 상태를 바꾸지 말 것 · 같은 규칙) · ⏰ 기다림에는 **타이머 안전망**이 있다(`RUNE_LOOK_MAX` 900ms) — rAF 만 믿으면 탭이 가려졌을 때 비행도 도착도 안 와서 받을 칸이 `_runeVeil` 인 채 **빈칸으로 남는다**(장착은 됐는데 화면엔 없다 · 실측) · 🤏 **두 손가락 확대는 구역 전체가 받는다**(`#campRune{touch-action:none}` + `.uiScroll{touch-action:pan-y}`) — 안 막으면 **페이지째** 커졌다 작아진다(2026-09-12 사용자 신고) · ⚠ 판(`.rnMap`/`#rnSvg`)만 막으면 모자라다(가방·상단 띠 위에서 벌리면 샌다) · ⭐ 환생 트리 `#ctSvg` 와 **같은 규칙**이고 휠은 `svvBind` 의 `preventDefault` 가 막는다 · ⛔ `auto` 로 되돌리지 말 것 |
| **환생 트리(마인드맵)** | `#campTree` + `campTreeOpen()` · 노드는 `campTreeSvg()` | `#phone` 직속 전체 화면 · 밀고 확대 · 입구는 **하단 네비 「환생 › 트리」 하나**(⛔ 맵 띠에 칩을 되돌리지 말 것) · 갈래 색은 `CAMP_TREE_BR` 한 곳 |
| **가스 교환**(가스→미네랄) | **정제소 프로필의 커맨드 카드** — `techBldgPlainModel` 안 `b.gas` 분기 + `campGasExAll()` | 정제소를 지정하면 그 프로필에 카드 하나로 붙는다(2026-09-02 사용자 확정) · ⛔ **캠프에서만** — 유즈맵·오토배틀 정제소에는 안 붙는다(환생 값이 대전에 새면 안 된다) · ⛔ 옛 자리(캠프 채굴 시트 `.cmRow`)로 되돌리지 말 것 — 미네랄 판이라 가스가 남의 집에 얹혀 있었다 · ⭐ **고정 교환비가 아니다** — 지금 미네랄 수입의 몇 초치(`CAMP_GASEX_SEC`)를 준다 · ⛔ 지갑은 `campAddRes()` 한 입구 · ⚠ 카드 규약: 받는 것은 우상단 `tr`, 내는 것은 `bottom` 의 **`.cc.en` 안에** (맨 아이콘만 넣으면 크기가 안 잡힌다 · `meta` 는 이 카드 꼴에서 안 보인다) |
| **강제 튜토리얼** | `TUTO_STEPS`(js/06-daily.js) + `tutoPaint()` · 챕터는 `TUTO_CH` | 🎓 **아주 기본만**(2026-09-10 사용자 확정 · 26 → 17단계) — 모르면 게임을 시작조차 못 하는 것만 손을 잡는다. ⛔ 조작 하나하나를 강제 단계로 되돌리지 말 것: 강제가 길수록 개편 때마다 통째로 낡는다(던전 개편에서 안내 셋이 한꺼번에 거짓말이 됐다) · 나머지는 **구역 안내**로 · 📚 챕터 셋(자원 · 기지와 부대 · 출격)이고 **경계는 표가 아니라 챕터 카드 단계**다(번호도 그 사이에서 센다) · 🎁 보상은 **끝에 한 번**(젬 20 + 밑천 미네랄 500 · 그리고 판을 걷어 처음부터 · `campTutoReset`) · ⛔ 안내 문구에 화면 글자를 손으로 박지 말 것 — 던전 이름은 `campDgName`, 버튼 글자는 `_cdGoLabel`, 진행 축은 `CAMP_DG_STEPS` 에서 온다 |
| **구역 안내** | `ZONE_TIPS`(js/06-daily.js) + `zoneTipPaint()` · 입구는 `zoneTipKick()` | 🗺 그 화면에 **처음 왔을 때 한 장**(2026-09-10) — 강제 튜토리얼에서 뺀 것들이 여기 산다(연구 구역 · 부대 조작 · 화면 조작 · 적 기지) · ⭐ 껍데기는 **챕터 카드 그대로**(`.tutoOv > .tuTip.ch`) — 새 컴포넌트를 만들지 않는다 · ⛔ 화면을 **막지 않는다**(`.ztOv` 가 판 넷의 pointer-events 를 끈다) — 자유 플레이 중에 뜨는 쪽지다 · 「봤다」는 `S.zt` 에 남고 환생·튜토리얼 되감기로 안 지워진다(배운 것은 안 되감는다) · ⛔ 튜토리얼이 도는 동안에는 안 뜬다 |
| **🔁 환생 강화 구역** | **`campRebEnter('boost')` 가 유일한 입구** — 화면 `#rebUpgScreen` · 내용은 `renderRebUpg()`(19-camp.js) | 환생 구역의 **넷째 칸**(환생 · 성장 트리 · **환생 강화** · 유즈맵 강화) · ⭐ **껍데기도 줄 목록도 유즈맵 강화 것을 그대로 쓴다** — CSS 는 `#mapUpgScreen` 선택자에 **얹었고**(⛔ 규칙을 복사하지 말 것) 줄은 **`ptRowsHTML(pred, src)`** 한 함수가 만든다(09-usemap-base.js · 여기서 주는 것은 **출처** `_rebUpgSrc` 뿐이다) · ⛔ 두 번째 줄 렌더러·전용 CSS 블록 금지 · 🔷 갈래 탭은 룬 상점·유즈맵 강화와 같은 **아이콘 탭**(`segNavHTML` + `.stack`)이고 그림은 **`_PT_ICO` 의 키**를 쓴다(⛔ 없는 키를 주면 육각만 그려지고 속이 빈다) · 표는 `CAMP_REB_UPG`(갈래 `CAMP_REB_UPG_GRP` — 스킵·자동화·상한 해제) · 지갑은 `C.rbPts` · 보유는 `C.rbUpg`(**영구** — `campRunReset` 이 안 지운다) · ⛔ **「세지는 것」을 팔지 말 것**(레벨·성장 트리의 몫) · ⛔ **성장 트리와 같은 효과를 팔지 말 것**(인구 상한·시작 미네랄·시작 일꾼 · 스모크가 키 겹침을 잰다) · 🚨 **`dgStart.max` ≤ `CAMP_DG_MAX − 1`** — 마지막 던전까지 열면 환생 직후부터 관문이 참이라 **무한 환생**이 된다(스모크가 잰다) · 🤖 **켜고 끄는 줄**은 `onoff:true` + `max:1`(자동 강화 · 자동 원정) — 힌트가 값이 아니라 **「켜짐」**이다 · 🚫 **일부러 안 넣은 것 셋**(보급소 상한 · 오프라인 수익률 배수 · 시작 자원/일꾼)은 전부 **트리와 같은 자리**라 넣으면 안 된다 — 이유는 `ARCHITECTURE.md` §「🔁 환생 강화」 표 · ✅ **항목끼리의 값 순서를 쟀다**(2026-09-11 · `node scripts/reb-upg-bench.mjs` · BALANCE §5-19) — **고칠 것이 없다**: 포인트당 분당 미네랄이 `capWk` 373 · `autoTap` 243 · `awayCap` 153 으로 **2.4배 안**이라 죽은 줄도 압도하는 줄도 없다(재구매 완화는 병력 +32~39% · 던전 개방은 시작 던전 1 → 3 이라 돈으로 환산 안 한다) · ⚠ **`autoTap` 은 탭 레벨에 통째로 매달린다**(포인트당 Lv.0 **4** → Lv.60 **664** · 166배) — ⛔ 탭 레벨을 안 밝히고 이 줄의 값을 논하지 말 것 · ✅ **자동화 넷이 지갑을 안 비운다**(다 켜도 번 돈의 **51%** 가 남고 수입은 +55%) — `CAMP_AUTOUPG_KEEP`(4) 이 제 일을 한다 · ⛔ 낮추지 말 것 · ⚠ 아직 안 잰 것: **켜고 끄는 넷의 값 순서**(손이 덜 가는 것이라 돈으로 환산하지 않았다 · `autoWk` 16 · `autoRes` 18 은 여전히 근거 없는 출발점이다) |
| **🗺 유즈맵 강화 구역** | **`campRebEnter('umap')` 가 유일한 입구** — 화면 `#mapUpgScreen` · 내용은 `renderMapUpg()`(12-appshell.js) | 🔁 **환생 구역의 셋째 칸**이다(2026-09-10 사용자 확정 · 환생 · 환생 트리 · **유즈맵 강화**) — 포인트를 주는 쪽이 환생이라 「버는 곳 옆에서 쓴다」 · ⛔ 유즈맵 구역으로 되돌리지 말 것(거기 있다가 옮겨 왔다) · ⛔ `mapUpgOpen()` 을 직접 부르지 말 것 — `campRebEnter` 가 나머지 둘을 닫아 준다(안 닫으면 겹쳐 뜬다) · 껍데기는 환생·룬과 **같은 규격**(`#phone` 직속 · z 120 · `bottom:var(--navH)` · 배경은 공용 `#campRebBg` 한 장 — `_rebArtAnyOn` 에 이 화면도 세어 넣었다: 안 세면 칸을 옮길 때 그림이 꺼진다) · 🏷 이름은 `campZoneTitle()` 이 말한다(⛔ 화면이 `curSetTitle` 을 직접 부르지 말 것) · 🔷 갈래 탭은 **룬 상점과 같은 아이콘 탭**(`.pdSeg.stack` + `segNavHTML` · 아이콘은 `_PT_ICO` 를 육각에 넣는다 — ⚠ `_PT_ICO` 는 **선 그림**이라 fill 로 그리면 뭉개진다 · ⛔ 청록(--acc-sel)을 쓰지 말 것: 「지금 선택된 것」 전용) · **두 단**이다: 맵 목록 → 고른 맵의 강화(무장 칸의 「계열 고르기 → 항목」과 같은 어법) · ⭐ **뒷단을 새로 만들지 않는다** — 표는 `META_BUILDS`(03-meta.js), 저장은 `PLAYER_META.buildLevels`, 판 적용은 `G.metaB` 로 **공학소와 한 벌**이다(⛔ 두 벌이 되면 값이 갈린다) · 🏷 표의 꼬리표 둘이 「어디서 사는가」를 정한다: **`map`**(어느 유즈맵) · **`out:true`**(바깥에서 산다 · 없으면 **공학소**) — 지금 바깥 23 / 공학소 8(뽑기 2 · 자동화 6) · ⭐ 가른 기준은 「이 판을 편하게」(안) ↔ 「앞으로 늘 강해지게」(바깥) · 🧱 **강화 줄은 `ptRowsHTML(pred)`(09-usemap-base.js) 한 함수**가 만든다 — 두 화면이 거르개만 달리 준다(⛔ 두 번째 줄 렌더러 금지) · 🧭 탭은 `ptTabsFor` 가 **실제로 있는 갈래만** 세운다(⛔ 칸 수를 코드·스모크에 박지 말 것 — 표가 단일 소스) · 🪙 지갑은 **`PLAYER_META.coins` 하나**(판 보상도 환생 보상도 여기로) — ⛔ 새 재화를 만들지 말 것 · ⚠ **환생 보상은 아직 이 지갑에 안 붙었다**(다른 작업) — 지급 한 줄만 나중에 붙인다 · ⛔ 화면 CSS 에 `display` 를 쓰지 말 것: `#아이디` 가 `.appScreen.hide` 를 이겨 **화면이 영영 안 숨는다**(실측: 게임 중 조작 버튼·☰ 를 통째로 먹었다) · ⛔ 맵 그림에 이모지를 박지 말 것 — 목록과 같은 `mapThumbHTML`·`MAP_ACCENT` 를 쓴다 · ⚠ 항목이 있는 맵만 목록에 선다 · ⚙ **오토 배틀(cpu) 6항목**(2026-09-10): 시작 자금 · 광산 값 인하 · 광산 수입 · 기본 수입 · 유닛 공격력 · 유닛 체력 — 값은 `cpuBonus()`(09-usemap-base.js) 한 곳이 정한다 · ⚠ **유닛 강화는 약하다**(다 채워도 공격 +8% · 체력 +10%) — ⛔ 두 자릿수로 올리지 말 것: 오토 배틀은 **8인 대전**이라 세지면 「오래 한 사람이 이긴다」가 된다(스모크가 상한을 잰다) · 💰 값은 **영향 순으로 비싸진다**(시작 자금 660 < 광산 값 2,200 < 광산 수입 3,300 < 기본 수입 4,400) — 뒤로 갈수록 판 내내 복리로 불어난다(스모크가 순서를 잰다) · ⛔ **캠프로 새지 않게 하는 문은 `stkUpgOn(side)`(18-strike.js) 하나**다 — 캠프 전투가 이 파일의 부품(`strikeAtkMul`·`strikeSpawnUnit`)을 그대로 빌려 쓰므로 **함수를 나누는 것으로는 못 가른다**: ①캠프가 아니다 ②지금 맵이 cpu 다 ③내 진영이다 셋을 다 본다(하나라도 빼면 캠프 밸런스나 상대 컴퓨터에 샌다 · 스모크가 ①②를 잰다) · ⚠ 시작 자금·광산 값은 **판 시작 때 한 번**만 건다(`strikeNewState` 안에서는 `STK` 가 아직 없어 진영을 못 가린다) · 🚪 **나가는 길**: 화면 목록은 **`NAV_TREE` 의 `reb.subs`(07-home-upgrade.js) 하나**다 — 칸마다 `scr`(요소 id)·`shut`(닫는 함수)을 들고 있고 `rebZoneShut()`/`rebZoneScreens()`/`rebZoneIsOn()` 셋이 그것만 읽는다(쓰는 곳 셋: `navShow` 가 구역을 떠날 때 · `curSplitSync` 가 띠를 맞출 때 · `campRebEnter` 가 나머지를 닫을 때) · ⛔ **화면 id 를 다른 곳에 또 적지 말 것** — 목록이 셋으로 갈려 있었고 그중 `navShow` 것만 옛 둘(campReb·campTree)에 멈춰 있어서, 나중에 붙은 유즈맵 강화·환생 강화가 **구역을 떠나도 안 닫혔다**(2026-09-12 사용자 신고: 「나오려고 하면 화면이 계속 남아 다른 구역으로 못 가거나 겹쳐 보인다」 — 둘 다 z 120 전체 화면이라 다음 화면을 통째로 덮는다) · ⚠ 스모크는 **칸 하나가 아니라 규칙**을 잰다(구역 안에서 옮기면 하나만 열림 · 어느 칸에서 나가도 전부 닫힘) — ⛔ 그 스텝에 화면 id 를 손으로 적지 말 것(다섯째 칸이 생기면 저절로 걸려야 한다) · ⚠ 「칸마다 나갔다 들어오는」 식으로 재면 **겹침 검사가 통째로 거짓**이 된다(나가면서 다 닫히므로 겹칠 수가 없다 · 실측으로 한 번 밟았다) |
| **일일 퀘스트** | `openDaily()` → `#hbDailySheet` + `renderDaily()` | 더보기 ☰ > 일일 퀘스트 · 하루 5개 + 주간 25개 |
| **출석** | `openAtt()` → `#hbAttSheet` + `renderAtt()` | 더보기 ☰ > 출석 · **퀘스트와 화면이 다르다**(같은 판에 탭으로 묶지 말 것 — 2026-08-14 분리) |
| 일일 진행 계측·보상 | **`dqNote(kind, n)`** / `dqGive(rw)` | 출석·퀘스트가 함께 쓰는 뒷단 · 새 계측 지점이 생기면 `dqNote` 한 줄만 넣는다 |
| HOME 팝업 껍데기·버튼 색 | `.hbModal` / `.hbmCard` / `.hbRow` · 버튼 면색 토큰은 `.hbModal{--btnA/B/C}` | 새 HOME 팝업을 만들 때 `#새시트 .ecGo` 같은 선택자를 **덧붙이지 말 것** — `.hbModal` 하위면 자동으로 같은 물성·회색톤이 된다 |
| **둘 중 하나 고르기(토글)** | **`.setSeg` + `.segBtn`** (`css/10-game.css`) | 설정 > 그래픽 품질(절전/고화질) = 방 만들기 공개/비공개 — 한 컴포넌트(2026-08-27 통일) · **탭 띠(`.pdSeg`)와 다르다**: 칸이 둘이고 「고른 것이 켜진 면」으로 읽혀야 할 때만 이것 · 액센트는 화면이 `--setAcc` 로 정한다(빨강이 주 동작인 화면에선 중립 밝은 색) · ⛔ 새 토글을 만들지 말 것 |
| 세그먼트 이동 바(한 구역 안에서 나뉘는 네비 = **모든 탭 띠**) | **`segNavHTML(items, i, act)`** → `.pdSeg`/`.pdSegInd`/`.pdSegBtn` | 장비창 섹션 바(장비·장신구) = 사냥터 업그레이드 탭 = 유즈맵 정렬 띠 = **채팅/파티/친구**(`renderSocialTabs`) = **친구 필터**(`renderHubFriendTabs`) = **코인 공학소**(`renderPtTabs`) — 한 함수 · ⛔ **탭 띠는 이것 하나뿐이다**(2026-08-27 통일 · 옛 사본 `.msTab2`·`.ptTab` 은 없앴고 스모크 「옛 사본이 되살아나지 않았다」가 잡는다) · 버튼 안 끝에 뭘 붙여야 하면 `tail` 슬롯(친구 배지) · **글자만**(아이콘을 같이 넣으면 한 덩어리로 가운데 정렬돼 글자가 밀린다) · 생김새도 공용 규칙이 전부 갖는다(각진 3px · 검정 판 · 밑변 1px 광원) · 화면이 덮는 건 광원 색 `--segCol` 하나 · 변형이 필요하면 **공용 클래스로 추가**한다(지금 둘: `.plain` = 판 없는 띠(유즈맵 정렬) · `.stack` = 아이콘 위·이름 아래(룬 상점)) — ⛔ 화면 전용 탭을 새로 짜지 말 것 · 안쪽 틈은 `--pad` 변수 하나가 단일 소스(계산식을 복사해 박지 말 것) |

> **아이콘 일반 원칙:** 새 UI를 만들 때 **기존 에셋을 먼저 찾아 쓴다**(`assets/icons/` — 재화 4종 · `buildings/` 52 · `skills/` 28 · `upgrades/` 24 · **`tree/` 33**). 같은 뜻의 아이콘이 이미 있으면 그것을 쓰고, 없을 때만 대체 표기를 쓴다. **새로 뽑을 땐 `ART.md` §15**(규격·공통 블록·컷아웃 도구) — ⛔ 배경 지우는 코드를 직접 짜지 말 것.

## Scope — when to run the full workflow (operating mode: A)
Scale the process to the change:
- **Substantial work** (new feature, gameplay system, architecture, multi-step change — e.g. the 직스/strike combat system): run the FULL workflow below — brainstorm → clarify → propose 2-3 approaches → get approval → plan → implement → verify → review.
- **Small, unambiguous changes** (numeric/size/layout/color tweaks, copy edits, obvious bug fixes — e.g. "make the temple smaller", "widen the road"): proceed directly, then verify in the live app. No clarifying questions or approach proposals needed.
- When unsure which bucket a request is in, ask one quick question instead of assuming.

## Core Philosophy
You are a Senior Software Engineer following the "Superpowers" methodology. You do NOT just write code; you engineer solutions. You must follow this strict workflow for every feature request:

1.  **Brainstorming & Requirements**: Clarify intent before planning.
2.  **Planning**: Create a detailed plan before coding.
3.  **TDD**: Write tests first, then implementation. *(In this repo: verify behavior in the live app first — see Project Context.)*
4.  **Review**: Self-review code against the plan and best practices.

## Rules & Workflow

### 1. 🧠 Brainstorming First (No Code Yet)
- When I ask for a new feature, DO NOT write implementation code immediately.
- Instead, ask clarifying questions to narrow down requirements.
- Propose 2-3 different architectural approaches with trade-offs.
- Wait for my approval on the approach.
- *(Small, unambiguous tweaks may proceed directly.)*

### 2. 📋 Plan & Design
- Once an approach is selected, write a step-by-step implementation plan.
- List all files to be created or modified.
- Define the exact function signatures and data structures.
- **Output:** A distinct "Implementation Plan" section.

### 3. 🔴🟢 Test-Driven Development (TDD)
- **Step 1 (Red):** Write a failing test case that covers the requirement. Run it to confirm failure.
- **Step 2 (Green):** Write the *minimum* code necessary to pass the test.
- **Step 3 (Refactor):** Clean up the code while ensuring tests still pass.
- **Constraint:** Never write implementation code without a corresponding test.
- *(This repo has no test runner → substitute live-app verification: capture current behavior, make the minimum change, re-verify the new behavior.)*

### 4. 🔍 Systematic Debugging
- If a test fails or an error occurs, do not blindly try fixes.
- **Phase 1:** Analyze the error log and stack trace.
- **Phase 2:** Formulate a hypothesis about the root cause.
- **Phase 3:** Create a reproduction script/test to prove the hypothesis.
- **Phase 4:** Apply the fix and verify.

> **⛔ 증상부터 만지지 말 것 — 흐름부터 파악 (MANDATORY).**
> 버그·이상 동작 요청이 오면, **관련 코드 경로 전체를 먼저 끝까지 읽고 근본 원인을 특정한 뒤** 손댄다. 증상만 보고 눈에 띄는 값·파라미터부터 조정하는 것 금지.
> - 특히 **상호작용/입력 버그**(터치·롱프레스·드래그·선택 등)는 관련 핸들러 흐름(예: `techPtrDown → techPtrMove → techPtrUp`, 그리고 tick 루프)을 **먼저 통째로 따라가** 어느 지점이 상태를 덮어쓰는지 찾는다.
> - "왜 이 증상이 나는가"를 한 문장으로 설명할 수 없으면 아직 고치지 마라.
> - 선례: 벙커/커널 입장 버그 — 실제 원인은 이동 명령(`_btCmd`)이 입장 명령(`_boardTgt`)을 취소하던 것이었는데, 증상만 보고 throttle 간격부터 손대서 한 번 헛돌고 오히려 악화됨. 처음부터 포인터 핸들러 흐름을 읽었으면 한 번에 끝났을 일.

### 5. 📝 Final Review
- Before finishing, run a self-review:
    - Does the code match the plan?
    - Are there any hardcoded values or magic numbers?
    - Is the code readable and documented?
