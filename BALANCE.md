# BALANCE — 성장 곡선 핸드오프

> **이 파일 하나만 보고 새 세션에서 밸런스 작업을 이어갈 수 있도록** 정리한 문서.
> *왜 그렇게 설계했는가*는 `ARCHITECTURE.md`(§성장 축 · §던전 곡선 · §환생)가 단일 소스다.
> 여기 적는 것은 **지금 값 · 실측 기준선 · 남은 일 · 재는 방법** 넷뿐이다.
>
> ⚠ 값을 바꿨으면 **반드시 §4 로 다시 재고 §2 표를 갱신할 것.** 표만 고치면 거짓말이 된다.

---

## 0. 한 장 요약 — 성장 축 넷의 역할

이 게임의 성장은 축이 넷이고 **역할이 전부 다르다.** ⛔ 섞지 말 것 — 지수 축이 둘이 되면 반드시 한쪽이 폭주한다.

| 축 | 역할 | 성장 | 환생하면 |
|---|---|---|---|
| **미네랄 업그레이드** (`hunt.upg`) | 한 사이클 안에서 쌓는 것 | `log(미네랄)` — 값은 덧셈인데 비용이 곱셈이라 눌린다 | 0으로 |
| **레벨 포인트** (`unit.pts`) | 가끔 눌러 두면 조용히 붙는 것 | 선형 `1+n×0.05` | 0으로 |
| **환생 포인트** (`unit.rpts`) | **던전을 뚫는 힘** | **복리 `1.27^칸`** ← 유일한 지수 축 | **남는다** |
| **환생 배수** (`c.rebMul`) | 사이클을 빠르게 돌리는 것 | 선형 `(Lv-100)×0.01` | 남는다(누적) |

**왜 지수 축이 하나는 있어야 하나** — 적 체력이 라운드에 대해 지수(`1.10^prog` 대)라, 어떤 축도 지수가 아니면 진행이 멎는다. 실측: 미네랄을 ×1300만 벌어도 라운드가 88에서 멈췄다.

**왜 배수는 선형이어야 하나** — 기하로 두면 배수가 XP 수입을 올리고 → 다음 사이클 레벨이 오르고 → 배수가 `g^레벨`이라 또 커진다. 되먹임이다. 실측 5회 만에 **Lv1411 · 배수 ×1900만**.

---

## 1. 환생 규칙 (2026-08-19 확정)

- **`PROF_REB_MIN_LV`(100)부터 언제든, 몇 번이든.** 레벨 상한 없음.
- 보상은 **회차가 아니라 그때의 레벨**이 정한다.
- `c.rebLvMax`가 같은 레벨 재환생을 막는다(늘 지난번보다 높아야 한다).
- 2회차부터 **유즈맵 포인트 관문** `(레벨-100)×3` — 첫 환생(딱 Lv100)만 무료.

| | 되돌아감 | 남음 |
|---|---|---|
| | 레벨 · 경험치 · 유닛 레벨 · 레벨 포인트 | 환생 포인트(찍은 것 포함) |
| | **`hunt.upg`** (미네랄 업그레이드) | **`hunt.unl`** (업그레이드 해금) |
| | **`p.pcoin`** (미네랄 재화) | **`hunt.best`** (최고 기록) |
| | **`hunt.dg`/`hunt.round` → 1-1** | 배수 누적 · 장비 · 펫 · 가스 · 젬 · 뽑기권 |

⭐ **1-1로 되돌아가되 "깼던 구간은 열려 있다"** — `hunt.best`를 안 지우므로 라운드 선택·던전 이동으로 곧장 복귀한다. `hbDoRebirth`가 토스트로 알려 준다.
⛔ `hunt.best`를 같이 지우지 말 것 — 던전 해금과 복귀 경로가 통째로 사라진다.

---

## 2. 지금 값 (2026-08-19)

### 경험치 곡선
| 상수 | 값 | 뜻 |
|---|---|---|
| `PROF_LV_SOFT` | **100** | 여기까지는 멱함수(싸다) · 첫 환생 레벨과 같아야 한다 |
| `PROF_XP_A` / `PROF_XP_P` | **16 / 1.15** | `lv<100`: `16×lv^1.15` — Lv100 누적 ≈ 14.7만 |
| `PROF_XP_GEO` | **1.012** | `lv≥100`: 레벨당 배수. 60레벨마다 ×2.05 |

### 환생
| 상수 | 값 | 뜻 |
|---|---|---|
| `PROF_REB_MIN_LV` | **100** | 첫 환생 |
| `REB_LIN` | **0.01** | 배수 = `(Lv-100)×이 값` (선형) |
| `PROF_REB_RP_K` | **60** | 포인트 = `1 + 60×ln(1+Lv-100)` |
| `PROF_REB_POINT_R` | **3** | 유즈맵 포인트 관문 = `(Lv-100)×이 값` |
| `PROF_REB_COIN_R` | **0.7** | 미네랄 배수 = `1 + 0.7×rebMul` |

### 포인트
| 상수 | 값 | 뜻 |
|---|---|---|
| `LP_PER_LEVEL` / `LP_STEP` | **1 / 0.05** | 레벨당 1점 · 1점 +5% **선형** |
| `RP_STEP` | **0.27** | 1점 ×1.27 **복리** |
| `ptCostAt('rp')` | `1+floor(k/5)` | 재투자 체증 → 칸 = `√(10P)`(네 배 모아야 두 배) |
| `ptCostAt('lp')` | `1` 고정 | ⛔ 체증 넣지 말 것 — '조용히 붙는' 축의 성격이 어긋난다 |

### 던전·라운드
| 상수 | 값 | 뜻 |
|---|---|---|
| `HB_ROUND_MAX` | **99** | 던전 하나 = 99라운드. 99 클리어 시 자동 이동 |
| `HB_ROUND_HP` | **1.10** | **던전 1** 라운드당 적 체력 |
| `HB_ROUND_HP_D` | **0.035** | 던전마다 더하는 값 → `hbRoundHp(dg)` |
| `HB_ROUND_ATK` | **1.13** | 던전 1 라운드당 적 공격 · `hbDgK`로 같이 가팔라진다 |
| `HB_ROUND_REW` | **1.14** | 던전 1 라운드당 재화 보상 |
| `HB_ROUND_XP` | **1.03** | 던전 1 라운드당 경험치 — **제일 완만해야 '벽'이 생긴다** |
| `HB_ROUND_S` | **0.5** | 던전 안 S자 세기(0=균일) · **총량 불변** |
| `HB_DG_ENTER` | **40** | 던전 최초 진입 보너스(클리어 보너스의 배수) |
| `HB_DG_MAX` / `HB_DG_UNLOCK` | **10 / 99** | 던전 수 · 다음 던전 해금 조건 |

**던전별 기울기**

| 던전 | 라운드당 체력 | 던전 총 배수 | 라운드당 보상 |
|---|---|---|---|
| 1 | ×1.100 | ×3.1e4 | ×1.140 |
| 2 | ×1.135 | ×6.6e5 | ×1.185 |
| 3 | ×1.170 | ×1.3e7 | ×1.231 |
| 4 | ×1.205 | ×2.4e8 | ×1.277 |
| 10 | ×1.415 | — | — |

---

## 3. 실측 기준선 (§4 방법으로 잰 값)

**조건**: 신규 계정 · 던전 1 시작 · 자동 구매/배분 · 벽에서 25게임분 정체 시 환생 · **환생 뒤 최고 기록으로 복귀**.

| 던전 완주(다음 던전 진입) | 목표 Lv | **실측 Lv** | 시각 | 환생 |
|---|---|---|---|---|
| 던전 1 | 250~300 | **288** | 2.3h | 1 |
| 던전 2 | 550 | **662** | 2.9h | 1 |
| 던전 3 | 1300 | **1,188** | 5.5h | 3 |
| 던전 4 | 2000 | **1,817** | 9.5h | 6 |

⚠ **목표 네 지점은 하나의 매끄러운 곡선으로 정확히 맞지 않는다.** D2→D3 간격이 2.7배로 튄다(275→750). 지금은 `250~300 / 550 / 950~1200 / 1400~1800` 로 읽는 것이 실제 곡선에 맞다.

**조정 레버**
- 던전:레벨 대응 → `HB_ROUND_HP`(던전1 기울기) · `HB_ROUND_HP_D`(던전마다 더하는 값)
- "뒤 던전일수록 더 드는가"의 모양 → `PROF_REB_RP_K`(지급식). 선형이면 거꾸로 줄어들고, √도 부족했고, **log라야** 뒤가 더 든다.
- 사이클 길이 → `RP_STEP`(내리면 길어짐)

---

## 4. 재는 방법 — 엔진 자동 플레이

⚠ **모델로 추정하지 말 것.** 이 세션에서 해석적 추정이 여러 번 크게 빗나갔다(회수 시간·손익분기·레벨 간격 전부). **실제 `hbStep` 루프를 돌려서 재는 것만 믿을 것.**

```bash
npx -y serve            # .claude/launch.json 의 starcraft-web
```

브라우저 콘솔(또는 preview 도구)에서:

```js
// 신규 계정으로 초기화
const p=PROF(); p.chars.length=0; p.curId=''; p.pcoin=0; p.gas=0;
PLAYER_META.coins=1e12;                       // 유즈맵 포인트 관문은 이 측정의 주제가 아니다
p.hunt={dg:1,round:1,climb:true,climbChosen:1,best:{},rw:{},mates:{},party:[],upg:{},unl:{},dgIn:{}};
profCreateChar('ranger','시뮬'); saveMeta();
hbEnd(); openHome();                          // 잠깐 기다린 뒤
_hb.manual=true;                              // ⚠ 인터벌 시계를 끄고 hbStep 을 직접 돌린다
```

그 다음 루프를 돌린다. 요지는 넷:
1. **자동 구매** — 살 수 있는 것 중 가장 싼 업그레이드를 계속 산다(해금 포함)
2. **자동 배분** — `lpAdd`/`rpAdd` 로 남은 포인트를 소진
3. **환생 판단** — 전역 진행도(`hbProg`)가 25게임분간 안 오르고 `profCanRebirth`면 환생
4. ⭐ **환생 뒤 복귀** — `hbGoDungeon(최고던전)` + `hbSetRound(최고라운드)`.
   ⛔ 이걸 빼면 매 사이클 1-1부터 다시 걷느라 결과가 통째로 틀어진다(실제로 한 번 헛짚었다).

**도구 타임아웃 주의** — 한 번에 20초 남짓만 돌리고, 상태를 `window.__sim` 같은 곳에 두어 여러 번 나눠 이어 돌린다.

---

## 5. 남은 일

### A. 검증이 안 된 것 (먼저 할 것)

| # | 무엇 | 왜 급한가 | 어떻게 확인 |
|---|---|---|---|
| **A1** | **유즈맵 포인트 관문이 감당 가능한가** | `(Lv-100)×3` 이라 Lv1817 환생이면 **5,151 포인트**. 유즈맵에서 그만큼 나오는지 안 쟀다. **안 나오면 후반에 환생이 막힌다** | 유즈맵 한 판 포인트 수입 × 하루 판 수를 재고, §3 의 환생 주기(2~3시간)와 대조 |
| **A2** | **던전 5~10 구간** | 던전 10은 라운드당 체력 ×1.415(던전당 ×2e14). 실제로 뚫리는지 안 쟀다 | §4 시뮬을 던전 10까지 이어 돌린다 |
| **A3** | **던전 진입 보너스 체감** | `HB_DG_ENTER=40` 을 넣었지만 "확 늘었다"로 느껴지는지 안 봤다 | 진입 순간의 보너스 / 그 시점 시급 비율을 잰다 |

| **A4** | **자동 토벌 배속(`HB_SUB_MAX=16`)** | 자동 토벌은 "실제 걸리는 시간보다 훨씬 짧게" 끝나야 한다(사용자 확정). 배속은 `dt` 를 키우는 게 아니라 **작은 `dt` 로 여러 번 미는 것**이라, 배속을 올리면 그만큼 CPU 를 쓴다. 16배가 프레임을 잡아먹지 않는지 안 쟀다 | 자동 토벌 세션을 켠 채 사냥터 FPS 를 잰다. 떨어지면 상한을 낮추거나 배경 세션의 인터벌을 늘린다 |

| **A5** | **토벌 보상 배수(`DG_CUR_MUL=6` · `DG_GAS_RATE=0.35`)** | 일반 토벌은 "미네랄·가스 **대량**"이 존재 이유다(사용자 확정). 6배가 대량으로 느껴지는지, 그리고 사냥터 방치 수입을 압도해 사냥터를 무의미하게 만들지 않는지 **안 쟀다** | 같은 시간 동안 ① 사냥터 방치 수입 ② 일반 토벌 열쇠 2개 소모분을 각각 재서 비율을 본다. 토벌 쪽이 몇 배면 사냥터가 죽는다 |

> ⚔ **토벌 종류표(2026-08-20 확정).** 종류를 가르는 것은 **보상 성격**이다. 단계 진행도는 **종류마다 따로** 쌓인다(`c.dgFloors`).
>
> | 종류 | 해금 | 보상 |
> |---|---|---|
> | 일반 토벌 | Lv.1 | 미네랄·가스 대량(`DG_CUR_MUL`) |
> | 장비 토벌 | Lv.10 | 장비 뽑기권 |
> | 펫 토벌 | Lv.30 | 펫 뽑기권 |
> | 동료 토벌 | Lv.50 | 동료 뽑기권 |
> | 룬 토벌 | Lv.100 | 룬 뽑기권 (룬 시스템은 후속) |
>
> ⚠ **해금 레벨(`reqLv`)과 단계 상한(`dgFloorCap` → `DG_LV_PER_FLOOR`)은 다른 문이다.** 한 값으로 합치지 말 것.
>
> 📈 **보상은 단계가 깊을수록 커진다**(2026-08-20 사용자 확정 — "초반은 적게, 라운드가 올라갈수록 조금씩 더해지면서").
> 재화는 `40+n*22` 로 이미 선형이고, 🎟 뽑기권은 `dgTixN(n)=1+⌊(n-1)/DG_TIX_STEP⌋`(`DG_TIX_STEP=5` → 1~5단계 1장 · 6~10단계 2장 · …).
> ⛔ 뽑기권을 항상 1장으로 되돌리지 말 것 — 깊이 갈 이유가 재화밖에 안 남아 종류를 나눈 뜻이 반쯤 사라진다.
>
> 🧹 **소탕(이전 단계)과 입장(다음 단계)은 같은 보상표·같은 지급 함수**(`dgFloorReward` → `dgGrantReward`)를 쓴다.
> 계획서 원문이 "이전 스테이지 토벌 시 **해당 스테이지의 보상이 즉시 지급**"이다. 다른 것은 '전투가 없다'와 '이미 깬 단계까지만'뿐 —
> **소탕 = 천장에서 수확 / 입장 = 천장을 민다.** ⛔ 지급 입구를 둘로 나누지 말 것(옛 코드가 그래서 소탕만 뽑기권을 못 받았다).

> ⚔ **자동 / 직접(2026-08-20 확정).** 다른 점은 셋뿐이다 — ① 자동은 화면에 안 들어간다 ② 자동은 **제자리에서** 싸운다 ③ 자동은 `DG_AUTO_SPEED`(10) 배속.
> ⛔ 자동에도 접근 이동을 켜면 둘이 같아져 '직접'을 고를 이유가 사라진다. 직접이 이동·후퇴·카이팅으로 이길 확률을 올리는 것이 두 갈래를 나눈 이유다.
>
> **실측(2026-08-20 · Lv.40 · upg atk30/hp30/aspd10/crit10):** 한 판 시뮬 길이 1단계 12.4s · 5단계 19.9s · 10단계 패배 7.5s.
> 10배속 실제 소요 = **승리 2.9초 / 패배 1.5초** (헤드리스 rAF 기준, 실기기는 더 짧다). 요구 "거의 1초정도"에 부합.
> ✅ 4단계에서 사냥터 엔진(`hbPumpAll`, setInterval)으로 옮겨 **탭을 내려도 돈다.** 실측 재확인: 사냥터가 배경에서 도는 중 자동 토벌 1단계 = **2.0~2.2초**, 사냥터 라운드·처치는 그대로.
> ⚠ 직접 전투는 아직 옛 DG 화면이다(5단계에서 옮긴다).

| **A6** | **토벌 단계 → 사냥터 곡선 매핑(`DG_ROUND_PER_FLOOR=1`)** | 토벌 단계 n 을 사냥터 라운드 n 으로 놓았다. **토벌이 사냥터보다 쉬우면 아무도 사냥터를 안 한다**(열쇠가 있어 총량은 막히지만, 체감 난이도가 뒤집히면 안 된다) | 같은 스펙으로 사냥터 라운드 n 과 토벌 단계 n 을 각각 돌려 클리어율·소요를 비교 |

> ⏩ **배속 규약(2026-08-20 확정).** `S.speed` = 배속, `HB_SUB_MAX` = 상한(지금 16). `hbPump()` 가 한 번의 실제 경과시간 `dt` 를 `speed` 등분해 `hbStep` 을 그만큼 부른다.
> ⛔ **`hbStep(dt*배속)` 으로 구현하지 말 것** — 충돌·사거리 판정이 통째로 샌다(적이 벽을 통과하고 사거리를 건너뛴다). 스모크 「배속은 dt 를 키우지 않고 잘게 쪼갠다」가 스텝 횟수·크기·상한을 직접 검사한다.
> ⚠ 배속은 **체감 시간**만 줄인다 — 전투 결과(클리어 확률·보상)는 1배와 같아야 한다. 다르게 나오면 그건 배속이 아니라 버그다.

### B. 설계가 비어 있는 것

| # | 무엇 | 메모 |
|---|---|---|
| **B1** | **Lv200 이후 RP 추가 사용처** | 사용자 요청: "환생을 많이 하면 이후로 추가 업그레이드 개방". 지금 RP는 7축에만 쓴다 |
| **B2** | **반복 작업 자동화** | 사용자 요청: "이전에 했던 반복작업은 자동으로". 환생 후 복귀를 지금은 토스트로 **안내만** 한다 → **자동 복귀 버튼**이 자연스러운 첫 걸음 |
| **B3** | 유즈맵으로 주 컨텐츠 이동 | 사냥터는 "눌러 놓고 떠나는 곳"으로 확정. 유즈맵 쪽 설계는 다른 세션이 진행 중 |

### C. 되돌려야 할 임시 설정

| # | 무엇 | 메모 |
|---|---|---|
| **C1** | `HB_DG_ALL_OPEN = true` | 밸런스 확인용 **전체 개방**. 진행 설계를 실제로 체감하려면 `false` 로 돌려야 한다 |

---

## 6. 이 세션에서 실제로 밟은 함정 (같은 실수 반복 금지)

| 함정 | 무슨 일이 있었나 |
|---|---|
| **기하 배수 되먹임** | 배수를 XP 곡선과 같은 기하로 두면 배수→레벨→배수로 순환한다. 5회 만에 Lv1411 · ×1900만 |
| **어떤 축도 지수가 아님** | 미네랄은 `log`, LP는 선형이라 라운드가 88에서 멎었다. RP 복리가 그 자리를 맡는다 |
| **던전에서 내려올 길 없음** | 자동 이동으로 올라간 뒤 약해지면 그 던전 1라운드에 갇혔다(40시간 헛돔) → `hbRetreatDungeon()` |
| **시뮬이 복귀를 안 함** | 환생 후 1-1부터 걷게 두면 "진행도를 유지해야 한다"는 **엉뚱한 결론**이 나온다. 실제 플레이어는 되돌아간다 |
| **퍼센트 padding** | CSS `padding:%` 는 높이가 아니라 **폭** 기준. 8%를 줬는데 높이의 3.9%만 움직였다 |
| **부동소수에 묻힘** | 잔고가 1e20이면 +5가 사라진다. 테스트는 제 조건(낮은 라운드·잔고 0)을 직접 세워야 한다 |
| **테스트가 앞 스텝에 기댐** | 던전 스텝이 앞 스텝이 남긴 업그레이드로 통과하고 있었다. 스텝은 **순서와 무관**해야 한다 |
| **`window.` 로 읽기** | `_hb` 는 `let` 선언이라 `window._hb` 는 `undefined`. 전투가 안 도는 것처럼 보였다 |

---

## 7. 관련 문서

| 파일 | 무엇 |
|---|---|
| `ARCHITECTURE.md` | **왜 그렇게 만들었나** — 성장 축 · 던전 곡선 · 환생 · 배선 지점 |
| `DESIGN.md` | 시각 규칙(각진 SF · 볼륨 3단 · 라운드 0/3/6/9) |
| `CLAUDE.md` | 작업 규칙 — 단일 소스 원칙 · `npm test` 필수 |
| `test/smoke.js` | 위 규칙을 **강제하는** 검사들(환생 · 던전 곡선 · 포인트 · 업그레이드 배선) |
